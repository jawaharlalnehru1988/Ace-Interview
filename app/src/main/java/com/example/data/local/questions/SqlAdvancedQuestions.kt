package com.example.data.local.questions

import com.example.data.local.entity.QuestionEntity
import com.example.data.local.questions.QuestionHelper.q

object SqlAdvancedQuestions {

    fun getAll(): List<QuestionEntity> {
        return part1() + part2() + part3() + part4()
    }

    private fun part1(): List<QuestionEntity> = listOf(
        q(
            id = "sql_adv_001",
            categoryId = "sql",
            title = "PostgreSQL MVCC: Tuple Header Visibility Fields (xmin, xmax)",
            prompt = "In PostgreSQL's MVCC implementation, how do the hidden tuple header fields `xmin` and `xmax` determine row visibility for a reading transaction?",
            opt0 = "xmin is the minimum column value; xmax is the maximum column value",
            opt1 = "`xmin` is the Transaction ID that created (inserted) this tuple version",
            opt2 = "xmin and xmax are GPS latitude and longitude coordinates",
            opt3 = "xmin is the file offset on disk; xmax is the memory address",
            correctIndex = 1,
            explanation = "In PostgreSQL, every tuple header stores 23 bytes including `xmin` (creator transaction ID) and `xmax` (destroyer transaction ID). A query's snapshot compares its active transaction IDs against xmin/xmax to decide visibility.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,MVCC,xmin,xmax,Internals"
        ),
        q(
            id = "sql_adv_002",
            categoryId = "sql",
            title = "MySQL InnoDB MVCC: Hidden Clustered Columns and Undo Log Pointers",
            prompt = "What three hidden system columns does MySQL InnoDB append to every row in a clustered index?",
            opt0 = "ROW_ID, TRANSACTION_ID, and CHECKSUM by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "`DB_TRX_ID` (6-byte ID of last modifying transaction), `DB_ROLL_PTR` (7-byte pointer to undo log record holding historical image), and `DB_ROW_ID` (6-byte synthetic PK if no PK exists)",
            opt2 = "CREATED_AT, UPDATED_AT, and DELETED_AT by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "PAGE_NO, OFFSET, and LENGTH to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "InnoDB rows store `DB_TRX_ID` and `DB_ROLL_PTR`. When a transaction reads an older snapshot, it follows `DB_ROLL_PTR` into the Undo Log, reconstructing older historical row versions on-the-fly.",
            difficulty = "Advanced",
            tags = "SQL,MySQL,InnoDB,MVCC,UndoLog,DB_ROLL_PTR"
        ),
        q(
            id = "sql_adv_003",
            categoryId = "sql",
            title = "PostgreSQL MVCC vs MySQL InnoDB MVCC Architecture",
            prompt = "What is the fundamental architectural trade-off between PostgreSQL's Append-Only MVCC versus MySQL InnoDB's Undo-Log-Based MVCC?",
            opt0 = "PostgreSQL runs in RAM; MySQL runs on disk without scanning unindexed pages during complex join queries",
            opt1 = "PostgreSQL writes new tuple versions directly into the main table data pages (causing table bloat requiring VACUUM)",
            opt2 = "InnoDB cannot support transactions to enforce referential constraints and table consistency",
            opt3 = "PostgreSQL cannot use B-Tree indexes to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Postgres inserts new versions directly in heap pages (producing bloat and requiring vacuuming). InnoDB writes changes in-place and stores historical snapshots in Undo Logs, discarding undo blocks once transactions finish.",
            difficulty = "Advanced",
            tags = "SQL,DatabaseInternals,PostgreSQL,InnoDB,MVCCComparison"
        ),
        q(
            id = "sql_adv_004",
            categoryId = "sql",
            title = "Crash Recovery: ARIES Algorithm",
            prompt = "What are the three canonical phases of C. Mohan's ARIES (Algorithms for Recovery and Isolation Exploiting Semantics) crash recovery protocol?",
            opt0 = "Compile, Link, and Execute by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "1. Analysis Phase (scans WAL forward to identify active transactions and dirty pages)",
            opt2 = "Backup, Validate, and Restore to enforce referential constraints and table consistency",
            opt3 = "Read, Write, and Verify without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "ARIES is the foundational crash-recovery algorithm implemented in SQL Server, DB2, and PostgreSQL: Analysis reconstructs database state; Redo repeats history; Undo reverses uncommitted active transactions.",
            difficulty = "Advanced",
            tags = "SQL,StorageEngines,ARIES,CrashRecovery,WAL"
        ),
        q(
            id = "sql_adv_005",
            categoryId = "sql",
            title = "ARIES Crash Recovery: Compensation Log Records (CLRs)",
            prompt = "Why does the ARIES recovery algorithm write 'Compensation Log Records' (CLRs) during the Undo Phase, and what critical guarantee do they provide?",
            opt0 = "CLRs calculate financial compensation for server downtime",
            opt1 = "CLRs log the undo actions themselves",
            opt2 = "CLRs compress the log file by 50%",
            opt3 = "CLRs notify the application of transaction rollback",
            correctIndex = 1,
            explanation = "CLRs ensure idempotent crash recovery. Each CLR records that an undo operation completed: on repeated crash during recovery, the engine skips already-undone actions, guaranteeing recovery progress.",
            difficulty = "Advanced",
            tags = "SQL,StorageEngines,ARIES,CLRs,IdempotentRecovery"
        ),
        q(
            id = "sql_adv_006",
            categoryId = "sql",
            title = "Log Sequence Numbers (LSN) and Fuzzy Checkpointing",
            prompt = "How does the Log Sequence Number (LSN) on physical database disk pages enable recovery to know whether a committed write has reached disk?",
            opt0 = "LSNs store the physical GPS coordinates of the database",
            opt1 = "Each disk page stores `page_LSN` (the LSN of the last log record that modified it)",
            opt2 = "LSNs are encrypted using public key cryptography to enforce referential constraints and table consistency",
            opt3 = "LSNs reset to zero every midnight to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "LSNs are monotonically increasing byte offsets in the WAL. Comparing log record LSN against the page's embedded `page_LSN` allows the recovery engine to skip already-persisted changes instantly.",
            difficulty = "Advanced",
            tags = "SQL,StorageEngines,LSN,WAL,CrashRecovery"
        ),
        q(
            id = "sql_adv_007",
            categoryId = "sql",
            title = "Database Latches vs Database Locks",
            prompt = "In database engine internals, how does a 'Latch' differ fundamentally from a 'Transaction Lock'?",
            opt0 = "Latches are in Java; Locks are in SQL to enforce referential constraints and table consistency",
            opt1 = "Locks are transactional logical constructs (shared/exclusive on rows/tables, held for the duration of a transaction, detected for deadlocks)",
            opt2 = "Latches persist on disk; Locks reside in memory to enforce referential constraints and table consistency",
            opt3 = "There is no difference; they are exact synonyms by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "Locks protect logical database integrity (tables, rows) throughout transaction lifecycles. Latches are low-level C++ synchronization primitives (rwlocks/spinlocks) protecting in-memory data structures for microseconds.",
            difficulty = "Advanced",
            tags = "SQL,DatabaseInternals,Latches,Locks,Concurrency"
        ),
        q(
            id = "sql_adv_008",
            categoryId = "sql",
            title = "Bw-Tree Architecture (Microsoft Hekaton)",
            prompt = "What breakthrough does Microsoft's Bw-Tree (used in Azure SQL and Cosmos DB) introduce to eliminate latch contention in high-concurrency B-Trees?",
            opt0 = "It converts the B-Tree into a flat text file to prevent lock contention and serialization failures on tables",
            opt1 = "It is completely lock-free: updates append immutable Delta Records to a node's delta chain via an atomic CAS on a logical Page Mapping Table, consolidating pages asynchronously without holding physical memory latches",
            opt2 = "It runs entirely on GPU processors by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "It stores data in alphabetical order only by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "The Bw-Tree decouples logical page IDs from physical memory pointers via a mapping table. Updates prepend delta records to the page's memory chain using atomic CAS, achieving lock-free/latch-free B-Tree concurrency.",
            difficulty = "Advanced",
            tags = "SQL,DataStructures,BwTree,LockFree,Hekaton"
        ),
        q(
            id = "sql_adv_009",
            categoryId = "sql",
            title = "Compaction Amplification: Write, Read, and Space Amplification",
            prompt = "In Log-Structured Merge (LSM) storage engines (RocksDB, Cassandra), what do the three fundamental trade-off metrics (WA, SA, RA) represent?",
            opt0 = "Width, Size, and Radius of the disk to enforce referential constraints and table consistency",
            opt1 = "Write Amplification (bytes written to storage per logical user byte written), Space Amplification (total disk storage consumed divided by logical uncompressed data size), and Read Amplification (disk reads per logical get)",
            opt2 = "Warnings, Alerts, and Rejections to prevent lock contention and serialization failures on tables",
            opt3 = "Windows, Apple, and RedHat metrics by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "The 'RUM Conjecture' governs storage: you cannot optimize all three simultaneously. Optimizing for Write Amplification (STCS) increases Space/Read Amplification; optimizing for Read Amplification (LCS) increases Write Amplification.",
            difficulty = "Advanced",
            tags = "SQL,StorageEngines,LSMTree,AmplificationMetrics,RUMConjecture"
        ),
        q(
            id = "sql_adv_010",
            categoryId = "sql",
            title = "LSM Compaction: Size-Tiered (STCS) vs Leveled (LCS)",
            prompt = "What is the primary architectural trade-off between Size-Tiered Compaction Strategy (STCS) and Leveled Compaction Strategy (LCS)?",
            opt0 = "STCS is for numbers; LCS is for strings without scanning unindexed pages during complex join queries",
            opt1 = "STCS has lower Write Amplification (groups SSTables of similar sizes) but high Space Amplification (requires up to 50% free disk space for compaction)",
            opt2 = "LCS runs in memory; STCS runs on tape drive to enforce referential constraints and table consistency",
            opt3 = "STCS cannot use Bloom filters by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "STCS merges files of similar size, requiring up to 50% free disk space during major compactions. LCS divides data into levels where keys do not overlap within a level, slashing space and read amplification at the cost of high write rewriting.",
            difficulty = "Advanced",
            tags = "SQL,StorageEngines,LSMTree,STCS,LCS,RocksDB"
        ),
        q(
            id = "sql_adv_011",
            categoryId = "sql",
            title = "PostgreSQL Full-Text Search: tsvector vs tsquery",
            prompt = "In PostgreSQL full-text search, what is the technical distinction between a `tsvector` and a `tsquery`?",
            opt0 = "tsvector is for numbers; tsquery is for text by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "`tsvector` is a sorted list of distinct normalized lexemes (stemmed words stripped of stop words) with word positions",
            opt2 = "tsvector is stored in RAM; tsquery is stored on disk without scanning unindexed pages during complex join queries",
            opt3 = "tsquery is written in Python by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "A document is parsed into a `tsvector` (e.g. 'jump':1 'quick':2). A search query is parsed into a `tsquery` (e.g. 'quick & jump'). Matching is evaluated via the match operator `tsvector @@ tsquery`.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,FullTextSearch,tsvector,tsquery"
        ),
        q(
            id = "sql_adv_012",
            categoryId = "sql",
            title = "PostgreSQL GIN Fastupdate Buffer and Vacuum",
            prompt = "What is PostgreSQL GIN's `fastupdate` buffer, and why can it cause unexpected latency spikes on GIN-indexed tables?",
            opt0 = "It updates table statistics automatically without scanning unindexed pages during complex join queries",
            opt1 = "To avoid slow in-place insertions into the main GIN index tree on every write, GIN queues inserts in a pending list page buffer",
            opt2 = "It compresses GIN indexes into ZIP files to prevent lock contention and serialization failures on tables",
            opt3 = "Fastupdate is only used in test databases by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "Fastupdate buffers writes in a pending page list. When it fills (`gin_pending_list_limit`), an arbitrary incoming INSERT is forced to pause and merge all pending entries into the main GIN tree, causing latency spikes.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,GINIndex,fastupdate,Performance"
        ),
        q(
            id = "sql_adv_013",
            categoryId = "sql",
            title = "Google Spanner: TrueTime Architecture",
            prompt = "How does Google Spanner achieve globally consistent distributed ACID transactions without cross-datacenter locking coordination on read queries?",
            opt0 = "By routing all queries through a single master computer in California to enforce referential constraints and table consistency",
            opt1 = "TrueTime API: synchronized atomic clocks and GPS receivers in every datacenter provide bounded clock uncertainty \$\\epsilon\$",
            opt2 = "By disabling database transactions on replicas without scanning unindexed pages during complex join queries",
            opt3 = "By running queries on quantum computers by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "TrueTime bounds clock skew \$[t.earliest, t.latest]\$ with uncertainty \$\\epsilon\$. If a transaction commits at timestamp \$s\$, it waits \$2\\epsilon\$ before releasing locks, guaranteeing that any subsequent transaction anywhere in the world receives a timestamp > s.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,GoogleSpanner,TrueTime,CommitWait"
        ),
        q(
            id = "sql_adv_014",
            categoryId = "sql",
            title = "Strict Serializability (External Consistency)",
            prompt = "What formal guarantee does 'Strict Serializability' (External Consistency) provide beyond standard Serializability?",
            opt0 = "It ensures that code compiles with zero warnings to prevent lock contention and serialization failures on tables",
            opt1 = "It combines Serializability (transactions appear to execute in some serial order) with Linearizability (if Transaction B starts globally in real wall-clock time AFTER Transaction A commits, Transaction B is guaranteed to observe Transaction A's writes)",
            opt2 = "It eliminates the need for database backups by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "It requires all transactions to run on a single CPU core without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "Standard serializability permits transactions to be ordered arbitrarily as long as an equivalent serial order exists. Strict serializability enforces that the equivalent serial order strictly respects real-world wall-clock causality.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,StrictSerializability,Linearizability,ConsistencyModels"
        ),
        q(
            id = "sql_adv_015",
            categoryId = "sql",
            title = "CockroachDB: Hybrid Logical Clocks (HLC)",
            prompt = "Why does CockroachDB use Hybrid Logical Clocks (HLC) instead of Google Spanner's TrueTime hardware?",
            opt0 = "CockroachDB is written in Go; Spanner is in C++ to prevent lock contention and serialization failures on tables",
            opt1 = "HLC runs on commodity cloud virtual machines without requiring expensive dedicated GPS antennas or atomic clocks, combining physical NTP time with Lamport logical counters to track causality across distributed nodes",
            opt2 = "HLC eliminates clock synchronization entirely to enforce referential constraints and table consistency",
            opt3 = "HLC uses 90% less memory than TrueTime to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Commodity cloud VMs lack atomic clocks. HLC combines physical NTP time with a logical tick counter $(l, c)$, maintaining physical clock proximity while strictly preserving causal happens-before relationships.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,CockroachDB,HLC,DistributedClocks"
        ),
        q(
            id = "sql_adv_016",
            categoryId = "sql",
            title = "Multi-Raft Architecture in Distributed SQL",
            prompt = "In distributed SQL databases (CockroachDB, TiDB, YugabyteDB), what is 'Multi-Raft'?",
            opt0 = "Running multiple copies of Raft in separate terminal windows",
            opt1 = "The database keyspace is divided into millions of contiguous 64MB-512MB 'Ranges'",
            opt2 = "A protocol for connecting databases over Wi-Fi to prevent lock contention and serialization failures on tables",
            opt3 = "Running Raft consensus on single database tables only",
            correctIndex = 1,
            explanation = "A single cluster-wide Raft log would bottleneck throughput at tens of thousands of writes/sec. Multi-Raft runs millions of independent, parallel Raft consensus groups across discrete ranges.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,MultiRaft,CockroachDB,Consensus"
        ),
        q(
            id = "sql_adv_017",
            categoryId = "sql",
            title = "Distributed SQL: Leaseholder Pattern",
            prompt = "In CockroachDB's Multi-Raft architecture, what is the role of the 'Leaseholder' replica for a Range?",
            opt0 = "The node that pays for cloud hosting billing to prevent lock contention and serialization failures on tables",
            opt1 = "A single designated replica that holds a time-bounded lease: it serves strictly linearizable read queries directly in O(0) consensus rounds without executing Raft quorum, and orchestrates write proposals to the Raft log",
            opt2 = "The replica that stores database backups by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "The replica that runs schema migrations by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "Serving reads via standard Raft requires contacting a quorum of nodes. The Leaseholder holds an exclusive lease, guaranteeing that no writes can commit without its knowledge, allowing it to serve reads locally in 0ms.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,CockroachDB,Leaseholder,Optimization"
        ),
        q(
            id = "sql_adv_018",
            categoryId = "sql",
            title = "Distributed Two-Phase Commit (2PC) over Multi-Raft",
            prompt = "How do distributed SQL databases execute a cross-range transaction affecting Range 1 and Range 2 atomically?",
            opt0 = "By merging Range 1 and Range 2 into a single range permanently by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "A Coordinator node executes Two-Phase Commit (2PC) across the Leaseholders of Range 1 and Range 2, where each phase's state transition (Prepare, Commit) is replicated via Raft within its respective range",
            opt2 = "By locking the entire database cluster for 1 second without scanning unindexed pages during complex join queries",
            opt3 = "Cross-range transactions are forbidden in distributed SQL to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Distributed transactions combine 2PC with Raft: 2PC coordinates the atomic commit across disparate ranges, while Raft ensures that each individual participant's 2PC state is fault-tolerant.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,2PC,MultiRaft,DistributedTransactions"
        ),
        q(
            id = "sql_adv_019",
            categoryId = "sql",
            title = "Distributed Deadlock Prevention: Wound-Wait vs Wait-Die",
            prompt = "In distributed transaction management, how does the 'Wound-Wait' non-preemptive deadlock prevention algorithm resolve lock conflicts?",
            opt0 = "By randomly killing 50% of active transactions to prevent lock contention and serialization failures on tables",
            opt1 = "Older transaction T_old requests a lock held by younger transaction T_young: T_old 'wounds' (aborts and restarts) T_young",
            opt2 = "All transactions wait until a timer expires to prevent lock contention and serialization failures on tables",
            opt3 = "The transaction with the lower priority wins without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "Wound-Wait uses transaction age timestamps. Older transactions pre-emptively abort younger ones ('wounding' them); younger transactions patiently wait for older ones, mathematically preventing circular wait deadlocks.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,DeadlockPrevention,WoundWait,WaitDie"
        ),
        q(
            id = "sql_adv_020",
            categoryId = "sql",
            title = "Cost-Based Optimizer: System R Dynamic Programming Algorithm",
            prompt = "How does the classic Selinger System R algorithm find the optimal join order for an N-table join query in exponential \$O(3^N)\$ time?",
            opt0 = "By testing all permutations randomly to prevent lock contention and serialization failures on tables",
            opt1 = "Bottom-up Dynamic Programming: it first finds the best single-table access paths",
            opt2 = "By evaluating tables in the order they appear in the FROM clause",
            opt3 = "By converting the query to a recursive CTE to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Pat Selinger's 1979 algorithm is the foundation of relational optimizers: it builds join trees bottom-up with memoization, retaining only the lowest-cost plans for each sub-combination of tables.",
            difficulty = "Advanced",
            tags = "SQL,QueryOptimizer,SystemR,Selinger,DynamicProgramming"
        ),
        q(
            id = "sql_adv_021",
            categoryId = "sql",
            title = "Query Optimizer: Interesting Orders Concept",
            prompt = "In query optimization, why does the optimizer intentionally retain a higher-cost join plan if it produces an 'Interesting Order'?",
            opt0 = "Because the query optimizer likes interesting things to prevent lock contention and serialization failures on tables",
            opt1 = "An access path produces output sorted by a key requested by a downstream `ORDER BY`, `GROUP BY`, or subsequent Merge Join",
            opt2 = "It is used only for debugging optimizer bugs without scanning unindexed pages during complex join queries",
            opt3 = "Interesting orders bypass table permissions to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "An 'Interesting Order' is a physical sort order that matches a downstream query requirement. Even if an Index Scan is slightly more expensive than a Seq Scan, avoiding a separate downstream Sort step saves overall time.",
            difficulty = "Advanced",
            tags = "SQL,QueryOptimizer,InterestingOrders,Selinger,Sorting"
        ),
        q(
            id = "sql_adv_022",
            categoryId = "sql",
            title = "Query Optimizer: Volcano and Cascades Frameworks",
            prompt = "What architectural paradigm shift do the Volcano and Cascades optimizer frameworks (used in SQL Server, Calcite, CockroachDB) introduce over System R?",
            opt0 = "They compile SQL into C++ binaries to prevent lock contention and serialization failures on tables",
            opt1 = "Rule-based, extensible, top-down goal-driven transformation search: query plans are represented as algebraic relational operator trees transformed via declarative equivalence rules with memoization (MEMO structure)",
            opt2 = "They eliminate cost-based optimization without scanning unindexed pages during complex join queries",
            opt3 = "They run queries on volcanic geothermal energy to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Goetz Graefe's Cascades framework separates transformation rules from the search engine. Operators are transformed via algebraic rules (e.g. FilterPushdown, JoinCommutativity), stored in an efficient equivalence class MEMO container.",
            difficulty = "Advanced",
            tags = "SQL,QueryOptimizer,Cascades,Volcano,Calcite"
        ),
        q(
            id = "sql_adv_023",
            categoryId = "sql",
            title = "Query Optimization: Predicate Pushdown",
            prompt = "What is 'Predicate Pushdown' in relational algebra query optimization?",
            opt0 = "Pushing WHERE clauses to the end of the query string by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "Moving filtering predicates as deep down the operator tree as possible (pushing filters below JOINs down to the base table scans), drastically reducing the volume of rows that must be joined and shuffled across memory",
            opt2 = "Converting WHERE clauses into HAVING clauses by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "Deleting non-matching rows from the database to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Joining two 1-Million row tables and then filtering by date takes gigabytes of RAM. Pushing the filter down to the scans filters both tables to 1,000 rows BEFORE joining them, speeding up queries by 1000x.",
            difficulty = "Advanced",
            tags = "SQL,QueryOptimizer,PredicatePushdown,RelationalAlgebra"
        ),
        q(
            id = "sql_adv_024",
            categoryId = "sql",
            title = "Query Optimization: Join Elimination",
            prompt = "Under what exact database schema conditions can a Cost-Based Optimizer eliminate a `LEFT JOIN` table entirely from an execution plan (Join Elimination)?",
            opt0 = "When the joined table has fewer than 10 rows without scanning unindexed pages during complex join queries",
            opt1 = "1. No columns from the right table are projected in SELECT or referenced in WHERE",
            opt2 = "When the join condition uses greater-than (>) to prevent lock contention and serialization failures on tables",
            opt3 = "Join elimination is illegal in SQL standard to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "If `customers` has a unique PK `id`, `SELECT o.* FROM orders o LEFT JOIN customers c ON o.customer_id = c.id` can eliminate `customers` completely: the unique key guarantees zero row duplication and no columns are used.",
            difficulty = "Advanced",
            tags = "SQL,QueryOptimizer,JoinElimination,Optimization"
        ),
        q(
            id = "sql_adv_025",
            categoryId = "sql",
            title = "Query Optimization: Outer-to-Inner Join Conversion",
            prompt = "When does the query optimizer automatically convert an expensive `LEFT OUTER JOIN` into a fast `INNER JOIN` under the hood?",
            opt0 = "When the query author adds a comment to prevent lock contention and serialization failures on tables",
            opt1 = "When the `WHERE` clause contains a strict 'Null-Rejecting Predicate' on any column from the right table",
            opt2 = "When the query runs in under 1 second to prevent lock contention and serialization failures on tables",
            opt3 = "When the table has more than 3 indexes to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "If the WHERE clause tests `WHERE right.col = 'foo'`, that predicate evaluates to FALSE for NULLs. Any row that didn't match the outer join is filtered out anyway. The optimizer converts it to INNER JOIN, unlocking join reordering.",
            difficulty = "Advanced",
            tags = "SQL,QueryOptimizer,OuterToInnerJoin,NullRejectingPredicate"
        )
    )

    private fun part2(): List<QuestionEntity> = listOf(
        q(
            id = "sql_adv_026",
            categoryId = "sql",
            title = "PostgreSQL MVCC: HOT Chain Pointers and Line Pointers",
            prompt = "In PostgreSQL heap pages, how do Page Line Pointers (`item pointers` / `ctid`) maintain index stability during Heap-Only Tuple (HOT) updates?",
            opt0 = "By rewriting all secondary indexes on the table without scanning unindexed pages during complex join queries",
            opt1 = "The index continues pointing to the original line pointer (ItemPointer 1)",
            opt2 = "By storing pointers in an external file on disk to enforce referential constraints and table consistency",
            opt3 = "By disabling concurrent reads to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Line pointer redirection allows tuples to update in-page: external indexes still point to root slot 1. Slot 1 redirects to slot 2, avoiding expensive B-Tree index updates completely.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,HOT,LinePointers,PageInternals"
        ),
        q(
            id = "sql_adv_027",
            categoryId = "sql",
            title = "Change Data Capture (CDC): MySQL Binlog Formats",
            prompt = "Why does enterprise Change Data Capture (Debezium / Kafka Connect) strictly mandate MySQL binary logging to be set to `binlog_format = ROW` instead of `STATEMENT`?",
            opt0 = "ROW format consumes less network bandwidth by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "`STATEMENT` logs the raw SQL text (which is non-deterministic: `UUID()`, `NOW()`, or `LIMIT` without ORDER BY produce different data on replicas)",
            opt2 = "STATEMENT format is deprecated in MySQL 8 to prevent lock contention and serialization failures on tables",
            opt3 = "ROW format is only supported on Linux to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Statement logging records SQL text. Non-deterministic functions (`NOW()`, `RAND()`) diverge on downstream consumers. Row-based logging captures exact physical row byte deltas, ensuring 100% CDC fidelity.",
            difficulty = "Advanced",
            tags = "SQL,CDC,Debezium,MySQL,Binlog,Replication"
        ),
        q(
            id = "sql_adv_028",
            categoryId = "sql",
            title = "Change Data Capture: PostgreSQL Logical Decoding (pgoutput)",
            prompt = "How does PostgreSQL's Logical Decoding mechanism extract structured change streams from the append-only Write-Ahead Log without polling tables?",
            opt0 = "By running `SELECT *` on every table every 100ms to prevent lock contention and serialization failures on tables",
            opt1 = "A Logical Replication Slot uses an output plugin to read raw binary WAL records directly off disk/memory, decode them into row-level change events, and track consumer ACK offsets via LSN positions",
            opt2 = "By installing triggers on every table in the schema to enforce referential constraints and table consistency",
            opt3 = "By writing changes to a temporary table without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "Logical replication slots decode WAL changes as they are committed. It reads the WAL stream with near-zero overhead on active tables, tracking consumption via confirmed LSN flush pointers.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,LogicalDecoding,pgoutput,CDC"
        ),
        q(
            id = "sql_adv_029",
            categoryId = "sql",
            title = "PostgreSQL: Abandoned Replication Slot Disk Outage Hazard",
            prompt = "What catastrophic infrastructure failure occurs if an application creates a PostgreSQL Logical Replication Slot and permanently disconnects without dropping the slot?",
            opt0 = "PostgreSQL refuses all new client connections without scanning unindexed pages during complex join queries",
            opt1 = "Disk Exhaustion Crash: PostgreSQL CANNOT delete or recycle any WAL segment files created since the slot's last confirmed restart_lsn",
            opt2 = "The database automatically drops the slot after 1 hour without scanning unindexed pages during complex join queries",
            opt3 = "All tables become read-only without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "PostgreSQL guarantees durability to replication slots: it refuses to delete WAL segments until the slot confirms reading them. An abandoned slot holds back WAL purging until the disk hits 100% and crashes.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,ReplicationSlots,DiskOutage,ProductionHazards"
        ),
        q(
            id = "sql_adv_030",
            categoryId = "sql",
            title = "Zero-Downtime Schema Migrations: Expand-Contract (Parallel Run) Pattern",
            prompt = "How do you rename a production column `customer_phone` to `phone_number` on a 500-Million row table without any downtime or deployment locking?",
            opt0 = "Run `ALTER TABLE customers RENAME COLUMN customer_phone TO phone_number;` during peak hours",
            opt1 = "Expand-Contract Pattern: 1. Add new column `phone_number`",
            opt2 = "Export the table to CSV, rename the header, and re-import",
            opt3 = "Take the website down for 8 hours on Sunday morning",
            correctIndex = 1,
            explanation = "In-place column renames break running application code. The Expand-Contract pattern adds the new column, dual-writes in application code, backfills history, cuts over reads, and finally contracts (drops old column).",
            difficulty = "Advanced",
            tags = "SQL,Migrations,ZeroDowntime,ExpandContract,DevOps"
        ),
        q(
            id = "sql_adv_031",
            categoryId = "sql",
            title = "Zero-Downtime Schema Migrations: Online DDL Tools (gh-ost / pt-online-schema-change)",
            prompt = "How does GitHub's `gh-ost` tool execute heavy table alterations (e.g. altering column types on a 1TB table) in MySQL without locking tables or triggering trigger performance degradation?",
            opt0 = "By rewriting MySQL source code to prevent lock contention and serialization failures on tables",
            opt1 = "It creates a ghost copy table with the new schema, streams historical rows in small batches, tails the MySQL binary log (binlog) like a replica to apply real-time concurrent mutations, and executes an atomic table rename cutover",
            opt2 = "By running ALTER TABLE in a low-priority thread by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "By taking the database offline for 5 minutes to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "`gh-ost` acts as a binlog replica client: it copies rows in small throttleable batches and replays live binlog events onto the ghost table. An atomic metadata swap cuts over tables with sub-second locking.",
            difficulty = "Advanced",
            tags = "SQL,MySQL,OnlineDDL,ghost,ZeroDowntime,GitHub"
        ),
        q(
            id = "sql_adv_032",
            categoryId = "sql",
            title = "CQRS and Event Sourcing: Relational Event Store Optimistic Concurrency",
            prompt = "In an Event Store implemented on PostgreSQL (`events` table: `stream_id, version, payload`), how is concurrent append race condition detected without table locks?",
            opt0 = "By locking the entire events table with an exclusive lock without scanning unindexed pages during complex join queries",
            opt1 = "Composite Unique Constraint `UNIQUE (stream_id, version)`: when appending event \$N\$, the transaction asserts `version = expected_version + 1`",
            opt2 = "By comparing timestamps in application memory to prevent lock contention and serialization failures on tables",
            opt3 = "By generating random UUID version numbers to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "A unique index on `(stream_id, version)` provides bulletproof optimistic concurrency. If two threads attempt to append version 4 to the same aggregate stream simultaneously, the database rejects the second with a unique violation.",
            difficulty = "Advanced",
            tags = "SQL,EventSourcing,CQRS,OptimisticConcurrency,UniqueConstraint"
        ),
        q(
            id = "sql_adv_033",
            categoryId = "sql",
            title = "PostgreSQL: CREATE INDEX CONCURRENTLY Mechanics",
            prompt = "How does `CREATE INDEX CONCURRENTLY` in PostgreSQL build a B-Tree index on a 100-Million row table without taking an exclusive write lock on the table?",
            opt0 = "It builds the index in RAM and never writes to disk to prevent lock contention and serialization failures on tables",
            opt1 = "It executes two separate table scan passes: Pass 1 builds the index while concurrent writes continue (flagged as non-ready for queries)",
            opt2 = "It takes an exclusive lock for only 10 milliseconds to enforce referential constraints and table consistency",
            opt3 = "It creates the index on read replicas first by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "Standard `CREATE INDEX` takes a ShareLock, blocking all concurrent INSERT/UPDATE/DELETE. `CONCURRENTLY` takes a weak lock, executing two table passes and waiting for overlapping transactions, keeping writes 100% active.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,CreateIndexConcurrently,ZeroDowntime,Locking"
        ),
        q(
            id = "sql_adv_034",
            categoryId = "sql",
            title = "PostgreSQL: INVALID Index from Failed CONCURRENTLY Build",
            prompt = "What state is left in the database if a `CREATE INDEX CONCURRENTLY` statement fails mid-build due to a unique constraint violation or client cancellation?",
            opt0 = "The table is automatically rolled back and deleted to enforce referential constraints and table consistency",
            opt1 = "An `INVALID` index remains registered in the database catalog: it is NOT used by queries, but every subsequent INSERT and UPDATE statement is forced to maintain and write to this invalid index",
            opt2 = "The database crashes and restarts by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "The index is automatically fixed by autovacuum by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "Because `CONCURRENTLY` runs outside a transaction, a failure does not roll back catalog entries. The invalid index remains, degrading write performance until manually purged via `DROP INDEX`.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,InvalidIndex,CreateIndexConcurrently,Pitfalls"
        ),
        q(
            id = "sql_adv_035",
            categoryId = "sql",
            title = "PostgreSQL: Extended Statistics for Correlated Columns",
            prompt = "When two columns in a table have strong physical correlation (e.g. `make = 'Audi'` and `model = 'A4'`), why does the standard query optimizer severely underestimate matching rows, and how is it fixed?",
            opt0 = "The optimizer ignores the model column entirely to prevent lock contention and serialization failures on tables",
            opt1 = "The optimizer assumes column independence (\$P(A \\cap B) = P(A) \\times P(B)\$), multiplying probabilities and estimating tiny row counts",
            opt2 = "By combining both columns into a single string to enforce referential constraints and table consistency",
            opt3 = "By disabling cost-based optimization to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Standard stats assume independent probabilities. If make='Audi' matches 1% and model='A4' matches 1%, it estimates \$0.01 \\times 0.01 = 0.0001\$ (disastrously underestimating). Extended statistics capture joint distributions.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,ExtendedStatistics,CorrelatedColumns,QueryTuning"
        ),
        q(
            id = "sql_adv_036",
            categoryId = "sql",
            title = "Distributed SQL: Range Splitting and Rebalancing",
            prompt = "In distributed SQL systems (CockroachDB / TiDB), what triggers an automatic Range Split and how is it executed?",
            opt0 = "When the clock reaches midnight without scanning unindexed pages during complex join queries",
            opt1 = "When a Range grows beyond its configured size limit: the Raft leader proposes a special `Split` command into the Raft log",
            opt2 = "When a node crashes without scanning unindexed pages during complex join queries",
            opt3 = "Range splits can only be initiated manually by administrators to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Ranges split when exceeding byte limits. The split is an internal Raft log operation: it updates range boundaries in cluster metadata, allowing subsequent background rebalancers to move one half to another node.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,RangeSplit,MultiRaft,Scalability"
        ),
        q(
            id = "sql_adv_037",
            categoryId = "sql",
            title = "PostgreSQL GiST (Generalized Search Tree) Indexing",
            prompt = "What architectural property of GiST (Generalized Search Tree) allows indexing complex geometric polygons, geographic shapes, and date ranges (`tsrange`)?",
            opt0 = "GiST stores shapes as JPEG images by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "It is an extensible height-balanced search tree where keys are arbitrary Predicates",
            opt2 = "GiST replaces B-Trees for integer primary keys without scanning unindexed pages during complex join queries",
            opt3 = "GiST runs in user-space memory only to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "B-Trees require a total strict ordering ($<$ or $>$) which 2D polygons lack. GiST supports generalized predicates (enclosure, overlap, intersection), serving as the backbone of PostGIS and range types.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,GiST,SpatialIndex,PostGIS,RangeTypes"
        ),
        q(
            id = "sql_adv_038",
            categoryId = "sql",
            title = "Database Indexing: SP-GiST (Space-Partitioned GiST)",
            prompt = "When is an SP-GiST (Space-Partitioned Generalized Search Tree) index chosen over a standard GiST index in PostgreSQL?",
            opt0 = "For audio and video files to prevent lock contention and serialization failures on tables",
            opt1 = "For non-balanced, asymmetric space-partitioning structures, ideal for telephone numbers with shared prefixes or spatial data with extreme density clustering",
            opt2 = "When the table has no primary key to enforce referential constraints and table consistency",
            opt3 = "SP-GiST is only available on 32-bit operating systems by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "SP-GiST implements space partitioning (recursively splitting search space into non-overlapping regions like Quadtrees or Tries), outperforming standard balanced trees for prefix and clustered spatial searches.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,SPGiST,DataStructures,Quadtree,Trie"
        ),
        q(
            id = "sql_adv_039",
            categoryId = "sql",
            title = "Database Storage: Slotted-Page Architecture Internals",
            prompt = "In relational storage engines (PostgreSQL, SQLite), how does the internal 'Slotted-Page' binary layout organize rows within an 8KB data block?",
            opt0 = "Rows are written as comma-separated text strings from left to right by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "The page header is at the top; Line Pointers (slots) grow downward from the top; actual tuple data rows grow upward from the bottom of the page; free space sits in the middle between slots and tuples",
            opt2 = "Rows are compressed into a single binary tree by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "Tuples are stored on even pages; headers on odd pages to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Slotted-page layout grows pointers downward and row bytes upward. This allows rows to vary in length while giving each row an immutable slot index pointer that never changes even if rows are compacted.",
            difficulty = "Advanced",
            tags = "SQL,StorageEngines,SlottedPage,DatabaseInternals,PageLayout"
        ),
        q(
            id = "sql_adv_040",
            categoryId = "sql",
            title = "PostgreSQL: Tuple De-duplication in B-Trees (Version 12+)",
            prompt = "How did PostgreSQL 12+ slash B-Tree index storage by up to 40% on columns with duplicate values?",
            opt0 = "By converting indexes into hash tables to prevent lock contention and serialization failures on tables",
            opt1 = "B-Tree Deduplication: when multiple index entries share the identical key value, PostgreSQL stores the key once followed by a packed array of tuple item pointers (posting list)",
            opt2 = "By deleting duplicate rows from the table to prevent lock contention and serialization failures on tables",
            opt3 = "By compressing index pages with GZIP to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Historically, each duplicate key stored its own separate B-Tree leaf entry. Postgres 12+ combines identical keys into a single key with an array of TID pointers (posting list), saving massive RAM and disk space.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,Indexing,BTreeDeduplication,Optimization"
        ),
        q(
            id = "sql_adv_041",
            categoryId = "sql",
            title = "PostgreSQL: Free Space Map (FSM) and Visibility Map (VM)",
            prompt = "What are the distinct roles of the Free Space Map (`_fsm`) and Visibility Map (`_vm`) fork files in PostgreSQL tables?",
            opt0 = "FSM stores passwords; VM stores virtual tables without scanning unindexed pages during complex join queries",
            opt1 = "FSM tracks available free byte space per data page (to quickly find pages with room for new inserts)",
            opt2 = "VM stores video files; FSM stores audio files without scanning unindexed pages during complex join queries",
            opt3 = "They are temporary files deleted on query exit by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "FSM enables fast \$O(1)\$ space allocation for new rows. VM flags pages where all tuples are committed and frozen, enabling Index-Only Scans to skip visiting heap pages and speeding up VACUUM.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,FSM,VisibilityMap,IndexOnlyScan,Internals"
        ),
        q(
            id = "sql_adv_042",
            categoryId = "sql",
            title = "MySQL InnoDB Adaptive Hash Index (AHI)",
            prompt = "What is MySQL InnoDB's 'Adaptive Hash Index' (AHI) and how does it dynamically optimize B-Tree point lookups?",
            opt0 = "A manual hash index created by developers using SQL syntax",
            opt1 = "An internal in-memory feature: InnoDB monitors index page access patterns",
            opt2 = "An index that adapts to screen resolutions to prevent lock contention and serialization failures on tables",
            opt3 = "An index that automatically deletes slow queries",
            correctIndex = 1,
            explanation = "AHI is an autonomous in-memory cache. If InnoDB detects patterns like `WHERE id = ?`, it builds hash table pointers directly to buffer pool pages, transforming 3-level B-Tree traversals into instant O(1) memory lookups.",
            difficulty = "Advanced",
            tags = "SQL,MySQL,InnoDB,AdaptiveHashIndex,Optimization"
        ),
        q(
            id = "sql_adv_043",
            categoryId = "sql",
            title = "MySQL InnoDB Change Buffer (Insert Buffer)",
            prompt = "What is the role of the InnoDB Change Buffer (formerly Insert Buffer)?",
            opt0 = "It buffers changes to user passwords without scanning unindexed pages during complex join queries",
            opt1 = "It caches modifications to non-unique secondary index pages when those pages are NOT currently in the buffer pool, avoiding expensive random disk reads",
            opt2 = "It converts UPDATE statements into INSERT statements by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "It stores deleted rows for 30 days to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Secondary index updates are scattered and require random disk reads. The Change Buffer caches these writes in RAM, merging them lazily whenever the target index page is eventually read into memory.",
            difficulty = "Advanced",
            tags = "SQL,MySQL,InnoDB,ChangeBuffer,DiskIOOptimization"
        ),
        q(
            id = "sql_adv_044",
            categoryId = "sql",
            title = "Distributed SQL: Raft Joint Consensus for Reconfiguration",
            prompt = "In distributed consensus (Raft), how does 'Joint Consensus' safely transition a replica group from Configuration \$C_{old}\$ (3 nodes) to Configuration \$C_{new}\$ (5 nodes)?",
            opt0 = "By taking down all nodes and rebooting the cluster by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "It introduces a transitional configuration \$C_{old,new}\$ where log entries must achieve a majority quorum in BOTH \$C_{old}\$ AND \$C_{new}\$ independently before committing",
            opt2 = "By electing a leader on the command line without scanning unindexed pages during complex join queries",
            opt3 = "By disabling replication during node addition without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "Naive reconfiguration risks split-brain if two disjoint majorities form during transition. Joint consensus requires majorities from both old and new configurations simultaneously, guaranteeing absolute safety.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,Raft,JointConsensus,ClusterMembership"
        ),
        q(
            id = "sql_adv_045",
            categoryId = "sql",
            title = "PostgreSQL: JIT (Just-In-Time) Compilation Overhead",
            prompt = "Why can PostgreSQL's JIT compilation (via LLVM) sometimes cause short OLTP queries to run 10x SLOWER than with JIT disabled?",
            opt0 = "JIT compilation is written in Python to prevent lock contention and serialization failures on tables",
            opt1 = "The CPU compilation time required to compile expression trees and tuple deforming into native machine code (often 5-15 milliseconds) far exceeds the execution time of a lightweight query that executes in 0.5 milliseconds",
            opt2 = "JIT forces all queries to run sequentially on 1 thread to prevent lock contention and serialization failures on tables",
            opt3 = "JIT causes memory corruption on Linux to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "JIT compiles SQL expressions to machine code. For complex 10-minute analytics, saving 20% runtime justifies a 10ms compile. For 1ms OLTP point lookups, the 10ms JIT compile phase ruins p99 latency.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,JIT,LLVM,PerformanceTuning,OLTP"
        ),
        q(
            id = "sql_adv_046",
            categoryId = "sql",
            title = "Database Indexing: Partial Index for Soft-Deleted Unique Constraints",
            prompt = "What is the exact DDL to enforce that `email` must be globally unique among ACTIVE users while allowing infinite duplicate emails among soft-deleted users in PostgreSQL?",
            opt0 = "ALTER TABLE users ADD CONSTRAINT uq_email UNIQUE (email);",
            opt1 = "CREATE UNIQUE INDEX uq_active_user_email ON users(email) WHERE deleted_at IS NULL",
            opt2 = "CREATE INDEX uq_email ON users(email, deleted_at); without scanning unindexed pages during complex join queries",
            opt3 = "Unique constraints cannot be used with soft deletes by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "A partial unique index (`WHERE deleted_at IS NULL`) indexes only active users. It enforces strict uniqueness on active accounts while completely ignoring soft-deleted rows with non-null timestamps.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,PartialIndex,SoftDelete,UniqueConstraint"
        ),
        q(
            id = "sql_adv_047",
            categoryId = "sql",
            title = "Database Query Tuning: Work_mem and Sort Spilling to Disk",
            prompt = "In PostgreSQL, what happens when an `ORDER BY` or `Hash Join` query exceeds the allocated `work_mem` buffer size?",
            opt0 = "The query fails immediately with an OutOfMemory error to prevent lock contention and serialization failures on tables",
            opt1 = "It spills to disk (External Merge Sort / Hash Batches): temporary files are written to disk storage, causing a dramatic performance drop from microsecond RAM sorting to slow millisecond disk I/O",
            opt2 = "The database automatically allocates all server RAM to the query to enforce referential constraints and table consistency",
            opt3 = "The query cancels sorting and returns unsorted rows to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "If sort data exceeds `work_mem`, Postgres switches from quicksort in RAM to external disk merge sort, generating disk I/O. Tuning `work_mem` appropriately keeps sorts in memory.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,work_mem,DiskSpilling,QueryTuning"
        ),
        q(
            id = "sql_adv_048",
            categoryId = "sql",
            title = "Distributed SQL: Distributed Transaction Latency (Pipelined Raft)",
            prompt = "How does 'Pipelined Raft' in modern distributed databases hide cross-datacenter WAN round-trip latency during distributed mutations?",
            opt0 = "By disabling network encryption to prevent lock contention and serialization failures on tables",
            opt1 = "A Raft leader does not wait for a proposal's consensus response before proposing the NEXT command",
            opt2 = "By routing network packets through fiber optic lasers to prevent lock contention and serialization failures on tables",
            opt3 = "By skipping consensus on 50% of writes to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Without pipelining, transactions stall waiting for cross-region ACKs. Pipelining streams multiple proposals in flight simultaneously over TCP connections, achieving high throughput despite 50ms WAN latency.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,Raft,Pipelining,WANLatency"
        ),
        q(
            id = "sql_adv_049",
            categoryId = "sql",
            title = "PostgreSQL: Table Partitioning with Declarative Partitioning",
            prompt = "How does PostgreSQL 11+ Declarative Partitioning route an incoming `INSERT INTO measurement (...) VALUES (...)` to the correct sub-partition table?",
            opt0 = "By executing an external Python script without scanning unindexed pages during complex join queries",
            opt1 = "The query planner uses internal tuple routing: the partition key value is evaluated against partition bounds, routing the tuple directly to the destination leaf partition without executing triggers or manual rules",
            opt2 = "By broadcasting the insert to all partitions without scanning unindexed pages during complex join queries",
            opt3 = "By saving the row in a temporary table first to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Declarative partitioning natively routes tuples at the engine level. It evaluates partition bounds and writes directly to the correct partition table, delivering near-native insert speeds.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,Partitioning,DeclarativePartitioning,TupleRouting"
        ),
        q(
            id = "sql_adv_050",
            categoryId = "sql",
            title = "SQL Antipattern: Using Triggers for Complex Business Invariants",
            prompt = "Why do enterprise database architects strongly advise against encoding complex multi-table business logic inside database Triggers?",
            opt0 = "Triggers cannot be compiled by database engines to enforce referential constraints and table consistency",
            opt1 = "Triggers execute implicitly with invisible side-effects, making workflows impossible to trace/debug, interfering with bulk data migrations, complicating automated testing, and introducing catastrophic cascading deadlock storms",
            opt2 = "Triggers consume 100% of CPU memory without scanning unindexed pages during complex join queries",
            opt3 = "Triggers are deprecated in SQL 2023 to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Triggers run implicitly and invisibly. A single UPDATE can trigger 5 secondary updates and 3 audit tables, making performance bottlenecks and lock ordering deadlocks almost impossible to diagnose.",
            difficulty = "Advanced",
            tags = "SQL,Architecture,Triggers,AntiPatterns,CleanDesign"
        )
    )

    private fun part3(): List<QuestionEntity> = listOf(
        q(
            id = "sql_adv_051",
            categoryId = "sql",
            title = "Distributed SQL: Read-Your-Own-Writes Consistency over Raft",
            prompt = "In a distributed SQL cluster where reads can be served by local follower replicas, how is 'Read-Your-Own-Writes' (Causal Consistency) enforced for a client?",
            opt0 = "By clearing the client's web browser cache by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "The client tracks the Lamport/HLC timestamp or Raft Index of its most recent write",
            opt2 = "Follower replicas are forbidden from serving reads without scanning unindexed pages during complex join queries",
            opt3 = "By executing all reads on the primary node only by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "Follower reads may lag. By passing the client's latest write timestamp watermark, the follower verifies its applied log index exceeds the watermark before answering, guaranteeing the client sees its own updates.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,CausalConsistency,FollowerReads,Watermarks"
        ),
        q(
            id = "sql_adv_052",
            categoryId = "sql",
            title = "Database Indexing: Covering Index with Deduplication Math",
            prompt = "On a 100-Million row table where `status` has only 4 distinct values, what is the storage benefit of a B-Tree index on `(status, user_id)` with PostgreSQL 13+ deduplication?",
            opt0 = "It consumes zero bytes of disk space",
            opt1 = "The 4 status keys are stored only 4 times per leaf page",
            opt2 = "Deduplication only works on primary keys",
            opt3 = "Deduplication slows down SELECT queries by 10x",
            correctIndex = 1,
            explanation = "Posting lists pack duplicate keys into arrays of TIDs. For low-cardinality leading columns, index size collapses dramatically, allowing the entire index to fit in memory buffer cache.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,Indexing,BTreeDeduplication,StorageOptimization"
        ),
        q(
            id = "sql_adv_053",
            categoryId = "sql",
            title = "Database Performance: Buffer Pool Hit Ratio Formula",
            prompt = "What is the standard formula for calculating the Database Buffer Pool (Cache) Hit Ratio, and what target percentage indicates healthy memory sizing in production OLTP?",
            opt0 = "Hit Ratio = (Total Writes / Total Reads) * 100; Target > 50%",
            opt1 = "Hit Ratio = (Buffer Pool Reads from RAM) / (Total Logical Reads) * 100",
            opt2 = "Hit Ratio = (Total Queries / Active Connections) * 100; Target > 80%",
            opt3 = "Hit Ratio = (Free RAM / Total RAM) * 100; Target > 20%",
            correctIndex = 1,
            explanation = "Buffer cache hit ratio measures how often a requested page was already in memory. In OLTP, a healthy ratio is >99%. A ratio dropping below 95% indicates buffer pool exhaustion and severe disk I/O thrashing.",
            difficulty = "Advanced",
            tags = "SQL,Performance,BufferPool,CacheHitRatio,DBA"
        ),
        q(
            id = "sql_adv_054",
            categoryId = "sql",
            title = "Database Storage: Copy-On-Write B-Trees (LMDB / CouchDB)",
            prompt = "How does a Copy-On-Write (COW) B-Tree engine (like LMDB or CouchDB) achieve lock-free concurrent reads without an Undo Log or WAL?",
            opt0 = "By storing all data in text files by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "Mutations never overwrite pages in-place: modifying a leaf page creates a new cloned page and clones parent pages up to a new root pointer",
            opt2 = "By running on a single CPU thread to enforce referential constraints and table consistency",
            opt3 = "By disabling database writes to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "COW B-Trees provide instantaneous snapshots: modifying a page creates a new path to a new root. Readers hold a pointer to an older root and read immutable pages with zero locking or WAL overhead.",
            difficulty = "Advanced",
            tags = "SQL,DataStructures,COWBTree,LMDB,StorageEngines"
        ),
        q(
            id = "sql_adv_055",
            categoryId = "sql",
            title = "PostgreSQL: Zero-Downtime Foreign Key Creation",
            prompt = "How do you add a Foreign Key constraint to a 100-Million row table in PostgreSQL without locking out concurrent writers during the multi-minute validation scan?",
            opt0 = "Run standard `ALTER TABLE ... ADD CONSTRAINT FOREIGN KEY` by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "Two-step addition: 1. `ALTER TABLE child ADD CONSTRAINT fk_name FOREIGN KEY (...) REFERENCES parent(...) NOT VALID",
            opt2 = "Take the database offline for maintenance without scanning unindexed pages during complex join queries",
            opt3 = "Foreign keys can only be created when a table is empty to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "`NOT VALID` adds the constraint in 1 millisecond (enforcing it only on new writes). Then `VALIDATE CONSTRAINT` verifies historical data in the background with only a weak ShareUpdateExclusiveLock, permitting concurrent writes.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,ZeroDowntime,ForeignKey,NOTVALID"
        ),
        q(
            id = "sql_adv_056",
            categoryId = "sql",
            title = "Database Indexing: Indexing Geospatial Coordinates (PostGIS R-Tree vs Geohash)",
            prompt = "What is the architectural difference between indexing coordinates using PostGIS GiST (R-Tree) versus a B-Tree index on a Geohash string?",
            opt0 = "Geohash can only store points in the United States to prevent lock contention and serialization failures on tables",
            opt1 = "PostGIS GiST indexes 2D bounding boxes hierarchically, supporting true Euclidean distance (`<->`) and polygon intersections",
            opt2 = "R-Tree indexes run on mobile phones only without scanning unindexed pages during complex join queries",
            opt3 = "There is no difference in search performance to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Geohash flattens 2D space into a 1D Z-order curve searchable via standard B-Tree prefixes (suffering from boundary edge artifacts). PostGIS GiST natively indexes 2D spatial geometries using R-Trees.",
            difficulty = "Advanced",
            tags = "SQL,Spatial,Geohash,PostGIS,GiST,RTree"
        ),
        q(
            id = "sql_adv_057",
            categoryId = "sql",
            title = "Database Concurrency: Serializable Snapshot Isolation (SSI) Internals",
            prompt = "How does PostgreSQL's Serializable Snapshot Isolation (SSI) detect and abort serialization anomalies (like Write Skew) without acquiring shared locks on reads?",
            opt0 = "By pausing all transactions for 10 milliseconds by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "SIREAD lock tracking: it maintains an in-memory lock graph tracking rw-antidependencies (when a transaction writes a version that another concurrent transaction read)",
            opt2 = "By running transactions sequentially on a single thread without scanning unindexed pages during complex join queries",
            opt3 = "By checking CPU cache line invalidations without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "Cahill's SSI algorithm tracks 'SIREAD' flags in memory. A serialization anomaly mathematically requires two consecutive rw-antidependency edges in the dependency graph. When detected, the engine terminates one transaction.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,SSI,SerializableSnapshotIsolation,Concurrency"
        ),
        q(
            id = "sql_adv_058",
            categoryId = "sql",
            title = "Distributed SQL: Distributed Partition Pruning in Hash Sharding",
            prompt = "In a distributed SQL cluster with 100 database shards, how does the query router avoid querying all 100 shards for `SELECT * FROM orders WHERE customer_id = 42;`?",
            opt0 = "It broadcasts the query to all 100 shards simultaneously (scatter-gather) by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "Targeted Routing: the query coordinator applies the cluster's hash function to `customer_id = 42` (\$hash(42) \\% 100 = 7\$), dispatching the network query directly and exclusively to Shard #7",
            opt2 = "It stores a copy of all data on the client's laptop by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "It queries Shard #1, then Shard #2 sequentially until found without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "Deterministic hashing allows the query router to calculate the exact shard node holding the requested data in O(1) time, turning a cluster-wide scatter-gather into a targeted single-shard RPC.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,Sharding,QueryRouting,HashPartitioning"
        ),
        q(
            id = "sql_adv_059",
            categoryId = "sql",
            title = "Database Indexing: Index Skip Scan (Loose Index Scan)",
            prompt = "What is an 'Index Skip Scan' (Loose Index Scan) and how does it allow a composite index on `(gender, age)` to satisfy `WHERE age = 30` without a leading `gender` predicate?",
            opt0 = "It skips reading the index entirely and performs a table scan",
            opt1 = "The optimizer recognizes that `gender` has few distinct values ('M', 'F')",
            opt2 = "It skips rows that contain null values to prevent lock contention and serialization failures on tables",
            opt3 = "It is only available in SQLite to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "When the leading column of a composite index has very low cardinality (e.g. 2 values), an Index Skip Scan executes discrete binary searches for each distinct prefix value, bypassing the leftmost prefix rule.",
            difficulty = "Advanced",
            tags = "SQL,Indexing,IndexSkipScan,LooseIndexScan,Optimization"
        ),
        q(
            id = "sql_adv_060",
            categoryId = "sql",
            title = "Database Performance: Connection Pooling Queue Latency Spikes",
            prompt = "What metric in connection pool monitoring (HikariCP) is the most critical leading indicator of an impending application-wide database outage?",
            opt0 = "Total queries executed per second by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "`Connection Acquisition Time` (Pool Wait Time): when threads begin queuing and waiting tens of milliseconds to acquire a connection from the pool, queue latency compounds exponentially into total connection timeout failures",
            opt2 = "Database disk storage percentage to enforce referential constraints and table consistency",
            opt3 = "The number of dropped tables to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Pool acquisition time reflects contention. Once threads start waiting for idle connections, thread queues explode exponentially, causing cascading HTTP 504 gateway timeouts across the microservice mesh.",
            difficulty = "Advanced",
            tags = "SQL,HikariCP,ConnectionPool,Monitoring,OutagePrevention"
        ),
        q(
            id = "sql_adv_061",
            categoryId = "sql",
            title = "PostgreSQL: Table Bloat and Autovacuum Starvation",
            prompt = "What prevents PostgreSQL's `autovacuum` from reclaiming dead tuples on a high-traffic table, leading to runaway disk bloat?",
            opt0 = "The table having too many columns to enforce referential constraints and table consistency",
            opt1 = "A long-running query, uncommitted open transaction, or orphaned prepared transaction elsewhere in the database holds back the global `xmin` horizon",
            opt2 = "Autovacuum only runs on weekends by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "Tables with indexes cannot be vacuumed to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Autovacuum can only delete dead tuples whose xmax is older than the oldest running transaction in the ENTIRE database. A single abandoned transaction running for 24 hours blocks vacuuming across all tables.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,Autovacuum,TableBloat,xminHorizon"
        ),
        q(
            id = "sql_adv_062",
            categoryId = "sql",
            title = "Database Storage: Log-Structured Merge (LSM) Compaction Debt",
            prompt = "What is 'Compaction Debt' in an LSM storage engine (RocksDB/Cassandra) and why does it cause severe write stalls?",
            opt0 = "Money owed to cloud storage providers to prevent lock contention and serialization failures on tables",
            opt1 = "When the rate of incoming user write traffic exceeds the background compaction thread's disk I/O throughput",
            opt2 = "When disk sectors are corrupted by power loss by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "Compaction debt can only occur on SSD drives to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "If incoming writes outpace compaction, L0 files multiply. Because L0 files have overlapping keys, read queries must check every L0 file. RocksDB injects artificial write stalls to let compaction catch up.",
            difficulty = "Advanced",
            tags = "SQL,StorageEngines,LSMTree,CompactionDebt,WriteStalls,RocksDB"
        ),
        q(
            id = "sql_adv_063",
            categoryId = "sql",
            title = "Database Indexing: Index Bloat from Frequent In-Place Updates",
            prompt = "Why does updating a single column `counter = counter + 1` 1,000 times per second on a single row in PostgreSQL cause massive table bloat if HOT optimization is disabled?",
            opt0 = "Because counters cannot be incremented in SQL by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "PostgreSQL writes a completely new tuple version for every single update (generating 1,000 new rows per second on disk) and updates all secondary indexes on the table, generating gigabytes of dead tuples within hours",
            opt2 = "Because PostgreSQL does not support integers without scanning unindexed pages during complex join queries",
            opt3 = "Because the table is automatically converted to a view to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Unlike MySQL (which updates in-place and writes to an Undo Log), Postgres writes a new tuple version for every update. 1,000 updates/sec creates 1,000 dead tuples/sec. Redis is vastly better for high-frequency counters.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,TableBloat,CounterAntiPattern,MVCC"
        ),
        q(
            id = "sql_adv_064",
            categoryId = "sql",
            title = "Database Partitioning: Global vs Local Indexes",
            prompt = "What is the difference between a Local Index and a Global Index on a partitioned table?",
            opt0 = "Local is stored on the client's phone; Global is on the server without scanning unindexed pages during complex join queries",
            opt1 = "A Local Index is partitioned identically to the underlying table (each partition has its own isolated independent B-Tree index)",
            opt2 = "Global indexes can only index strings to prevent lock contention and serialization failures on tables",
            opt3 = "Local indexes cannot be used in WHERE clauses to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Local indexes are isolated per partition: dropping a partition drops its local index instantly. Global indexes enforce global unique constraints across all partitions, but make partition drops expensive.",
            difficulty = "Advanced",
            tags = "SQL,Partitioning,LocalIndex,GlobalIndex,DataArchitecture"
        ),
        q(
            id = "sql_adv_065",
            categoryId = "sql",
            title = "Database Indexing: Composite Index Prefix Column Matching with Ranges",
            prompt = "Given an index on `(tenant_id, created_at, status)`, why does `WHERE tenant_id = 5 AND created_at > '2025-01-01' AND status = 'COMPLETED'` fail to use the index for the `status` column filter?",
            opt0 = "tenant_id must be a string to enforce referential constraints and table consistency",
            opt1 = "In a B-Tree composite index, binary search traversal stops after the FIRST range comparison column (`created_at`)",
            opt2 = "The status column is invalid to enforce referential constraints and table consistency",
            opt3 = "The query planner ignores composite indexes with more than 2 columns without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "B-Tree ordering: once a range filter ($>$ or $<$) is applied, subsequent columns in the index are no longer in sorted order within the matched range. Reordering the index as `(tenant_id, status, created_at)` fixes this.",
            difficulty = "Advanced",
            tags = "SQL,Indexing,CompositeIndex,BTreeTraversal,Rule"
        ),
        q(
            id = "sql_adv_066",
            categoryId = "sql",
            title = "Database Concurrency: Explicit Table Locks in PostgreSQL",
            prompt = "What operation acquires an `ACCESS EXCLUSIVE` lock in PostgreSQL (the strongest lock mode, blocking all reads and writes)?",
            opt0 = "`SELECT * FROM table;` without scanning unindexed pages during complex join queries",
            opt1 = "`ALTER TABLE ... ADD COLUMN`, `DROP TABLE`, `TRUNCATE`, and standard `REINDEX`",
            opt2 = "`INSERT INTO table VALUES (...);` to prevent lock contention and serialization failures on tables",
            opt3 = "`SELECT ... FOR UPDATE;` to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "ACCESS EXCLUSIVE conflicts with all lock modes (even simple SELECT reads). Running standard DDL or TRUNCATE acquires ACCESS EXCLUSIVE, freezing all concurrent queries until finished.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,AccessExclusiveLock,Locking,DDL"
        ),
        q(
            id = "sql_adv_067",
            categoryId = "sql",
            title = "Database Query Tuning: Parallel Query Execution",
            prompt = "How does PostgreSQL parallel query execution coordinate multiple worker processes to accelerate large aggregate scans (`SELECT COUNT(*) FROM large_table`)?",
            opt0 = "By connecting to multiple internet databases",
            opt1 = "The Leader process spawns background worker processes",
            opt2 = "By converting the table to an in-memory array",
            opt3 = "By running queries on separate GPU cores",
            correctIndex = 1,
            explanation = "PostgreSQL parallel query spawns background workers using shared memory queues. Workers scan page ranges in parallel, computing partial aggregates that are merged by a Parallel Gather node.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,ParallelQuery,Parallelism,Performance"
        ),
        q(
            id = "sql_adv_068",
            categoryId = "sql",
            title = "Database Performance: Query Plan Cache Invalidation",
            prompt = "What causes the database engine to invalidate and recompile a cached Prepared Statement execution plan?",
            opt0 = "Every time a user logs out of the application to prevent lock contention and serialization failures on tables",
            opt1 = "DDL schema changes on referenced tables, index additions/deletions, running `ANALYZE`, or significant changes in table statistics that alter the cost threshold between sequential scans and index scans",
            opt2 = "Cached plans never expire under any circumstances to prevent lock contention and serialization failures on tables",
            opt3 = "Recompilation happens every 60 seconds automatically without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "Cached execution plans are invalidated when the underlying schema or statistics change. This ensures the optimizer does not stick with a stale plan after a new index is created or table statistics evolve.",
            difficulty = "Advanced",
            tags = "SQL,QueryOptimizer,PlanCache,Recompilation,PreparedStatements"
        ),
        q(
            id = "sql_adv_069",
            categoryId = "sql",
            title = "Database Storage: Write Amplification in B-Tree vs LSM-Tree",
            prompt = "Why do write-heavy logging and messaging workloads achieve 5x-10x higher ingestion throughput on an LSM-Tree (RocksDB/Cassandra) than on a traditional B-Tree (PostgreSQL/MySQL)?",
            opt0 = "B-Trees are written in Java; LSM-Trees are written in C++ to prevent lock contention and serialization failures on tables",
            opt1 = "B-Trees write random 8KB-16KB pages to disk to update individual rows (high random disk I/O and page splits)",
            opt2 = "LSM-Trees do not persist data to disk without scanning unindexed pages during complex join queries",
            opt3 = "B-Trees can only write 1 row per second without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "Random disk I/O bottlenecks B-Trees. LSM-Trees convert all random incoming writes into sequential in-memory writes, flushing contiguous sequential blocks to disk at raw disk bandwidth speeds.",
            difficulty = "Advanced",
            tags = "SQL,StorageEngines,LSMTree,BTree,WriteAmplification,Throughput"
        ),
        q(
            id = "sql_adv_070",
            categoryId = "sql",
            title = "Database Security: SQL Injection Defense in Dynamic Multi-Filter Queries",
            prompt = "In an enterprise reporting dashboard with 15 optional user search filters, what is the safest way to construct the dynamic SQL query without risking SQL injection?",
            opt0 = "Concatenating user inputs directly if they do not contain the word 'DROP' by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "Using a Query Builder that dynamically appends predicates while strictly parameterizing all values using positional bind placeholders (`?` or `$1`), keeping query syntax strictly separate from data",
            opt2 = "Encrypting the entire query string with base64 to prevent lock contention and serialization failures on tables",
            opt3 = "Writing 32,768 separate hardcoded SQL queries for every possible permutation to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Dynamic query building must never concatenate literals. Type-safe query builders (like jOOQ) assemble relational algebra ASTs, parameterizing all values into bind variables automatically.",
            difficulty = "Advanced",
            tags = "SQL,Security,SQLInjection,jOOQ,QueryBuilder,BestPractices"
        ),
        q(
            id = "sql_adv_071",
            categoryId = "sql",
            title = "PostgreSQL: Zero-Downtime NOT NULL Constraint Addition",
            prompt = "Why does running `ALTER TABLE users ALTER COLUMN email SET NOT NULL;` on a 500-Million row table lock out production traffic, and how is it added without downtime?",
            opt0 = "SET NOT NULL is forbidden on tables with primary keys",
            opt1 = "It takes an exclusive lock and scans all 500M rows to verify zero NULLs exist",
            opt2 = "By taking the database offline by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "By converting email to an integer column by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "Direct `SET NOT NULL` scans the whole table under an AccessExclusiveLock. Adding a `CHECK ... NOT VALID` and validating it in the background allows the subsequent `SET NOT NULL` to succeed in 0ms without re-scanning.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,ZeroDowntime,NOTNULL,Constraints"
        ),
        q(
            id = "sql_adv_072",
            categoryId = "sql",
            title = "Database Storage: PostgreSQL TOAST Strategies (PLAIN, EXTERNAL, EXTENDED, MAIN)",
            prompt = "In PostgreSQL column definitions, what does the `EXTENDED` TOAST storage strategy do for large JSONB or text columns?",
            opt0 = "It extends the column length to 100 characters",
            opt1 = "It attempts in-line LZ4/pglz data compression first",
            opt2 = "It stores data in an AWS S3 bucket",
            opt3 = "It makes the column read-only to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "EXTENDED is default for text/jsonb: it tries compression first. If still too big, it pushes the compressed data out-of-line to TOAST storage, keeping primary heap pages compact.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,TOAST,StorageEngines,Compression"
        ),
        q(
            id = "sql_adv_073",
            categoryId = "sql",
            title = "Database Partitioning: Foreign Key Constraints Across Partitioned Tables",
            prompt = "What historic limitation existed regarding Foreign Keys referencing partitioned tables in relational databases?",
            opt0 = "Partitioned tables cannot store numbers to enforce referential constraints and table consistency",
            opt1 = "A foreign key referencing a partitioned table must include the partition key in its composite foreign key, because each partition maintains an isolated local index and cannot enforce foreign keys across other partitions",
            opt2 = "Foreign keys can only reference views to prevent lock contention and serialization failures on tables",
            opt3 = "Partitioned tables cannot have primary keys to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Because partitions maintain local indexes, verifying referential integrity across partitions is difficult. Modern engines require the partition key to be part of the referenced primary/unique key.",
            difficulty = "Advanced",
            tags = "SQL,Partitioning,ForeignKey,ReferentialIntegrity,Constraints"
        ),
        q(
            id = "sql_adv_074",
            categoryId = "sql",
            title = "Database Performance: Stored Procedure Recompilation Storms",
            prompt = "What causes a 'Recompilation Storm' when hundreds of concurrent application threads execute the same Stored Procedure in SQL Server / Oracle?",
            opt0 = "When the database server is struck by lightning by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "When a stored procedure creates temporary tables or performs DDL mid-execution: concurrent threads invalidate each other's cached query plans, forcing all threads to block on CPU-heavy recompilation loops simultaneously",
            opt2 = "When passwords expire during query execution to enforce referential constraints and table consistency",
            opt3 = "When tables have zero rows without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "DDL or creating temporary tables inside stored procedures invalidates the procedure plan. Under high concurrency, hundreds of threads recompile the procedure simultaneously, pegging CPU at 100%.",
            difficulty = "Advanced",
            tags = "SQL,StoredProcedures,RecompilationStorm,SQLServer,Performance"
        ),
        q(
            id = "sql_adv_075",
            categoryId = "sql",
            title = "Database Indexing: Index-Organized Table (IOT) in Oracle",
            prompt = "What is an Index-Organized Table (IOT) in Oracle (identical to MySQL InnoDB's Clustered Index model)?",
            opt0 = "A table that is organized in alphabetical order by column name to enforce referential constraints and table consistency",
            opt1 = "A table whose physical row data is stored entirely inside the leaf blocks of its Primary Key B-Tree index",
            opt2 = "A table stored in an Excel spreadsheet to enforce referential constraints and table consistency",
            opt3 = "A table with no primary key to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "In an IOT (Oracle) or Clustered Table (InnoDB/SQL Server), the index IS the table. Primary key lookups find all row attributes in the leaf node without a secondary heap lookup.",
            difficulty = "Advanced",
            tags = "SQL,Oracle,IndexOrganizedTable,IOT,ClusteredIndex"
        )
    )

    private fun part4(): List<QuestionEntity> = listOf(
        q(
            id = "sql_adv_076",
            categoryId = "sql",
            title = "Distributed SQL: Range Merging and Rebalancing",
            prompt = "In a distributed SQL database (CockroachDB), what happens when millions of rows are deleted from a 64MB Range, leaving it with only 100KB of data?",
            opt0 = "The empty space remains reserved forever by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "Automatic Range Merge: the cluster coordinator detects that adjacent ranges are underutilized and merges the small range with its neighboring sibling range via Raft, reducing metadata overhead",
            opt2 = "The database cluster reboots without scanning unindexed pages during complex join queries",
            opt3 = "The range is permanently corrupted to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "When deletions leave ranges sparsely populated, the cluster executes Raft range merges. This reclaims metadata storage and keeps range counts proportional to actual data volume.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,RangeMerge,CockroachDB,Scalability"
        ),
        q(
            id = "sql_adv_077",
            categoryId = "sql",
            title = "PostgreSQL: pg_stat_statements for Slow Query Analysis",
            prompt = "What makes the `pg_stat_statements` extension the most powerful query performance diagnostic tool in PostgreSQL?",
            opt0 = "It formats queries in HTML to enforce referential constraints and table consistency",
            opt1 = "It normalizes SQL queries by stripping literal constants and aggregates real runtime execution metrics across all queries (total execution time, calls, min/max time, shared buffer reads and dirtied blocks)",
            opt2 = "It automatically deletes slow queries to prevent lock contention and serialization failures on tables",
            opt3 = "It runs queries without connecting to the database to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "pg_stat_statements normalizes queries (`WHERE id = 5` and `WHERE id = 10` become `WHERE id = ?`), aggregating execution statistics and revealing top resource-consuming queries across the cluster.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,pg_stat_statements,Performance,Monitoring"
        ),
        q(
            id = "sql_adv_078",
            categoryId = "sql",
            title = "Database Storage: PostgreSQL Write Amplification from Index Updates",
            prompt = "Why does updating a single column `status` on a table with 10 secondary indexes produce massive write amplification in PostgreSQL if HOT is not triggered?",
            opt0 = "Because PostgreSQL only allows 1 index per table without scanning unindexed pages during complex join queries",
            opt1 = "PostgreSQL must write a new tuple version to the heap data page, AND insert a new index pointer entry into EVERY SINGLE ONE of the 10 secondary indexes (10 index writes for 1 row update)",
            opt2 = "Because secondary indexes are stored on floppy disks by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "Because PostgreSQL converts indexes to strings by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "Without HOT, a new tuple gets a new physical `ctid` (page offset). Every secondary index on the table must be updated with the new pointer, multiplying write I/O by 10.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,WriteAmplification,HOT,Indexing"
        ),
        q(
            id = "sql_adv_079",
            categoryId = "sql",
            title = "Distributed SQL: Distributed Query Execution Plans (Vectorized Engines)",
            prompt = "How do modern distributed analytical query engines (Presto/Trino, CockroachDB Vectorized Engine) execute aggregations on billions of rows efficiently?",
            opt0 = "By processing rows one at a time using Java iterators by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "Vectorized Columnar Batch Processing: processing columns in fixed arrays/vectors of 1,024 values at a time, keeping data in L1/L2 CPU caches and leveraging CPU SIMD instructions to eliminate virtual method call overhead",
            opt2 = "By storing results in temporary tables on disk by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "By guessing the results using statistical models to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Traditional Volcano-style engines use row-by-row `next()` iteration (high CPU instruction overhead). Vectorized execution passes 1,024-element column vectors directly through CPU SIMD instructions.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,VectorizedExecution,SIMD,Trino,CockroachDB"
        ),
        q(
            id = "sql_adv_080",
            categoryId = "sql",
            title = "Database Indexing: Indexing Polymorphic Foreign Keys",
            prompt = "If a legacy schema uses Polymorphic Associations (`commentable_type, commentable_id`), what composite index must be created to optimize lookups?",
            opt0 = "CREATE INDEX ON comments(id); to prevent lock contention and serialization failures on tables",
            opt1 = "CREATE INDEX idx_comments_polymorphic ON comments(commentable_type, commentable_id)",
            opt2 = "CREATE INDEX ON comments(commentable_id); to enforce referential constraints and table consistency",
            opt3 = "Polymorphic associations cannot have indexes to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Queries search for both the type and the ID: `WHERE commentable_type = 'Post' AND commentable_id = 42`. A composite index on `(commentable_type, commentable_id)` satisfies this in O(log N).",
            difficulty = "Advanced",
            tags = "SQL,Indexing,PolymorphicAssociations,CompositeIndex"
        ),
        q(
            id = "sql_adv_081",
            categoryId = "sql",
            title = "PostgreSQL: Vacuum Loitering on Long-Running Queries",
            prompt = "How does configuring `idle_in_transaction_session_timeout` protect PostgreSQL from runaway table bloat?",
            opt0 = "It reboots the PostgreSQL service every 5 minutes without scanning unindexed pages during complex join queries",
            opt1 = "It automatically terminates client connections that remain open in an uncommitted transaction state longer than the threshold, freeing the `xmin` horizon and allowing autovacuum to reclaim dead tuples",
            opt2 = "It compresses idle transactions into ZIP files by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "It commits idle transactions automatically to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Idle transactions in open transaction state block autovacuum. Setting `idle_in_transaction_session_timeout = '60s'` kills stuck client sessions, preventing table bloat outages.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,Autovacuum,TableBloat,TimeoutConfiguration"
        ),
        q(
            id = "sql_adv_082",
            categoryId = "sql",
            title = "Database Indexing: Hash Index vs B-Tree Crash Safety",
            prompt = "Why was the use of Hash Indexes strongly discouraged in PostgreSQL prior to version 10?",
            opt0 = "Hash indexes could only store 100 rows to prevent lock contention and serialization failures on tables",
            opt1 = "Prior to PostgreSQL 10, Hash Indexes were NOT Write-Ahead Logged (WAL-logged)",
            opt2 = "Hash indexes consumed 100x more memory than B-Trees",
            opt3 = "Hash indexes could only be queried by root users to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Before Postgres 10, Hash indexes lacked WAL support and did not replicate to replicas. In Postgres 10+, Hash indexes are fully WAL-logged and crash-safe for equality queries.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,HashIndex,WAL,CrashSafety,History"
        ),
        q(
            id = "sql_adv_083",
            categoryId = "sql",
            title = "Database Replication: Asynchronous vs Semi-Synchronous Replication",
            prompt = "In MySQL / PostgreSQL replication, what guarantee does 'Semi-Synchronous Replication' provide over standard Asynchronous replication?",
            opt0 = "It guarantees zero latency on write queries to prevent lock contention and serialization failures on tables",
            opt1 = "A commit on the primary node waits until at least ONE replica has received the WAL/binlog events and written them to its relay log on disk, ensuring zero data loss if the primary crashes immediately",
            opt2 = "It requires all replicas to commit simultaneously by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "It eliminates the need for primary nodes without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "Async replication commits on the primary before replicas receive data (risking data loss on primary crash). Semi-sync waits until at least 1 replica confirms receiving the log, eliminating data loss.",
            difficulty = "Advanced",
            tags = "SQL,Replication,SemiSynchronous,HighAvailability,DisasterRecovery"
        ),
        q(
            id = "sql_adv_084",
            categoryId = "sql",
            title = "Database Storage: Direct I/O (O_DIRECT) and Double Buffering",
            prompt = "Why do enterprise database storage engines (MySQL InnoDB, Oracle) open database files with the `O_DIRECT` flag?",
            opt0 = "To download files directly from the cloud",
            opt1 = "To completely bypass the Operating System's page cache",
            opt2 = "To make database files read-only to enforce referential constraints and table consistency",
            opt3 = "To disable disk writes during peak traffic",
            correctIndex = 1,
            explanation = "Without `O_DIRECT`, data is cached twice: once in the OS page cache and once in the DB buffer pool. Bypassing the OS cache frees gigabytes of RAM for the database's own buffer pool.",
            difficulty = "Advanced",
            tags = "SQL,StorageEngines,ODirect,DoubleBuffering,OperatingSystems"
        ),
        q(
            id = "sql_adv_085",
            categoryId = "sql",
            title = "Database Indexing: Index Conditioning with Collation",
            prompt = "Why can an index on a `VARCHAR` column using a non-C locale (e.g. `en_US.UTF-8`) fail to support `LIKE 'prefix%'` queries in PostgreSQL without special operator classes?",
            opt0 = "English locale does not support the letter E without scanning unindexed pages during complex join queries",
            opt1 = "Non-C collations have complex linguistic sorting rules (where 'a' and 'A' may sort adjacent) that do not match pure byte-order prefix matching",
            opt2 = "PostgreSQL forbids indexes on UTF-8 strings to prevent lock contention and serialization failures on tables",
            opt3 = "Collation causes database memory leaks to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Complex locales alter character sorting. To use B-Trees for `LIKE 'foo%'` pattern matching under UTF-8 locales, Postgres requires creating the index with `varchar_pattern_ops`.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,Indexing,Collation,varchar_pattern_ops"
        ),
        q(
            id = "sql_adv_086",
            categoryId = "sql",
            title = "Database Query Tuning: JIT Deforming Cost",
            prompt = "What is 'Tuple Deforming' in PostgreSQL and why does it consume significant CPU during wide table scans?",
            opt0 = "Corrupted data rows that change shape to prevent lock contention and serialization failures on tables",
            opt1 = "Because PostgreSQL rows have variable-length attributes and null bitmaps, locating column 50 requires sequentially parsing and decoding the lengths and null flags of all preceding 49 columns for every row",
            opt2 = "Translating text columns into binary numbers without scanning unindexed pages during complex join queries",
            opt3 = "Converting tables into JSON format by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "Postgres tuples do not have fixed column offsets. To read column 50, the CPU must inspect all prior columns to calculate byte offsets. JIT compilation compiles specialized deforming code to accelerate this.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,TupleDeforming,Internals,Performance"
        ),
        q(
            id = "sql_adv_087",
            categoryId = "sql",
            title = "Database Transactions: Saga vs Two-Phase Commit in Polyglot Systems",
            prompt = "Why is the Saga Pattern preferred over Two-Phase Commit (2PC) when coordinating distributed transactions across microservices with heterogeneous databases (e.g. Postgres, Mongo, Stripe)?",
            opt0 = "2PC is only supported on Windows operating systems by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "2PC requires all participating databases to hold row locks across network roundtrips until final commit (blocking concurrent transactions and failing if any service goes down)",
            opt2 = "Sagas do not require database connections to prevent lock contention and serialization failures on tables",
            opt3 = "2PC is deprecated in all cloud environments without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "2PC is a blocking protocol: if 1 node stalls, locks remain held cluster-wide. Sagas commit locally and execute asynchronous compensating actions on failure, providing high availability.",
            difficulty = "Advanced",
            tags = "SQL,DistributedTransactions,SagaPattern,2PC,Microservices"
        ),
        q(
            id = "sql_adv_088",
            categoryId = "sql",
            title = "Database Indexing: Index Skip Scan Mechanics",
            prompt = "What is the computational complexity of an Index Skip Scan on a composite index \$(A, B)\$ when column \$A\$ has cardinality \$C_A\$?",
            opt0 = "\$O(N)\$ full table scan by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "\$O(C_A \\times \\log N)\$ where \$C_A\$ is the number of distinct values in prefix column \$A\$, executing \$C_A\$ discrete binary searches into the B-Tree",
            opt2 = "\$O(1)\$ constant time to prevent lock contention and serialization failures on tables",
            opt3 = "\$O(N^2)\$ quadratic time to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "An Index Skip Scan performs a B-Tree search for each distinct value of the prefix column. If column A has 2 distinct values (e.g. Male/Female), it executes 2 binary searches (\$2 \\times \\log N\$).",
            difficulty = "Advanced",
            tags = "SQL,Indexing,IndexSkipScan,Complexity,Algorithms"
        ),
        q(
            id = "sql_adv_089",
            categoryId = "sql",
            title = "Database Performance: Huge Pages (TLB Cache Optimization)",
            prompt = "Why does enabling Linux 'Huge Pages' (2MB or 1GB pages instead of default 4KB pages) improve database buffer pool throughput by 5-15% on large memory servers?",
            opt0 = "Huge pages store more characters per text column to prevent lock contention and serialization failures on tables",
            opt1 = "It reduces the size of the OS Page Table by 500x, minimizing CPU Translation Lookaside Buffer (TLB) cache misses and TLB shootdown interrupts when translating virtual addresses to physical RAM",
            opt2 = "Huge pages prevent hard drives from crashing without scanning unindexed pages during complex join queries",
            opt3 = "Huge pages compress data on disk automatically by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "A 64GB buffer pool with 4KB pages requires 16 Million page table entries, overwhelming the CPU TLB cache. Using 2MB Huge Pages reduces page table entries to 32,000, eliminating TLB misses.",
            difficulty = "Advanced",
            tags = "SQL,Linux,HugePages,TLB,HardwareOptimization,Performance"
        ),
        q(
            id = "sql_adv_090",
            categoryId = "sql",
            title = "Database Storage: WAL Compression and Full Page Writes",
            prompt = "In PostgreSQL, why does the first modification to a data page after a Checkpoint write a 'Full Page Image' (8KB) to the WAL log?",
            opt0 = "To backup the database to cloud storage to enforce referential constraints and table consistency",
            opt1 = "To protect against Torn Pages caused by OS power loss: if an operating system crash occurs while writing an 8KB page, the full page image in the WAL allows crash recovery to restore the uncorrupted page before replaying deltas",
            opt2 = "Because WAL logs cannot store small changes without scanning unindexed pages during complex join queries",
            opt3 = "To encrypt the database data to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Full page writes (`full_page_writes = on`) protect against torn pages. After a checkpoint, the first write to a page writes the entire 8KB page to WAL. Subsequent writes write only diffs.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,WAL,FullPageWrites,TornPages,CrashRecovery"
        ),
        q(
            id = "sql_adv_091",
            categoryId = "sql",
            title = "Distributed SQL: Range Split Race Conditions during 2PC",
            prompt = "In CockroachDB, what happens if a Multi-Raft Range splits in the middle of executing a distributed 2PC transaction?",
            opt0 = "The transaction is aborted and the database crashes",
            opt1 = "The Range Split operation updates range boundary routing metadata atomically",
            opt2 = "The data in the split range is deleted to prevent lock contention and serialization failures on tables",
            opt3 = "Transactions are paused globally across the cluster during splits",
            correctIndex = 1,
            explanation = "Range splits are atomic metadata operations. If a range splits mid-transaction, the coordinator handles the epoch error and re-routes transactional intents to the new sub-range seamlessly.",
            difficulty = "Advanced",
            tags = "SQL,DistributedSQL,RangeSplit,2PC,CockroachDB"
        ),
        q(
            id = "sql_adv_092",
            categoryId = "sql",
            title = "Database Performance: Buffer Pinning vs Buffer Locking",
            prompt = "In database storage buffer pools, what does it mean for a page to be 'Pinned' in RAM?",
            opt0 = "The page is saved as a desktop shortcut without scanning unindexed pages during complex join queries",
            opt1 = "The page's pin count (reference count) is incremented, guaranteeing that the buffer pool replacement algorithm (LRU/Clock) will NOT evict or write over this memory page while a worker thread is reading its memory",
            opt2 = "The page is locked against all read queries to prevent lock contention and serialization failures on tables",
            opt3 = "The page is written to an external SSD to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Pinning a buffer increments its use count, preventing cache eviction while a thread dereferences pointers inside that page. Releasing the pin allows the buffer pool to evict it if needed.",
            difficulty = "Advanced",
            tags = "SQL,StorageEngines,BufferPool,BufferPinning,Internals"
        ),
        q(
            id = "sql_adv_093",
            categoryId = "sql",
            title = "PostgreSQL: GIN vs GiST for Text Search Trade-Offs",
            prompt = "When comparing GIN and GiST indexes for PostgreSQL full-text search, what is the core performance trade-off?",
            opt0 = "GiST uses more memory; GIN uses no memory to prevent lock contention and serialization failures on tables",
            opt1 = "GIN is ~3x faster for read queries (inverted index lookups are direct) but has heavy write penalties and index bloat",
            opt2 = "GIN cannot index text documents to prevent lock contention and serialization failures on tables",
            opt3 = "GiST can only be used on numbers without scanning unindexed pages during complex join queries",
            correctIndex = 1,
            explanation = "GIN provides lightning-fast reads (direct posting lists) but slower writes. GiST uses lossy hash signatures: writes are fast and compact, but reads must verify matching rows against the heap.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,GIN,GiST,FullTextSearch,Tradeoffs"
        ),
        q(
            id = "sql_adv_094",
            categoryId = "sql",
            title = "Database Security: SQL Injection via Dynamic ORDER BY",
            prompt = "Why does parameterized query binding fail to prevent SQL injection in dynamic sorting clauses like `SELECT * FROM users ORDER BY \" + sortColumn + \";\"`?",
            opt0 = "Because SQL databases do not support ORDER BY with parameters",
            opt1 = "SQL parameters (bind variables) can ONLY replace literal data values",
            opt2 = "Because ORDER BY is an external operating system command",
            opt3 = "Because sorting numbers is insecure to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Bind parameters cannot be used for column or table names. If an application concatenates `sortColumn`, attackers can inject expressions (`CASE WHEN ... THEN pg_sleep(5) END`). Whitelisting allowed columns is mandatory.",
            difficulty = "Advanced",
            tags = "SQL,Security,SQLInjection,ORDERBY,Whitelisting"
        ),
        q(
            id = "sql_adv_095",
            categoryId = "sql",
            title = "Database Storage: Write-Ahead Log Recycle vs Archive",
            prompt = "In enterprise database operations, why must WAL segments be archived (`archive_command` in PostgreSQL) rather than simply recycled by checkpoints?",
            opt0 = "Archived WAL files take up zero disk space by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "Point-In-Time Recovery (PITR): continuous archiving of historical WAL segments enables restoring a baseline backup from 30 days ago and replaying logs to recover the database to the EXACT second before an accidental DROP TABLE occurred",
            opt2 = "Recycled WAL files delete the primary key index to enforce referential constraints and table consistency",
            opt3 = "Archiving is required by the Linux kernel to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "PITR pairs a base physical backup with continuous historical WAL archives. If a developer drops a table at 14:02:15, the DBA can restore the base backup and replay WAL up to 14:02:14.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,WAL,PITR,DisasterRecovery,Backups"
        ),
        q(
            id = "sql_adv_096",
            categoryId = "sql",
            title = "Database Indexing: Partial Index for Multi-Tenant Isolation",
            prompt = "How can a multi-tenant SaaS application hosting 10,000 small tenants and 1 massive enterprise tenant (comprising 80% of all data) optimize queries on the large tenant using partial indexes?",
            opt0 = "By moving the enterprise tenant to a spreadsheet without scanning unindexed pages during complex join queries",
            opt1 = "Create a partial index targeting ONLY the enterprise tenant: `CREATE INDEX idx_enterprise ON orders(status, created_at) WHERE tenant_id = 999",
            opt2 = "By disabling indexes for all other tenants by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "By deleting the enterprise tenant's history to prevent lock contention and serialization failures on tables",
            correctIndex = 1,
            explanation = "Partial indexing allows creating dedicated indexes for whale tenants. The index stays compact and tuned precisely to the enterprise tenant's queries without indexing millions of smaller tenant rows.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,MultiTenancy,PartialIndex,SaaSArchitecture"
        ),
        q(
            id = "sql_adv_097",
            categoryId = "sql",
            title = "Database Query Tuning: Bitmap Index Scan vs Bitmap Heap Scan",
            prompt = "In PostgreSQL EXPLAIN plans, what happens during a two-phase 'Bitmap Index Scan' followed by a 'Bitmap Heap Scan'?",
            opt0 = "The database converts text into bitmap images without scanning unindexed pages during complex join queries",
            opt1 = "Phase 1 (Bitmap Index Scan): scans one or more indexes and constructs an in-memory Bitmap of matching physical page addresses",
            opt2 = "The database scans all rows in reverse order by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "The query runs on graphics cards to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "When multiple indexes match (or multiple rows match an index), Postgres creates a bitmap of target page IDs, sorts them, and visits disk pages in sequential order, converting random I/O into fast sequential I/O.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,BitmapIndexScan,BitmapHeapScan,Optimization"
        ),
        q(
            id = "sql_adv_098",
            categoryId = "sql",
            title = "Database Performance: Prepared Statement Leak",
            prompt = "What fatal bug occurs when an application dynamically generates unique SQL strings inside prepared statement calls (e.g. `prepareStatement(\"SELECT * FROM u WHERE id = \" + id)`) without closing statements?",
            opt0 = "The CPU overheats and shuts down by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "Driver and Database Memory Leak: the database and JDBC driver allocate plan cache metadata and server-side cursor handles for every unique string",
            opt2 = "The database stops accepting connections after 100 statements by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "It converts prepared statements to views to enforce referential constraints and table consistency",
            correctIndex = 1,
            explanation = "Prepared statements must use static SQL templates with bind parameters (`?`). Dynamically concatenating strings creates millions of distinct prepared statement handles, exhausting driver and server RAM.",
            difficulty = "Advanced",
            tags = "SQL,PreparedStatement,MemoryLeak,AntiPatterns,JDBC"
        ),
        q(
            id = "sql_adv_099",
            categoryId = "sql",
            title = "Database Storage: PostgreSQL Toast Pointer Dereferencing Cost",
            prompt = "Why does running `SELECT id FROM documents;` take 20 milliseconds, while `SELECT id, long_body FROM documents;` takes 15 seconds on a table with 100,000 rows where `long_body` is 50KB each?",
            opt0 = "Because id is a number and long_body is text by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "Out-of-line TOAST retrieval: `long_body` is compressed and stored in separate TOAST tables",
            opt2 = "Because long_body is encrypted with RSA without scanning unindexed pages during complex join queries",
            opt3 = "Because PostgreSQL forbids projecting text columns by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "TOAST keeps the main table lean. Querying only `id` scans small heap pages in 20ms. Selecting `long_body` forces the engine to read and decompress 5GB of out-of-line TOAST chunks, multiplying I/O by 1000x.",
            difficulty = "Advanced",
            tags = "SQL,PostgreSQL,TOAST,StorageEngines,Performance,SELECT"
        ),
        q(
            id = "sql_adv_100",
            categoryId = "sql",
            title = "The Zen of Relational Engineering: Cost-Based Optimizer Harmony",
            prompt = "What is the ultimate role of the senior database engineer when tuning queries for a modern Cost-Based Optimizer (CBO)?",
            opt0 = "Outsmarting the optimizer by forcing manual index hints on every query by indexing foreign key columns to avoid sequential disk scans",
            opt1 = "Providing the optimizer with accurate statistical distributions (ANALYZE), clean normalized relational schemas, SARGable query predicates, and indexes that align with query access patterns",
            opt2 = "Replacing all SQL queries with stored procedures by indexing foreign key columns to avoid sequential disk scans",
            opt3 = "Deleting the database query cache every morning by indexing foreign key columns to avoid sequential disk scans",
            correctIndex = 1,
            explanation = "Modern CBOs are sophisticated mathematical planning engines. Manual hints are brittle. Senior engineers design schemas, provide up-to-date statistical histograms, and write SARGable SQL that allows the CBO to excel naturally.",
            difficulty = "Advanced",
            tags = "SQL,BestPractices,CostBasedOptimizer,Mastery,Architecture"
        )
    )
}
