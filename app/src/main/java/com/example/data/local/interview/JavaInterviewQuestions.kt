package com.example.data.local.interview

import com.example.domain.model.InterviewQuestion

object JavaInterviewQuestions {

    private fun part1(): List<InterviewQuestion> = listOf(
        InterviewQuestion(
            id = "iq_java_001",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Abstraction vs Encapsulation",
            question = "What is the fundamental difference between Abstraction and Encapsulation in Object-Oriented Programming?",
            shortAnswer = "Abstraction focuses on 'what' an object does by hiding implementation complexity and exposing a clean interface (using abstract classes and interfaces). Encapsulation focuses on 'how' to protect data by binding data and methods together inside a class, hiding internal state via private fields and exposing controlled access through public getters/setters.",
            keyPoints = listOf(
                "Abstraction hides implementation complexity; Encapsulation hides internal data state",
                "Abstraction is achieved via interfaces and abstract classes",
                "Encapsulation is achieved via private fields and access mutators (getters/setters)",
                "Abstraction solves problems at the design level; Encapsulation solves problems at the implementation level",
                "Encapsulation protects invariants and prevents unauthorized direct modification",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_002",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Method Overloading vs Overriding",
            question = "Differentiate between Method Overloading and Method Overriding. Which is resolved at compile-time versus runtime?",
            shortAnswer = "Method Overloading occurs within the same class where methods share the same name but differ in parameter count, types, or order (resolved at compile-time via static binding). Method Overriding occurs when a subclass provides a specific implementation for a method defined in its superclass with identical signature and return type (or covariant return type), resolved dynamically at runtime via virtual method invocation (dynamic dispatch).",
            keyPoints = listOf(
                "Overloading: same class, same method name, different parameter signatures (static polymorphism)",
                "Overriding: parent-child hierarchy, identical method signature (dynamic polymorphism)",
                "Overriding methods cannot have more restrictive access modifiers than the parent",
                "Private, static, and final methods cannot be overridden",
                "Overloading return type alone is insufficient to differentiate methods",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_003",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Covariant Return Types in Java",
            question = "What is a Covariant Return Type in Java, and how does it improve object-oriented design?",
            shortAnswer = "Introduced in Java 5, a covariant return type allows an overriding subclass method to declare a return type that is a subtype of the return type declared in the superclass method. This eliminates the need for awkward and unsafe type casting on the caller side when working with concrete subclass instances.",
            keyPoints = listOf(
                "Overriding method can return a subtype of the parent method's return type",
                "Only applies to non-primitive reference types",
                "Eliminates explicit type casting for callers of subclass methods",
                "Implemented by the compiler generating synthetic bridge methods in bytecode",
                "Commonly used in Builder and Factory patterns",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_004",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Abstract Class vs Interface in Java 8+",
            question = "With default and static methods in Java 8 interfaces, how do Abstract Classes and Interfaces differ today?",
            shortAnswer = "The primary distinction is state and inheritance: 1) A class can implement multiple interfaces but extend only one abstract class. 2) Abstract classes can have instance state (non-static, non-final fields) and constructors, whereas interface fields are always public static final (constants) and cannot have constructors or internal mutable state. Use abstract classes for sharing mutable state among closely related classes; use interfaces to define contracts across unrelated classes.",
            keyPoints = listOf(
                "Multiple inheritance: classes can implement many interfaces but extend only one class",
                "Instance state: abstract classes can maintain mutable state; interfaces cannot",
                "Constructors: abstract classes have constructors; interfaces do not",
                "Access modifiers: interface methods are public by default; abstract classes support protected/package-private",
                "Interfaces represent 'can-do' capabilities; abstract classes represent 'is-a' hierarchies",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_005",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Diamond Problem & Interface Default Methods",
            question = "How does Java resolve the Diamond Problem when a class implements two interfaces that declare the same default method signature?",
            shortAnswer = "Java enforces three disambiguation rules: 1) Classes win over interfaces: an existing method on a superclass always takes precedence over interface default methods. 2) More specific interfaces win: if Interface B extends Interface A, B's default method wins. 3) If two independent interfaces provide conflicting default methods, compilation fails with an ambiguity error; the implementing class must explicitly override the method and choose via InterfaceName.super.methodName() or provide custom logic.",
            keyPoints = listOf(
                "Rule 1: Class method always beats interface default method",
                "Rule 2: Sub-interface default beats super-interface default",
                "Rule 3: Conflicting independent interfaces cause compilation error",
                "Disambiguation syntax: InterfaceA.super.methodName()",
                "Prevents the classic multiple-inheritance ambiguity found in C++",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_006",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Dynamic Method Dispatch & vtable",
            question = "How does the JVM implement runtime polymorphism under the hood? Explain the role of the vtable.",
            shortAnswer = "The JVM implements runtime polymorphism using dynamic method dispatch via virtual method tables (vtables) located in the Method Area / Metaspace for each loaded class. When an overridden method is invoked via the invokevirtual bytecode instruction, the JVM looks up the target method pointer in the receiver object's runtime class vtable rather than using the variable's compile-time type, jumping directly to the concrete subclass implementation.",
            keyPoints = listOf(
                "invokevirtual bytecode instruction triggers dynamic method dispatch",
                "vtable contains array of direct pointers to executable method bytecode",
                "Subclasses inherit parent vtable and overwrite entries for overridden methods",
                "invokestatic and invokespecial use static binding without vtable lookup",
                "JIT compiler optimizes virtual calls via monomorphic inline caching",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_007",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Composition vs Inheritance & Fragile Base Class",
            question = "Why is 'Favor Composition over Inheritance' a core OOP principle? What is the Fragile Base Class problem?",
            shortAnswer = "Inheritance creates tight coupling because subclasses depend on the internal implementation details of the superclass (white-box reuse), which can break subclasses when the superclass changes (the Fragile Base Class problem). Composition achieves black-box reuse by wrapping an instance of another class, exposing only clear interfaces. This enables dynamic runtime behavior swapping, minimizes ripple effects, and prevents accidental inheritance of unsuitable methods.",
            keyPoints = listOf(
                "Inheritance breaks encapsulation by exposing superclass implementation details",
                "Fragile Base Class: modifying a superclass inadvertently breaks subclass invariants",
                "Composition provides loose coupling and black-box reuse",
                "Enables changing delegate implementations dynamically at runtime",
                "Use inheritance strictly when a genuine, permanent 'is-a' relationship holds",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_008",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Pass-by-Value in Java",
            question = "Is Java pass-by-value or pass-by-reference? Explain what happens when an object reference is passed to a method.",
            shortAnswer = "Java is strictly pass-by-value, always. When an object is passed into a method, the value being copied and passed is the reference (the 32-bit or 64-bit memory pointer) to the object, not the object itself. Mutating fields of the passed object through this copied reference affects the original object in the heap. However, reassigning the parameter variable inside the method to a new object (e.g. obj = new Object()) only changes the local copy of the reference and has zero effect on the caller's reference.",
            keyPoints = listOf(
                "Java is strictly pass-by-value for both primitives and references",
                "For objects, the copied value is the reference address itself",
                "Mutating object state via the reference modifies the shared heap instance",
                "Reassigning the parameter reference variable does not alter the caller's pointer",
                "Primitives pass a copy of the raw literal value (int, double, boolean)",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_009",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Designing a Truly Immutable Class",
            question = "What are the strict design rules required to create a truly thread-safe, immutable class in Java?",
            shortAnswer = "To create a truly immutable class: 1) Declare the class as final (prevents subclassing). 2) Make all fields private and final. 3) Do not provide any setter/mutator methods. 4) If the class contains mutable fields (e.g. Date, List), perform defensive copying in the constructor. 5) Return defensive copies (or unmodifiable views) from getters to prevent leaking internal mutable references. 6) Avoid exposing the 'this' reference during constructor execution.",
            keyPoints = listOf(
                "Class declared final to prevent subclass override tampering",
                "All fields private and final (guarantees safe publication by JMM)",
                "No setter methods; state established strictly at construction",
                "Defensive copying of mutable objects in constructors and getters",
                "Java 16+ records provide built-in shallow immutability",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_010",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Shallow Copy vs Deep Copy in Java",
            question = "Differentiate between a Shallow Copy and a Deep Copy. Why is Java's Cloneable interface widely criticized?",
            shortAnswer = "A Shallow Copy duplicates the top-level object fields; primitive fields are copied, but object reference fields still point to the exact same nested instances in the heap. A Deep Copy recursively copies the top-level object and all nested objects, creating completely isolated graphs. Cloneable is heavily criticized because it is a marker interface with no methods (clone() is protected in Object), bypasses normal constructor execution, and performs only shallow copies by default. Copy constructors or serialization/cloning libraries are preferred.",
            keyPoints = listOf(
                "Shallow copy shares references to nested child objects",
                "Deep copy creates distinct copies of all referenced sub-objects",
                "Object.clone() bypasses constructors, breaking initialization invariants",
                "Cloneable interface lacks a public clone() method declaration",
                "Copy constructors (public Foo(Foo other)) are safer and more flexible",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_011",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Why finalize() Was Deprecated in Java 9",
            question = "Why has Object.finalize() been deprecated in Java 9 and marked for removal? What should be used instead?",
            shortAnswer = "finalize() was deprecated because its execution is completely unpredictable: the JVM makes no guarantee when (or even if) a finalizer will run, leading to severe resource leaks. Objects with finalizers delay garbage collection by at least two GC cycles (placed on a Finalizer queue), and uncaught exceptions in finalizers are silently swallowed. Modern Java replaces finalizers with the try-with-resources statement (AutoCloseable) and java.lang.ref.Cleaner.",
            keyPoints = listOf(
                "Execution is non-deterministic and never guaranteed by the JVM",
                "Delays memory reclamation; objects survive into Old Gen on FinalizerQueue",
                "Exceptions in finalize() are swallowed without failing the thread",
                "Replaced by try-with-resources and AutoCloseable for deterministic cleanup",
                "java.lang.ref.Cleaner provides safer, asynchronous off-heap cleanup",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_012",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "final, finally, and finalize Comparison",
            question = "Compare final, finally, and finalize in Java. Can a finally block fail to execute?",
            shortAnswer = "final is a keyword/modifier: makes classes unextendable, methods un-overridable, and variables non-reassignable. finally is a block associated with try-catch that executes regardless of whether an exception occurs, ensuring resource cleanup. finalize is an obsolete Object method called prior to garbage collection. A finally block can fail to execute if: 1) System.exit() is invoked, 2) The JVM crashes (SIGKILL, power failure, JVM fatal error), or 3) An infinite loop or deadlock freezes the try block.",
            keyPoints = listOf(
                "final: modifier for constants, non-overridable methods, unextendable classes",
                "finally: cleanup block in exception handling, runs after try/catch",
                "finalize: deprecated GC hook method in Object",
                "finally bypassed by System.exit(), JVM crashes, or infinite thread freezes",
                "Returning inside finally overrides return values and exceptions from try",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_013",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Static Class Members & Class Loading",
            question = "When are static variables and static initialization blocks executed in Java? Are static methods subject to polymorphism?",
            shortAnswer = "Static variables and static blocks execute exactly once when the class is initialized by the JVM ClassLoader (triggered by first active use, e.g. instantiating an instance, accessing a static member, or Class.forName()). Static methods cannot be overridden polymorphically; if a subclass declares a static method with the same signature, it merely 'hides' the parent method. The method invoked is resolved at compile time based strictly on the reference type (static binding).",
            keyPoints = listOf(
                "Static blocks run once upon class initialization in order of appearance",
                "Class initialization is thread-safe, managed by JVM initialization locks",
                "Static methods cannot be overridden; they are hidden (method hiding)",
                "invokestatic bytecode instruction uses static compile-time binding",
                "Static members belong to the Class object stored in the Metaspace/Heap",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_014",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Static Nested Class vs Inner Class",
            question = "What is the key technical difference between a static nested class and a non-static inner class in Java?",
            shortAnswer = "A non-static inner class maintains an implicit, hidden reference to the enclosing outer class instance (Outer.this), meaning it cannot exist without an outer instance and can cause unintended memory leaks if outer objects are held alive. A static nested class behaves like any top-level class packaged inside another for namespace grouping: it does not hold a reference to an outer instance and cannot access non-static outer members directly.",
            keyPoints = listOf(
                "Non-static inner class holds an implicit pointer to enclosing Outer instance",
                "Inner class can cause memory leaks in callbacks and event listeners",
                "Static nested class does not reference an outer instance (no Outer.this)",
                "Static nested class can only access static members of the outer class",
                "Prefer static nested classes unless outer instance state is required",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_015",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Functional Interfaces & @FunctionalInterface",
            question = "What defines a Functional Interface in Java? Can it contain methods from java.lang.Object?",
            shortAnswer = "A Functional Interface is an interface that declares exactly one abstract method (Single Abstract Method - SAM), enabling it to be implemented using lambda expressions or method references. It can declare any number of default or static methods. Importantly, public abstract methods that override methods from java.lang.Object (like equals(), hashCode(), toString()) do not count toward the single abstract method total.",
            keyPoints = listOf(
                "Single Abstract Method (SAM) contract enables lambda target typing",
                "@FunctionalInterface annotation enforces compiler verification of SAM contract",
                "Can contain unlimited default and static methods without violating SAM",
                "Public methods matching Object signatures (equals) are excluded from SAM count",
                "Examples: Runnable, Callable, Comparator, Predicate, Function",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_016",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Marker Interfaces in Java",
            question = "What is a Marker Interface? Why are modern APIs shifting away from marker interfaces toward Annotations?",
            shortAnswer = "A Marker Interface is an interface with zero method and field declarations (e.g. Serializable, Cloneable, Remote). It acts as a metadata tag signaling to the JVM or runtime libraries that the implementing class possesses a specific capability. Modern Java APIs prefer Annotations because annotations can accept configuration attributes, can be applied to methods/fields/classes, and avoid polluting type hierarchies with empty types.",
            keyPoints = listOf(
                "Contains no methods or fields; serves strictly as a runtime/compile-time tag",
                "Examples: java.io.Serializable, java.lang.Cloneable, java.rmi.Remote",
                "Checked via instanceof at runtime to alter execution behavior",
                "Annotations provide metadata with parameters without altering type hierarchies",
                "Marker interfaces still uniquely define a compile-time type, unlike annotations",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_017",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Access Modifiers & Package-Private Visibility",
            question = "Compare Java's 4 access levels: private, package-private (default), protected, and public. Where is protected accessible?",
            shortAnswer = "1) private: accessible only within the declaring class. 2) package-private (no modifier): accessible only within classes of the exact same package. 3) protected: accessible within the same package, and by subclasses in different packages (via inheritance). 4) public: accessible everywhere. Note: A subclass in another package can only access protected members on an instance of itself (or its own subtypes), not on an arbitrary parent instance.",
            keyPoints = listOf(
                "private: class-only access",
                "default (package-private): package-wide access, invisible outside package",
                "protected: package-wide access + subclass access in external packages",
                "public: unrestricted global visibility",
                "Subclass in another package accesses protected members through inheritance only",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_018",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Auto-Boxing and Integer Cache Gotcha",
            question = "What is autoboxing and unboxing? Why does Integer a = 127; Integer b = 127; return true for a == b, while 128 returns false?",
            shortAnswer = "Autoboxing is the automatic conversion between primitives and their corresponding wrapper classes (e.g. int to Integer via Integer.valueOf()). The JVM maintains an IntegerCache for values between -128 and 127 (configurable via -XX:AutoBoxCacheMax). When autoboxing values in this range, Integer.valueOf() returns the identical cached instance, so a == b is true. For values >= 128, new Integer instances are created in the heap, causing reference equality (==) to return false. Always compare wrappers using .equals().",
            keyPoints = listOf(
                "Autoboxing invokes static valueOf(); unboxing invokes xxxValue()",
                "IntegerCache caches objects between -128 and 127",
                "Values inside [-128, 127] share identical object references (== is true)",
                "Values outside the cache create distinct heap objects (== is false)",
                "Unboxing a null wrapper object throws a NullPointerException",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_019",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Variable Shadowing vs Method Hiding vs Overriding",
            question = "Explain the differences between variable shadowing, field/static method hiding, and method overriding.",
            shortAnswer = "Variable shadowing occurs when a local variable inside a method shares the same name as an instance field, shadowing the field (accessible via this.field). Field and static method hiding occurs when a subclass declares a field or static method with the same name as the superclass; fields and static methods are never polymorphic and are resolved statically based on reference type. Method overriding applies strictly to instance methods, where the runtime object type determines which method executes.",
            keyPoints = listOf(
                "Variable shadowing: local parameter/variable shadows class field",
                "Field hiding: subclass field hides parent field; resolved by reference type",
                "Static method hiding: static methods cannot be polymorphically overridden",
                "Instance methods are overridden polymorphically via virtual dispatch",
                "Avoid declaring fields in subclasses with identical names to parent fields",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_020",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Blank Final and Effectively Final Variables",
            question = "What is a 'blank final' variable? What does 'effectively final' mean for lambda expressions in Java 8+?",
            shortAnswer = "A 'blank final' is a final instance or static variable that is not initialized at its declaration; it must be initialized exactly once in every constructor (for instance fields) or static initializer (for static fields). 'Effectively final' describes a non-final variable whose value is never modified after its initialization. Java 8+ allows local variables captured by lambdas and anonymous classes to be effectively final, eliminating the mandatory explicit 'final' keyword requirement.",
            keyPoints = listOf(
                "Blank final: final variable initialized later in constructor or init block",
                "Compiler enforces that blank finals are assigned exactly once across all code paths",
                "Effectively final: variable whose value is never reassigned after creation",
                "Lambdas can capture effectively final local variables without explicit final modifier",
                "Reassigning an effectively final variable breaks lambda compilation",
            ),
            difficulty = "Mid-Level"
        ),
    )

    private fun part2(): List<InterviewQuestion> = listOf(
        InterviewQuestion(
            id = "iq_java_021",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "Why String is Immutable in Java",
            question = "Why is java.lang.String designed to be immutable and final? Cite security, caching, and thread-safety reasons.",
            shortAnswer = "String is immutable and final for: 1) String Pool Caching: allows multiple references to share identical literal values in heap, saving substantial memory. 2) Security: strings carry sensitive data (passwords, network sockets, DB URLs, file paths); mutability would allow malicious code to alter parameters after validation (TOCTOU attacks). 3) Thread Safety: immutable strings can be freely shared across concurrent threads without synchronization. 4) Hash Code Caching: hashCode is computed once and cached, enabling blazing fast lookups in HashMaps.",
            keyPoints = listOf(
                "Enables String Constant Pool (SCP) memory sharing and deduplication",
                "Critical for security: file paths, URLs, and classloading cannot be tampered with",
                "Thread-safe by default without requiring locks or defensive copies",
                "Enables caching of hashCode() for constant-time Map lookup performance",
                "Declared final to prevent malicious subclassing from altering immutability guarantees",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_022",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "String Constant Pool (SCP) Internals",
            question = "Where does the String Constant Pool reside in modern Java, and what is the difference between string literals and new String()?",
            shortAnswer = "Since Java 7, the String Constant Pool (SCP) resides in the standard Java Heap (previously in PermGen), allowing unreferenced interned strings to be reclaimed by standard Garbage Collection. When using a string literal ('hello'), the JVM checks the pool: if present, it returns the pooled reference; if absent, it creates and pools it. In contrast, 'new String(\"hello\")' forces the allocation of a completely new String object in the heap, while also ensuring the literal 'hello' exists in the SCP (creating 1 or 2 objects).",
            keyPoints = listOf(
                "SCP moved from PermGen to Java Heap in Java 7; garbage collectible",
                "String literals share pooled instances from the SCP",
                "new String(\"abc\") creates a new heap object distinct from the pool reference",
                "String.intern() retrieves or inserts the string reference into the SCP",
                "Comparing literals using == works because both reference the identical SCP address",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_023",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "String.intern() Method Mechanics",
            question = "How does the String.intern() method work, and what are its performance trade-offs in high-throughput applications?",
            shortAnswer = "When s.intern() is invoked, if the pool already contains a string equal to this String, the reference from the pool is returned. Otherwise, this String object is added to the pool and a reference to it is returned. In Java 7+, the pool stores the reference to the existing heap string instead of copying char arrays. Trade-off: heavy manual interning can degrade performance if the internal StringTable hash map suffers high collision rates (tunable via -XX:StringTableSize).",
            keyPoints = listOf(
                "Returns canonical representation from the String Constant Pool",
                "Enables fast reference equality (==) comparisons for frequent repeated tokens",
                "Stores references in native JVM StringTable hashtable",
                "Oversized StringTable without tuning (-XX:StringTableSize) causes hash collisions",
                "G1 String Deduplication is often preferred over manual intern() calls",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_024",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "Compact Strings in Java 9+",
            question = "How did Java 9 Compact Strings change the internal byte representation of String, and how does it halve heap consumption?",
            shortAnswer = "Prior to Java 9, String stored characters in a UTF-16 char[] array, consuming 2 bytes (16 bits) per character even for Latin-1 (ASCII) characters where the leading byte was always zero. Java 9 introduced Compact Strings: replacing char[] with byte[] value and a single byte 'coder' flag (LATIN1 = 0, UTF16 = 1). If all characters fit in Latin-1 (one byte), single-byte encoding is used, cutting String heap consumption by 50% for standard Western text with zero code changes required.",
            keyPoints = listOf(
                "Replaced char[] (2 bytes/char) with byte[] value + byte coder flag",
                "LATIN1 coder uses 1 byte per character for ASCII / ISO-8859-1 strings",
                "UTF16 coder uses 2 bytes per character when multi-byte characters are present",
                "Reduces heap memory usage by 40-50% in typical enterprise applications",
                "Completely transparent to developers; enabled by default (-XX:+CompactStrings)",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_025",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "String vs StringBuilder vs StringBuffer",
            question = "Compare String, StringBuilder, and StringBuffer. When should each be used and why is StringBuilder preferred inside single-threaded loops?",
            shortAnswer = "String is immutable: every concatenation (+) creates new temporary String objects, causing high GC pressure in loops. StringBuffer is mutable and thread-safe: all its public methods are synchronized, preventing race conditions at the cost of synchronization overhead. StringBuilder (Java 5) is mutable but non-synchronized: it offers identical API to StringBuffer without synchronization penalties. StringBuilder is preferred for all single-threaded string manipulations.",
            keyPoints = listOf(
                "String: immutable; creates new objects on modification",
                "StringBuffer: mutable, thread-safe (synchronized methods), higher overhead",
                "StringBuilder: mutable, non-thread-safe, maximum performance",
                "Default internal buffer capacity is 16 characters; doubles via (old * 2) + 2",
                "Modern javac compiler compiles simple + concatenations into StringBuilder automatically",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_026",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "String Concatenation & invokedynamic in Java 9+",
            question = "How does Java 9+ compile string concatenation (+ operator) compared to older Java versions?",
            shortAnswer = "Prior to Java 9, javac compiled string concatenations into explicit StringBuilder.append().toString() bytecodes. In Java 9+, JEP 280 replaced this with the invokedynamic bytecode instruction targeting StringConcatFactory.makeConcatWithConstants. This defers the exact concatenation strategy to runtime recipe evaluation, allowing JVM optimization (like precise single-pass byte array allocation) without altering class files.",
            keyPoints = listOf(
                "Replaced hardcoded StringBuilder bytecode with invokedynamic instruction",
                "Delegates concatenation to StringConcatFactory.makeConcatWithConstants at runtime",
                "Enables exact sizing of the destination byte[] array in a single memory allocation",
                "Avoids intermediate buffer resizing and multiple copies",
                "Future JVM string concatenation optimizations apply automatically without recompilation",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_027",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "G1 GC String Deduplication",
            question = "How does G1 Garbage Collector's automated String Deduplication feature work, and how is it enabled?",
            shortAnswer = "Enabled via -XX:+UseStringDeduplication, G1 GC inspects candidate String objects that have survived minor GC cycles. When it detects two distinct String objects whose underlying byte[] (or char[]) contain identical character sequences, it modifies one String to reference the existing byte[] of the other, allowing the duplicate array to be garbage collected. This transparently reclaims 10–20% heap without altering String object identities.",
            keyPoints = listOf(
                "Enabled with -XX:+UseG1GC -XX:+UseStringDeduplication",
                "Operates on underlying byte[]/char[] arrays, not the String reference itself",
                "Applies to long-lived strings surviving into survivor/tenured spaces",
                "Zero impact on application logic: does not change String object identity",
                "Reclaims significant memory in microservices processing JSON/XML payloads",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_028",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "Storing Passwords in char[] vs String",
            question = "Why is it an industry security best practice to store passwords in a char[] array rather than a java.lang.String?",
            shortAnswer = "Strings are immutable and interned/cached in memory; once created, a password string remains in the heap until Garbage Collection sweeps it (which is non-deterministic). An attacker taking a memory heap dump or an unprivileged process reading process memory can inspect cleartext passwords. With char[], developers can explicitly overwrite the array elements with zeros or dummy characters (Arrays.fill(pwd, '0')) immediately after use, completely wiping credentials from memory.",
            keyPoints = listOf(
                "Strings cannot be zeroed out due to immutability",
                "Plaintext passwords in Strings linger indefinitely until unpredictable GC cycles",
                "char[] arrays can be explicitly zeroed out with Arrays.fill() immediately after use",
                "Prevents password exposure in heap dumps, core dumps, and memory scanning",
                "APIs like java.security and KeyStore accept char[] for credentials",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_029",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "Unicode Surrogate Pairs and Code Points",
            question = "Why can string.length() return a value greater than the actual visual character count for emojis and special symbols in Java?",
            shortAnswer = "Java's char primitive is 16-bit, representing a UTF-16 code unit. Unicode characters outside the Basic Multilingual Plane (BMP, code points > U+FFFF, such as emojis 🚀 or rare Asian characters) require two 16-bit code units called a 'surrogate pair'. string.length() returns the number of 16-bit code units, so a single emoji yields length() == 2. To obtain the true visual character count, use string.codePointCount(0, string.length()).",
            keyPoints = listOf(
                "char represents a 16-bit UTF-16 code unit (max value 65535)",
                "Characters > U+FFFF (emojis, mathematical symbols) use surrogate pairs (2 chars)",
                "length() returns code unit count, not visual character count",
                "codePointCount(0, length()) returns true Unicode code point count",
                "Character.isSurrogate() and codePointAt() assist in accurate parsing",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_030",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "ReDoS: Regular Expression Denial of Service",
            question = "What is Regular Expression Denial of Service (ReDoS) in Java, and how do you protect production services against it?",
            shortAnswer = "ReDoS occurs when an inefficient regular expression with nested quantifiers (e.g. (a+)+\$ or (a|a)+\$) experiences catastrophic backtracking when evaluated against non-matching input strings. The Java regex engine (Pattern/Matcher) uses a Non-deterministic Finite Automaton (NFA) with backtracking, which takes exponential time (O(2^n)) on malicious inputs, consuming 100% CPU on worker threads. Mitigations: 1) Avoid nested quantifiers and ambiguous alternations, 2) Use possessive quantifiers (++ or *+) or atomic groups (?>...) to disable backtracking, 3) Set execution timeouts or use RE2J (linear time DFA).",
            keyPoints = listOf(
                "Catastrophic backtracking causes exponential execution time (O(2^n))",
                "Triggered by nested quantifiers and overlapping alternations on non-matching inputs",
                "Starves CPU threads, resulting in complete service denial of service",
                "Possessive quantifiers (a++ or a*+) prevent the engine from backtracking",
                "Pre-compile Patterns with static final Pattern instances to avoid compilation overhead",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_031",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "JVM Subsystems Architecture",
            question = "What are the three primary subsystems of the JVM? Describe their roles in executing Java bytecode.",
            shortAnswer = "The three subsystems are: 1) ClassLoader Subsystem: loads, links (verify, prepare, resolve), and initializes .class files into memory. 2) Runtime Data Areas: memory partitioned into Method Area/Metaspace (class metadata), Heap (objects), JVM Stacks (thread stack frames), PC Registers (current instruction address), and Native Method Stacks. 3) Execution Engine: executes bytecode instructions via the Interpreter, JIT Compiler (compiles hot code to machine code), and Garbage Collector (reclaims unreferenced heap memory).",
            keyPoints = listOf(
                "ClassLoader Subsystem handles loading, linking, and class initialization",
                "Runtime Data Areas allocate memory across shared (Heap, Metaspace) and thread-private (Stack, PC) zones",
                "Execution Engine houses the Interpreter, C1/C2 JIT Compilers, and Garbage Collector",
                "JNI (Java Native Interface) bridges calls to native C/C++ libraries",
                "Native Method Libraries provide OS-specific functionality",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_032",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Class Loading: Loading, Linking, and Initialization",
            question = "Walk through the three phases of class loading in the JVM: Loading, Linking, and Initialization.",
            shortAnswer = "1) Loading: acquires binary byte array of class from disk/network, creates java.lang.Class instance in heap. 2) Linking: consists of Verification (validates bytecode integrity and JVM structural constraints), Preparation (allocates memory for static variables and assigns default zero/null values), and Resolution (transforms symbolic references into direct memory pointers). 3) Initialization: executes static variable assignments and static initialization blocks (<clinit>) in source code order.",
            keyPoints = listOf(
                "Loading converts binary stream into Class<?> object in Metaspace/Heap",
                "Linking Verification guarantees bytecodes cannot crash or violate JVM security",
                "Linking Preparation zeroes static variables (e.g. int = 0, object = null)",
                "Linking Resolution converts constant pool symbolic references to direct memory addresses",
                "Initialization executes developer-specified static initializers and variable values",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_033",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "ZGC (Z Garbage Collector) Architecture",
            question = "How does ZGC achieve sub-millisecond maximum pause times on multi-terabyte heaps in modern Java?",
            shortAnswer = "ZGC (Z Garbage Collector) performs almost all its work concurrently with application threads (concurrent mark, concurrent pre-thread stack processing, concurrent relocate/compact). It achieves sub-millisecond pause times through two revolutionary innovations: 1) Colored Pointers: reference pointers carry metadata bits (Marked0, Marked1, Remapped) directly in the unused bits of 64-bit memory addresses. 2) Load Barriers: small JIT-compiled interceptors on field dereferences that test the pointer color; if an object was relocated, the load barrier self-heals the pointer to the new address before the thread reads it.",
            keyPoints = listOf(
                "Sub-millisecond pause times (<1ms) regardless of heap size (up to 16TB)",
                "Concurrent phases: marking, relocation (compaction), and reference processing",
                "Colored pointers store GC metadata directly inside reference pointer bits",
                "Load barriers intercept object reads, self-healing references to relocated addresses",
                "Generational ZGC (Java 21) separates young and old objects for higher throughput",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_034",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Shenandoah GC vs ZGC Comparison",
            question = "Compare Shenandoah GC with ZGC. How does Shenandoah perform concurrent compaction without colored pointers?",
            shortAnswer = "Both Shenandoah and ZGC are low-pause concurrent collectors. While ZGC historically used colored pointers in reference bits, Shenandoah originally used Brooks Pointers: an extra forwarding pointer prefixed to each object header. When an object is evacuated to a new region, its forwarding pointer is atomically CAS-updated to point to the new copy. Shenandoah uses read/load-reference barriers to follow forwarding pointers, allowing concurrent compaction without stopping application threads.",
            keyPoints = listOf(
                "Both achieve ultra-low pause times via concurrent heap compaction",
                "Shenandoah uses forwarding pointer headers and load-reference barriers",
                "ZGC uses colored pointers in 64-bit virtual memory addresses",
                "Both collectors avoid long Stop-the-World pauses during full heap evacuation",
                "Ideal for real-time trading systems and high-throughput low-latency microservices",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_035",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Diagnosing OutOfMemoryError: Java heap space",
            question = "What causes java.lang.OutOfMemoryError: Java heap space, and how do you diagnose memory leaks using heap dumps?",
            shortAnswer = "Causes: 1) True memory leak (unreferenced objects remain strongly reachable via static collections, ThreadLocals, or unclosed listeners). 2) Undersized heap allocation (-Xmx too low for peak load). 3) Sudden traffic spike creating excessive short-lived objects. Diagnosis: Configure -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/dumps/ to generate a heap dump (.hprof) on failure. Analyze the dump using Eclipse MAT (Memory Analyzer Tool) or VisualVM: inspect the 'Leak Suspects' report, check the Dominator Tree to identify objects retaining the largest retained heap, and trace GC Roots to identify which reference holds them.",
            keyPoints = listOf(
                "Triggered when allocator cannot satisfy an object allocation after Full GC",
                "Differentiate between memory leaks and memory exhaustion (undersized heap)",
                "Enable -XX:+HeapDumpOnOutOfMemoryError for post-mortem forensics",
                "Eclipse MAT Dominator Tree identifies retained heap consumers",
                "Path to GC Roots exposes the exact strong reference preventing collection",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_036",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Diagnosing OutOfMemoryError: Metaspace",
            question = "What causes java.lang.OutOfMemoryError: Metaspace, and how is it resolved in dynamic frameworks?",
            shortAnswer = "Metaspace stores loaded class metadata, bytecode representations, method annotations, and method tables in native OS memory. An OutOfMemoryError: Metaspace occurs when classes are continuously generated and loaded by custom classloaders without being unloaded (classloader leak), common in frameworks using dynamic proxies, CGLIB, Groovy scripts, or frequent application hot-redeployments in servlet containers. Fixes: 1) Increase -XX:MaxMetaspaceSize, 2) Enable class unloading, 3) Profile classloader retention in heap dumps to eliminate circular classloader leaks.",
            keyPoints = listOf(
                "Stores class metadata and constant pools in native off-heap memory",
                "Caused by excessive dynamic class generation (CGLIB, reflection proxies, scripting)",
                "Common in hot-redeployments where old ClassLoaders are retained by ThreadLocals",
                "Classes are only unloaded when their ClassLoader itself is garbage collected",
                "Diagnose via jcmd <pid> VM.metaspace and -verbose:class logs",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_037",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "GC Roots and Reachability Analysis",
            question = "What qualifies as a 'GC Root' in the JVM, and how does reachability analysis determine if an object can be collected?",
            shortAnswer = "The JVM uses reachability analysis: it traverses object reference graphs starting from a set of known alive anchor points called GC Roots. Any object unreachable from any GC Root is garbage. Standard GC Roots include: 1) Local variables and parameters on active thread stack frames. 2) Active Java threads (Thread instances). 3) Static variables of classes loaded by system classloaders. 4) JNI (Java Native Interface) local and global native references. 5) Objects used as monitors for synchronization synchronization locks.",
            keyPoints = listOf(
                "Replaces reference counting, naturally resolving circular references (A -> B -> A)",
                "Thread stack frame local variables and parameter references are GC Roots",
                "Static fields of loaded classes are GC Roots",
                "Active running Thread objects are GC Roots",
                "JNI global and local handles from C/C++ native code are GC Roots",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_038",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Reference Types: Strong, Soft, Weak, Phantom",
            question = "Compare Strong, Soft, Weak, and Phantom references in Java. What are their respective use cases?",
            shortAnswer = "1) Strong Reference: normal default reference (Object o = new Object()); never collected if reachable. 2) SoftReference: collected only when memory is low right before an OutOfMemoryError; ideal for memory-sensitive caches. 3) WeakReference: collected immediately during the very next GC cycle regardless of memory pressure; ideal for canonical mappings like WeakHashMap. 4) PhantomReference: get() always returns null; enqueued in ReferenceQueue only after object memory is finalized; ideal for scheduling off-heap post-mortem cleanup (replaces finalize()).",
            keyPoints = listOf(
                "Strong: standard pointer; collected only when completely unreachable",
                "SoftReference: collected under memory pressure; used for memory-sensitive caches",
                "WeakReference: collected at next GC cycle; used in WeakHashMap and ThreadLocal keys",
                "PhantomReference: get() returns null; monitors object reclamation in ReferenceQueue",
                "Cleaner API uses PhantomReferences to manage native memory deallocation",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_039",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "JIT Compiler: C1, C2, and Tiered Compilation",
            question = "How does Tiered Compilation work in the HotSpot JVM? Explain the interaction between the Interpreter, C1, and C2 compilers.",
            shortAnswer = "Tiered Compilation combines fast application startup with peak long-term execution speed across 5 execution tiers. The JVM initially executes bytecode via the Interpreter (Tier 0). As a method becomes 'hot' (invocations and backedge loops exceed counters), Tier 1-3 C1 (Client) compiler compiles it with profiling instrumentation for fast warmup. When counters indicate extremely hot code, Tier 4 C2 (Server) compiler uses the collected profiling data to apply aggressive global optimizations (loop unrolling, escape analysis, inlining, vectorization) for maximum throughput.",
            keyPoints = listOf(
                "Tier 0: Interpreter provides immediate fast startup without compilation delays",
                "Tier 1-3: C1 compiler generates quickly compiled machine code with profiling hooks",
                "Tier 4: C2 compiler applies deep optimizations for peak steady-state performance",
                "Deoptimization: if speculative assumptions fail, code falls back to Interpreter",
                "Enabled by default (-XX:+TieredCompilation) in HotSpot JVM",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_040",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Escape Analysis and Scalar Replacement",
            question = "What is JIT Escape Analysis, and how does it allow the JVM to avoid heap allocation via Scalar Replacement?",
            shortAnswer = "Escape Analysis is a JIT compiler optimization that analyzes whether a newly created object's reference escapes the declaring method or thread. If an object does not escape (e.g. created, used, and discarded entirely within a local method scope), the C2 compiler applies Scalar Replacement: it dissolves the object into its individual primitive fields (scalars) and allocates them directly in CPU registers or the thread stack. This completely eliminates heap allocation, pointer dereferencing, and GC overhead.",
            keyPoints = listOf(
                "Analyzes if object pointer escapes to external fields, return values, or other threads",
                "NoEscape: object stays strictly within local method scope",
                "Scalar Replacement dissolves object into primitive scalar fields in CPU registers / stack",
                "Lock Elision: removes synchronized locks on objects that do not escape threads",
                "Enabled by default via -XX:+DoEscapeAnalysis",
            ),
            difficulty = "Staff"
        ),
    )

    private fun part3(): List<InterviewQuestion> = listOf(
        InterviewQuestion(
            id = "iq_java_041",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Thread Dumps and Thread States Analysis",
            question = "How do you capture and analyze a JVM thread dump to diagnose CPU spikes versus thread deadlocks?",
            shortAnswer = "Capture a thread dump using 'jcmd <pid> Thread.print' or 'jstack -l <pid>'. For CPU spikes: take 3 thread dumps 10 seconds apart. Run 'top -H -p <pid>' to identify high-CPU OS thread IDs, convert the thread ID to hexadecimal, and search for that 'nid=0x...' in the thread dumps to find the exact line of executing Java code. For Deadlocks: the thread dump automatically prints a 'Found one Java-level deadlock' block at the bottom, listing the conflicting threads, the locks they hold, and the locks they are waiting to acquire.",
            keyPoints = listOf(
                "Capture with jcmd <pid> Thread.print or jstack -l <pid>",
                "Cross-reference top -H -p <pid> thread ID (converted to hex) with nid=<hex>",
                "Multiple consecutive thread dumps expose threads stuck in tight loops or waiting",
                "Thread dumps report monitor lock ownership (locked <0x...>, waiting to lock <0x...>)",
                "Identifies BLOCKED, WAITING, and RUNNABLE thread contention",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_042",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Java Flight Recorder (JFR) in Production",
            question = "What makes Java Flight Recorder (JFR) safe for continuous production monitoring compared to traditional profilers?",
            shortAnswer = "JFR is integrated directly into the HotSpot JVM kernel rather than relying on heavy bytecode instrumentation or JVMTI polling hooks. It captures events (CPU execution samples, memory allocations, thread locks, GC pauses, I/O latency) using pre-allocated ring buffers in native memory with an overhead of less than 1-2%, making it safe to run continuously in production. Recordings (.jfr) can be analyzed visually using JDK Mission Control (JMC).",
            keyPoints = listOf(
                "Built directly into HotSpot JVM kernel; ultra-low overhead (<1-2%)",
                "Safe for 24/7 continuous production profiling without crashing apps",
                "Captures thread execution, object allocation hot spots, GC events, and I/O bottlenecks",
                "Starts via command line: -XX:StartFlightRecording or jcmd <pid> JFR.start",
                "Visualized via JDK Mission Control (JMC) for root-cause latency analysis",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_043",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "ArrayList vs LinkedList Architectural Comparison",
            question = "Why does ArrayList outperform LinkedList in virtually all real-world enterprise scenarios, even for insertions in the middle?",
            shortAnswer = "ArrayList is backed by a contiguous array in memory, providing exceptional CPU cache locality: when one element is accessed, modern CPU L1/L2 caches prefetch adjacent elements into cache lines. LinkedList consists of fragmented Node objects scattered across the heap connected by pointers, causing CPU cache misses on every traversal. Although LinkedList has O(1) pointer insertion once at the position, finding the insertion position requires O(n) pointer chasing. For ArrayList, System.arraycopy moves contiguous memory blocks extremely fast via SIMD CPU instructions, making ArrayList faster and consuming 4x less memory.",
            keyPoints = listOf(
                "ArrayList contiguous array maximizes CPU L1/L2 cache prefetching (cache lines)",
                "LinkedList scattered heap nodes cause frequent CPU cache misses and pointer chasing",
                "LinkedList consumes ~24-32 extra bytes per node for pointers and object headers",
                "System.arraycopy uses hardware memory block transfers for fast element shifting",
                "ArrayList provides instant O(1) random index access; LinkedList requires O(n) traversal",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_044",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "ArrayList Dynamic Resizing Algorithm",
            question = "How does ArrayList dynamically grow when capacity is exhausted? What is its growth factor?",
            shortAnswer = "When an element is added and size equals elementData.length, ArrayList grows by 50% using the formula: newCapacity = oldCapacity + (oldCapacity >> 1) (bitwise right shift divides by 2). A new array of this size is allocated, and existing elements are copied via Arrays.copyOf / System.arraycopy. Amortized time complexity of add() remains O(1). Pre-sizing the ArrayList with an initial capacity (new ArrayList<>(1000)) avoids redundant resizing and array copies.",
            keyPoints = listOf(
                "Default initial capacity is 10 (allocated on first element insertion)",
                "Growth formula: newCapacity = oldCapacity + (oldCapacity >> 1) (~1.5x growth)",
                "Amortized insertion time is O(1); individual resizing insertion is O(n)",
                "Arrays.copyOf allocates new memory and executes native System.arraycopy",
                "Always pre-size ArrayList when expected element count is known",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_045",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "HashMap Capacity, Load Factor, and Power of Two",
            question = "Why must HashMap table capacity always be a power of two? What role does the load factor (0.75) play?",
            shortAnswer = "HashMap requires capacity n to be a power of two so that the bucket index can be computed using the ultra-fast bitwise AND operation: index = (n - 1) & hash, which is identical to hash % n but executes in a single CPU cycle. If n were not a power of 2, (n - 1) would have zero bits, causing severe hash collisions across certain buckets. The load factor (default 0.75) represents the trade-off between memory space and lookup time: when size exceeds capacity * 0.75, the table doubles to preserve O(1) lookup speed without excessive empty bucket waste.",
            keyPoints = listOf(
                "Power of 2 enables index calculation via (n - 1) & hash instead of expensive modulo (%)",
                "(n - 1) creates a bitmask of all 1s, ensuring uniform distribution across all bucket indices",
                "Load factor 0.75 is the optimal mathematical balance based on Poisson distribution",
                "Higher load factor saves memory but increases collisions and chain lengths",
                "Table doubles in size upon reaching threshold = capacity * loadFactor",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_046",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "HashMap High-Bits Perturbation Function",
            question = "Why does HashMap apply a perturbation function (hash ^ (hash >>> 16)) before computing the bucket index?",
            shortAnswer = "Most HashMap tables have relatively small capacities (e.g. 16 or 32). In the bitwise index formula (n - 1) & hash, only the lowest 4 or 5 bits of the hash code are evaluated; the high 16 bits are masked out and ignored. The perturbation function (key.hashCode() ^ (key.hashCode() >>> 16)) spreads the entropy of the upper 16 bits into the lower 16 bits via XOR, ensuring that differences in the high bits influence bucket selection and preventing massive clustering collisions.",
            keyPoints = listOf(
                "Small tables only use the bottom few bits of the 32-bit hash code",
                "Hash codes varying only in higher bits would all collide in the same bucket",
                "XORing hash with (hash >>> 16) mixes high-bit entropy into low bits",
                "Executes in negligible time (single shift and XOR) before index lookup",
                "Significantly reduces collision rates for objects with sequential IDs",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_047",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "LinkedHashMap & Building an LRU Cache",
            question = "How does LinkedHashMap maintain order? How do you implement an LRU (Least Recently Used) cache with it?",
            shortAnswer = "LinkedHashMap extends HashMap by maintaining a doubly-linked list running through all its entries (Entry extends HashMap.Node with before and after pointers). It supports two ordering modes: insertion-order (default) and access-order (configured via constructor boolean accessOrder = true, where get() or put() moves the accessed entry to the end of the list). To build an LRU Cache: extend LinkedHashMap with accessOrder=true, and override removeEldestEntry(Map.Entry eldest) to return size() > maxCapacity. When the map exceeds capacity, the eldest entry at the head is automatically evicted.",
            keyPoints = listOf(
                "Maintains a doubly-linked list through all bucket nodes (before, after pointers)",
                "accessOrder = true moves queried entries to tail (most recently used)",
                "removeEldestEntry(eldest) hook enables automated eviction policies",
                "Provides O(1) get, put, and eviction for LRU cache implementation",
                "Must be synchronized (Collections.synchronizedMap) if accessed by concurrent threads",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_048",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "TreeMap, TreeSet, and Comparable Contract",
            question = "How does TreeMap store entries internally, and why can it violate Set contracts if compareTo() is inconsistent with equals()?",
            shortAnswer = "TreeMap and TreeSet are backed by a Red-Black Tree (a self-balancing binary search tree) guaranteeing O(log n) time complexity for get(), put(), remove(), containsKey(). Rather than using hashCode() and equals(), TreeMap uses the Comparable.compareTo() (or Comparator.compare()) method to test key equality (if compare(k1, k2) == 0, keys are considered equal). If compareTo() returns 0 for two objects that return false from equals(), TreeMap/TreeSet treats them as duplicates, violating the Set interface contract.",
            keyPoints = listOf(
                "Backed by a Red-Black Tree with guaranteed O(log n) operations",
                "Keys are sorted according to natural ordering or custom Comparator",
                "Equality determined strictly by compare(k1, k2) == 0, ignoring equals() and hashCode()",
                "NavigableMap methods: firstKey(), lastKey(), ceilingKey(), floorKey(), subMap()",
                "Does not permit null keys in natural ordering (throws NullPointerException)",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_049",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "PriorityQueue Internals & Binary Heap",
            question = "How does PriorityQueue work internally in Java? Does iterator() traverse elements in priority order?",
            shortAnswer = "PriorityQueue is an unbounded priority queue implemented as a balanced binary min-heap stored inside a dynamically-resizing array (elementData[]). The parent of element at index i is at (i - 1)/2; children are at 2i + 1 and 2i + 2. Insertion (offer) appends to the end and executes siftUp() (O(log n)); poll() removes the root element at index 0 and executes siftDown() (O(log n)); peek() is O(1). Crucially, iterator() does NOT traverse elements in priority order; it traverses the backing array sequentially. To extract in order, elements must be polled via poll().",
            keyPoints = listOf(
                "Implemented as a binary min-heap inside a contiguous array",
                "offer() and poll() run in O(log n) time via siftUp and siftDown",
                "peek() retrieves the min/max element in O(1) time at index 0",
                "iterator() does NOT guarantee sorted/priority order traversal",
                "Non-thread-safe; use PriorityBlockingQueue for concurrent thread access",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_050",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "Fail-Fast vs Fail-Safe Iterators",
            question = "What is the difference between Fail-Fast and Fail-Safe iterators? What triggers ConcurrentModificationException?",
            shortAnswer = "Fail-Fast iterators (ArrayList, HashMap, HashSet) operate directly on the underlying collection. They maintain an expectedModCount equal to the collection's modCount. If the collection is structurally modified (add/remove) by any thread during iteration (other than via the iterator's own remove() method), next() detects expectedModCount != modCount and immediately throws ConcurrentModificationException. Fail-Safe / Weakly-Consistent iterators (ConcurrentHashMap, CopyOnWriteArrayList) iterate over a clone, snapshot, or concurrent view, never throwing ConcurrentModificationException.",
            keyPoints = listOf(
                "Fail-fast: throws ConcurrentModificationException immediately upon structural modification",
                "Tracked via modCount field incremented on structural edits",
                "Iterator's own remove() updates expectedModCount and is safe to use during iteration",
                "Fail-safe operates on a snapshot (CopyOnWriteArrayList) or concurrent segment view",
                "Fail-safe iterators do not reflect modifications made after iteration begins",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_051",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "CopyOnWriteArrayList Trade-Offs",
            question = "How does CopyOnWriteArrayList achieve thread safety without locking reads? When does its performance collapse?",
            shortAnswer = "CopyOnWriteArrayList provides thread-safe iteration without read locks by ensuring the backing array is volatile and immutable. When a write operation (add, set, remove) occurs, the entire underlying array is copied into a new array with the modification applied, and the volatile array reference is updated using a ReentrantLock. Reads and iterations read the immutable snapshot without locks. Performance collapses under write-heavy workloads because every single write requires allocating and copying the entire array, resulting in severe CPU overhead and GC churn.",
            keyPoints = listOf(
                "Reads and iterations are completely lock-free and fast",
                "Every mutating write copies the entire array into a new memory buffer",
                "Iterators reflect the snapshot of the array at the moment iterator() was created",
                "Iterator remove(), set(), add() are not supported (throw UnsupportedOperationException)",
                "Ideal for event listener lists where reads outnumber writes 100:1",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_052",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "ArrayDeque vs Stack vs LinkedList",
            question = "Why is java.util.Stack deprecated/obsolete, and why is ArrayDeque preferred over LinkedList for Stacks and Queues?",
            shortAnswer = "java.util.Stack is an obsolete legacy class from Java 1.0 that extends Vector: all its methods (push, pop, peek) are synchronized, adding unnecessary locking overhead, and it violates the LIFO principle by permitting index-based operations. For both Stack and Queue implementations, ArrayDeque is preferred over LinkedList because ArrayDeque uses a circular resizable array with no node allocation overhead, excellent CPU cache locality, and zero synchronization overhead. LinkedList allocates an extra Node object for every element.",
            keyPoints = listOf(
                "Stack extends Vector, inheriting synchronized method locking penalties",
                "Stack violates LIFO abstraction by exposing index-based Vector methods",
                "ArrayDeque implements Deque interface via efficient circular array buffer",
                "ArrayDeque avoids per-element node allocation and pointer overhead of LinkedList",
                "ArrayDeque does not allow null elements (throws NullPointerException)",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_053",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "Generics Type Erasure and Bridge Methods",
            question = "What is Type Erasure in Java Generics, and why does the compiler generate synthetic Bridge Methods?",
            shortAnswer = "Type Erasure was introduced in Java 5 to maintain backwards compatibility with pre-generics bytecode. The compiler replaces all generic type parameters with their upper bound (or Object if unbounded) and inserts explicit type casts at compile time; generic type information is erased from bytecode. When a generic class is inherited and an overridden method specializes the parameter type, the compiler synthesizes a synthetic Bridge Method in bytecode with the original erased signature (e.g. method(Object)) that delegates to the specialized method (e.g. method(String)) to preserve polymorphic dispatch.",
            keyPoints = listOf(
                "Generic type parameters are erased to Object (or upper bound) in compiled bytecode",
                "Ensures full binary compatibility with legacy pre-Java 5 libraries",
                "Bridge methods synthesized by compiler to preserve method overriding contracts",
                "Cannot instantiate generic types directly (new T() is invalid)",
                "Cannot create generic arrays (new T[10] is invalid; use reflection or lists)",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_054",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "PECS Rule: Producer Extends, Consumer Super",
            question = "Explain the PECS rule (Producer Extends, Consumer Super) in Java Generics wildcards with concrete examples.",
            shortAnswer = "PECS stands for Producer Extends, Consumer Super: 1) If a parameterized collection produces data for you to read, declare it with '? extends T' (e.g. List<? extends Number>). You can safely read items as type T, but you cannot add anything to it (except null) because the exact subtype is unknown. 2) If a collection consumes data you supply to write, declare it with '? super T' (e.g. List<? super Integer>). You can safely add items of type T, but reading only guarantees items of type Object. This enables maximal API flexibility.",
            keyPoints = listOf(
                "Producer Extends: use <? extends T> when reading elements from a collection",
                "Consumer Super: use <? super T> when writing/adding elements into a collection",
                "<? extends T> is read-only for items; compiler blocks adding new instances",
                "<? super T> allows adding T or its subtypes; reads return Object",
                "Collections.copy(List<? super T> dest, List<? extends T> src) is the classic example",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_055",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "Thread Lifecycle States in Java",
            question = "Describe the 6 thread states in Java as defined by java.lang.Thread.State. What transitions a thread to BLOCKED versus WAITING?",
            shortAnswer = "The 6 states are: 1) NEW: instantiated but not started. 2) RUNNABLE: executing in JVM or ready to run in OS scheduler. 3) BLOCKED: waiting to acquire a monitor lock to enter/re-enter a synchronized block/method. 4) WAITING: waiting indefinitely for another thread to perform an action (via Object.wait(), Thread.join(), or LockSupport.park()). 5) TIMED_WAITING: waiting for a specified timeout (Thread.sleep(), wait(timeout), join(timeout)). 6) TERMINATED: completed execution. A thread is BLOCKED strictly by monitor locks; WAITING is triggered by explicit wait/park calls.",
            keyPoints = listOf(
                "NEW: thread object created, start() not yet invoked",
                "RUNNABLE: executing or ready for CPU time in OS scheduler",
                "BLOCKED: strictly waiting to acquire an intrinsic monitor lock (synchronized)",
                "WAITING: waiting indefinitely via wait(), join(), or LockSupport.park()",
                "TIMED_WAITING: waiting with timeout via sleep(t), wait(t), parkNanos(t)",
                "TERMINATED: run() method finished normally or threw uncaught exception",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_056",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "Java Memory Model (JMM) & Happens-Before",
            question = "What is the Java Memory Model (JMM), and what does the 'Happens-Before' relationship guarantee?",
            shortAnswer = "The JMM specifies how the JVM and CPU hardware caches interact with main memory, defining visibility and ordering rules for shared variables. The 'Happens-Before' relationship is a formal guarantee that memory writes by one thread are guaranteed to be visible to another thread. Key happens-before rules: 1) Program Order: within a single thread, each statement happens-before later statements. 2) Monitor Lock: unlocking a monitor happens-before any subsequent lock acquisition on the same monitor. 3) Volatile: writing to a volatile variable happens-before any subsequent read of that variable. 4) Thread Start/Join: Thread.start() happens-before any action in the started thread; completion of thread actions happens-before a successful join().",
            keyPoints = listOf(
                "Defines memory visibility and instruction reordering constraints across threads",
                "Without happens-before, CPUs can reorder instructions and cache writes in L1/L2 buffers",
                "Volatile write happens-before subsequent volatile read of the same variable",
                "Unlock of monitor happens-before subsequent lock of the same monitor",
                "Transitivity: if A happens-before B and B happens-before C, then A happens-before C",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_057",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "Why wait() and notify() Must Be in synchronized Blocks",
            question = "Why must Object.wait() and notify() be invoked inside a synchronized block on that object? Why call wait() in a while loop?",
            shortAnswer = "wait() and notify() must be inside synchronized to prevent the 'Lost Wakeup' race condition: without a mutual exclusion lock, Thread A could check a condition, and before it calls wait(), Thread B could change the condition and call notify(); Thread A would then wait forever, missing the notification. wait() must ALWAYS be in a 'while (condition)' loop rather than an 'if' because of 1) Spurious Wakeups (the OS/JVM can wake a thread without a notify() call), and 2) Race after notification (another thread might acquire the lock first and invalidate the condition before this thread resumes).",
            keyPoints = listOf(
                "Throws IllegalMonitorStateException if invoked without holding the object monitor",
                "Prevents lost wakeups where notify() fires between condition check and wait() call",
                "wait() atomically releases the monitor lock and suspends the thread",
                "Must always call inside while (!condition) loop to guard against spurious wakeups",
                "Guards against state invalidation when multiple threads are awakened by notifyAll()",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_058",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "ReentrantLock vs synchronized",
            question = "What advanced capabilities does ReentrantLock provide over the built-in synchronized keyword?",
            shortAnswer = "ReentrantLock (from java.util.concurrent.locks) provides: 1) Non-blocking lock acquisition via tryLock(). 2) Timed lock acquisition via tryLock(timeout, unit). 3) Interruptible lock acquisition via lockInterruptibly(). 4) Fairness option: new ReentrantLock(true) guarantees longest-waiting threads acquire the lock first (prevents starvation). 5) Multiple Condition variables on a single lock (lock.newCondition()), allowing separate wait-sets for reading and writing (e.g. notFull, notEmpty in ArrayBlockingQueue). However, synchronized automatically releases locks, whereas ReentrantLock requires explicit unlock() in a finally block.",
            keyPoints = listOf(
                "tryLock() prevents indefinite thread blocking and facilitates deadlock avoidance",
                "Fairness policy ensures FIFO lock acquisition order",
                "lockInterruptibly() allows threads to respond to Thread.interrupt() while waiting",
                "Multiple Condition objects per lock replace single wait-set limitation",
                "Mandatory try-finally structure: lock.lock(); try { ... } finally { lock.unlock(); }",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_059",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "ReadWriteLock & StampedLock Optimistic Reads",
            question = "How does ReentrantReadWriteLock improve read-heavy concurrency? How does StampedLock eliminate write starvation?",
            shortAnswer = "ReentrantReadWriteLock maintains a pair of locks: multiple threads can hold the read lock concurrently (shared), but the write lock is exclusive. This drastically boosts throughput for read-heavy datasets. However, high read volume can starve write threads. StampedLock (Java 8) introduces Optimistic Reads: a thread obtains an optimistic stamp (tryOptimisticRead()) without acquiring any lock at all, reads fields, and validates the stamp via validate(stamp). If a write occurred during reading, validation fails, and the thread falls back to an acquiring read lock. This completely prevents writer starvation.",
            keyPoints = listOf(
                "ReadWriteLock allows multiple concurrent readers; writers are exclusive",
                "Risk of writer starvation under continuous read load in ReadWriteLock",
                "StampedLock optimistic reads do not acquire any lock or update state",
                "validate(stamp) checks if a write lock intervened during reading",
                "StampedLock is not reentrant; recursion causes self-deadlock",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_060",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "AtomicLong vs LongAdder Under Extreme Contention",
            question = "Why does LongAdder vastly outperform AtomicLong under extreme multi-threaded write contention?",
            shortAnswer = "AtomicLong uses a single volatile 64-bit value and relies on CAS (Compare-And-Swap) loops (compareAndSet). Under high thread contention (e.g. 64 threads incrementing), most threads fail their CAS loop repeatedly due to cache line invalidation, spinning CPU cycles and causing bus traffic storms. LongAdder (Java 8) maintains a cell array (Cell[]): threads hash to different cells and increment them independently without contention. When sum() is called, it sums the cells. This eliminates thread contention bottlenecks during metric collection.",
            keyPoints = listOf(
                "AtomicLong experiences high CAS loop retry rates under heavy concurrent updates",
                "LongAdder dynamically scales an internal array of Cell objects (striping)",
                "Threads hash to separate cells, eliminating cross-thread contention",
                "sum() aggregates base and cell values for the final result",
                "Ideal for high-throughput counters, metrics, and statistics tracking",
            ),
            difficulty = "Senior"
        ),
    )

    private fun part4(): List<InterviewQuestion> = listOf(
        InterviewQuestion(
            id = "iq_java_061",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "CountDownLatch vs CyclicBarrier vs Semaphore",
            question = "Compare CountDownLatch, CyclicBarrier, and Semaphore. Give a real-world scenario for each.",
            shortAnswer = "1) CountDownLatch: one or more threads wait until a set of operations being performed by other threads completes (countdown reaching zero). It is a one-time-use latch (cannot be reset). Scenario: waiting for 5 microservice warmup checks before starting the web container. 2) CyclicBarrier: a synchronization point where multiple threads wait for each other to reach a common barrier point. It can be reset and reused (cyclic). Scenario: parallel matrix computation steps where threads must sync at the end of each step. 3) Semaphore: controls the number of concurrent permits to access a shared resource. Scenario: rate-limiting outbound calls to 10 concurrent HTTP connections.",
            keyPoints = listOf(
                "CountDownLatch: one-shot latch; countDown() decrements, await() blocks until 0",
                "CyclicBarrier: reusable barrier; all parties call await() and proceed together",
                "CyclicBarrier supports an optional barrier action Runnable executed when barrier trips",
                "Semaphore: maintains permits via acquire() and release(); enforces concurrency caps",
                "Binary Semaphore (1 permit) acts as a non-reentrant mutual exclusion lock",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_062",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "Deadlock Detection and Prevention Strategies",
            question = "What are the 4 Coffman conditions required for a Deadlock? How do you prevent deadlocks in Java?",
            shortAnswer = "The 4 Coffman conditions are: 1) Mutual Exclusion, 2) Hold and Wait, 3) No Preemption, 4) Circular Wait. If any one condition is broken, deadlock cannot occur. In Java, prevention strategies include: 1) Lock Ordering: always acquire locks in a globally defined, consistent order (e.g. sort account IDs before locking sender and receiver accounts; breaks Circular Wait). 2) Lock Timeouts: use ReentrantLock.tryLock(timeout) instead of synchronized (breaks Hold and Wait / No Preemption). 3) Deadlock Detection: use ThreadMXBean.findDeadlockedThreads() or inspect thread dumps.",
            keyPoints = listOf(
                "Four Coffman conditions: Mutual Exclusion, Hold and Wait, No Preemption, Circular Wait",
                "Lock Ordering: ordering lock acquisition globally completely breaks Circular Wait",
                "tryLock(timeout) allows backing off and releasing already-held locks on failure",
                "Minimize lock scope: avoid calling external foreign methods while holding a lock",
                "ThreadMXBean detects deadlocks programmatically at runtime",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_063",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "False Sharing & @Contended Annotation",
            question = "What is False Sharing in multi-core processors, and how does Java's @Contended annotation eliminate it?",
            shortAnswer = "Modern CPU architectures load and invalidate memory in 64-byte chunks called Cache Lines. False Sharing occurs when two independent variables accessed by threads on different CPU cores reside on the same 64-byte cache line. When Thread 1 modifies its variable, the CPU invalidates the entire cache line in Thread 2's L1/L2 cache via cache coherence protocols (MESI), forcing Thread 2 to reload from L3/RAM, crippling throughput. The @Contended annotation (internal to JDK, enabled via -XX:-RestrictContended) inserts 128 bytes of padding around fields to ensure they occupy isolated cache lines.",
            keyPoints = listOf(
                "CPUs manage memory coherence in fixed 64-byte cache lines",
                "Modifying variable A invalidates the cache line containing unrelated variable B",
                "Massive cache coherence bus traffic degrades multicore parallelism",
                "@jdk.internal.vm.annotation.Contended adds memory padding to isolate fields",
                "Used heavily in High-Performance queues (LMAX Disruptor, LongAdder cells)",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_064",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "Virtual Threads Pinning Hazard",
            question = "What is 'Thread Pinning' in Java 21 Virtual Threads, and how does synchronized keyword cause it?",
            shortAnswer = "In Java 21 (Project Loom), when a virtual thread encounters blocking I/O (e.g. socket read), the JVM unmounts its continuation from the carrier OS thread. Thread Pinning occurs when a virtual thread cannot be unmounted: it is 'pinned' to its underlying carrier OS thread during blocking operations. This happens when blocking operations occur inside: 1) A synchronized block or method, or 2) A native C/JNI call. While pinned, the carrier OS thread is frozen, negating the throughput benefits of virtual threads. Fix: replace synchronized blocks with java.util.concurrent.locks.ReentrantLock.",
            keyPoints = listOf(
                "Pinning locks the virtual thread to its underlying OS carrier thread",
                "Occurs inside synchronized blocks/methods or native JNI call frames",
                "Blocking I/O while pinned blocks the platform thread, exhausting carrier pool",
                "Replace synchronized with ReentrantLock for virtual-thread compatibility",
                "Diagnose via JVM flag: -Djdk.tracePinnedThreads=full",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_065",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Built-in Functional Interfaces Hierarchy",
            question = "Explain the 4 fundamental built-in Functional Interfaces in java.util.function: Predicate, Function, Consumer, and Supplier. What are their method signatures?",
            shortAnswer = "1) Predicate<T>: boolean test(T t) - takes an argument of type T and returns a boolean (used for filtering). 2) Function<T, R>: R apply(T t) - takes an argument of type T and transforms/returns a result of type R (used for mapping). 3) Consumer<T>: void accept(T t) - takes an argument of type T and performs side-effects returning void (used for forEach). 4) Supplier<T>: T get() - takes no arguments and returns a value of type T (used for lazy generation/factories).",
            keyPoints = listOf(
                "Predicate<T>: boolean test(T t) - condition evaluation",
                "Function<T, R>: R apply(T t) - transformation and mapping",
                "Consumer<T>: void accept(T t) - consumes value with side-effects",
                "Supplier<T>: T get() - produces value without taking input",
                "Specialized primitive variants (IntPredicate, LongFunction, etc.) avoid autoboxing",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_066",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Stream API Collectors: groupingBy and partitioningBy",
            question = "How do Collectors.groupingBy and Collectors.partitioningBy differ in the Java Stream API? Give code examples.",
            shortAnswer = "Collectors.groupingBy groups stream elements by a classification function into a Map<K, List<T>> (or custom downstream collections), where K can be any type. Collectors.partitioningBy is a specialized form of grouping that classifies elements using a Predicate<T>, always returning a Map<Boolean, List<T>> with exactly two entries (true and false). partitioningBy is faster and guaranteed to contain both boolean keys even if one partition is empty.",
            keyPoints = listOf(
                "groupingBy accepts Function<T, K> and produces Map<K, List<T>>",
                "partitioningBy accepts Predicate<T> and produces Map<Boolean, List<T>>",
                "Downstream collectors (mapping, counting, toSet) can be chained with groupingBy",
                "partitioningBy is optimized for binary true/false classifications",
                "groupingByConcurrent uses concurrent maps for thread-safe parallel stream grouping",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_067",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Optional Best Practices & Anti-Patterns",
            question = "What is the design intent of java.util.Optional? What are the common anti-patterns to avoid?",
            shortAnswer = "Optional was designed strictly as a method return type to represent the potential absence of a value without returning null, forcing callers to explicitly consider the empty case. Anti-patterns: 1) Using Optional.get() without checking isPresent() (reintroduces NoSuchElementException). 2) Using Optional as method parameters, class fields, or collection elements (causes overhead and is not Serializable). 3) Calling orElse(compute()) instead of orElseGet(Supplier): orElse evaluates the fallback expression eagerly on every call, whereas orElseGet evaluates lazily only on empty.",
            keyPoints = listOf(
                "Designed strictly as a method return type to clearly communicate potential absence",
                "Never use Optional as a method parameter or class field (not Serializable)",
                "Avoid calling .get() directly; use map(), flatMap(), ifPresent(), or orElseThrow()",
                "orElse(expensiveCall()) executes eagerly; use orElseGet(() -> expensiveCall())",
                "Avoid wrapping Collections in Optional (return empty collections instead)",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_068",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Records in Java 16+: Canonical vs Compact Constructors",
            question = "What are Java Records (JEP 395)? How do compact constructors differ from canonical constructors?",
            shortAnswer = "Records are immutable data carriers that eliminate boilerplate. The compiler automatically synthesizes: private final fields for each component, public accessor methods matching component names (not getX()), a canonical constructor, equals(), hashCode(), and toString(). A canonical constructor explicitly lists all components. A Compact Constructor omits parameter declarations (e.g. 'public Point { ... }') and is executed before fields are assigned, making it the ideal idiomatic location for argument validation and normalization without duplicating assignments.",
            keyPoints = listOf(
                "Transparent, immutable data carriers; cannot extend other classes (extends Record)",
                "Compiler generates accessors (field()), equals, hashCode, and toString automatically",
                "Compact constructor (public Person { ... }) simplifies validation without 'this.x = x'",
                "Records are implicitly final and cannot be abstract",
                "Can implement interfaces and define static methods/fields",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_069",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Sealed Classes and Interfaces in Java 17",
            question = "What are Sealed Classes in Java 17 (JEP 409)? How do they enable Algebraic Data Types and exhaustive pattern matching?",
            shortAnswer = "Sealed classes and interfaces restrict which other classes or interfaces may extend or implement them using the 'permits' clause (e.g. 'public sealed class Shape permits Circle, Square'). Permitted subclasses must be in the same package/module and must explicitly declare whether they are final, sealed, or non-sealed. This models Algebraic Data Types (sum types) and enables exhaustive pattern matching in switch statements: the compiler knows all possible subtypes, eliminating the need for a fallback 'default' branch.",
            keyPoints = listOf(
                "Restricts inheritance hierarchy strictly to permitted types via 'permits' clause",
                "Subclasses must be explicitly declared as: final, sealed, or non-sealed",
                "Enables compiler-verified exhaustive switch statements without a 'default' case",
                "Subclasses must reside in the same package (or named module)",
                "Provides domain-driven modeling for finite state machines and algebraic types",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_070",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Pattern Matching for instanceof and switch in Java 21",
            question = "How does Pattern Matching in Java 21 modernize type checking and cast operations in switch statements?",
            shortAnswer = "Pattern Matching eliminates boilerplate 'instanceof + cast' sequences. 'instanceof Pattern' (Java 16) tests the type and binds it to a typed variable in a single step (if (obj instanceof String s) { s.length(); }). In Java 21, Pattern Matching for switch allows switching directly on object types with type patterns, guarded patterns ('when' clauses), and record deconstruction patterns (case Point(int x, int y) -> ...), completely replacing clumsy cascading if-else-if blocks.",
            keyPoints = listOf(
                "Combines type test, casting, and variable binding into a single expression",
                "Switch expressions support type patterns: case String s -> s.toLowerCase()",
                "Guarded patterns use the 'when' keyword: case String s when s.length() > 5",
                "Record patterns enable automatic destructuring of record components in case statements",
                "Null handling is supported directly: case null -> ...",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_071",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Sequenced Collections in Java 21",
            question = "What problem did Java 21's Sequenced Collections (JEP 431) solve across List, Deque, and SortedSet?",
            shortAnswer = "Prior to Java 21, Java collections lacked a unified interface for collections with a defined encounter order: getting the first element was list.get(0), deque.getFirst(), set.iterator().next(); getting the last element was list.get(list.size()-1) or deque.getLast(); and reversing order required disparate utilities. SequencedCollection, SequencedSet, and SequencedMap introduce uniform methods: getFirst(), getLast(), addFirst(), addLast(), removeFirst(), removeLast(), and reversed(), unifying order semantics across the entire collections hierarchy.",
            keyPoints = listOf(
                "Unifies collection hierarchy under new SequencedCollection and SequencedMap interfaces",
                "Uniform methods: addFirst(), addLast(), getFirst(), getLast(), removeFirst(), removeLast()",
                "reversed() returns a reverse-ordered view of the collection in O(1) time",
                "LinkedHashSet, TreeSet, List, and Deque now implement SequencedCollection",
                "Eliminates awkward list.get(list.size() - 1) index calculations",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_072",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Parallel Streams and the Common ForkJoinPool",
            question = "When should you use parallel streams, and what danger exists when parallel streams execute blocking I/O operations?",
            shortAnswer = "Parallel streams (stream.parallel()) partition data using Spliterators and process chunks concurrently across CPU cores using the shared ForkJoinPool.commonPool(). They are effective for CPU-intensive computations on massive in-memory datasets with independent, stateless operations. Danger: because ForkJoinPool.commonPool() is shared JVM-wide, if a parallel stream executes blocking I/O operations (HTTP calls, DB queries), it starves the common pool worker threads, freezing all parallel streams and CompletableFutures across the entire application. Use custom thread pools for blocking operations.",
            keyPoints = listOf(
                "Uses ForkJoinPool.commonPool() shared across the entire JVM process",
                "Beneficial only for CPU-bound computations on large in-memory collections (N * Q > 10,000)",
                "Never execute blocking network or database I/O in parallel streams",
                "Can cause thread starvation across unrelated components using commonPool",
                "Data source must split efficiently (ArrayList, arrays split well; LinkedList splits poorly)",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_073",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Constructor Chaining: this() and super()",
            question = "How does constructor chaining work in Java using this() and super()? What happens if neither is called?",
            shortAnswer = "Constructor chaining is the process of calling one constructor from another within the same class (using this()) or from the parent class (using super()). If a constructor does not explicitly invoke this() or super(), the Java compiler automatically inserts a no-argument super() call as the very first statement. If the superclass lacks a no-argument constructor, compilation fails with a constructor undefined error.",
            keyPoints = listOf(
                "this() delegates to an overloaded constructor in the same class",
                "super() invokes a superclass constructor",
                "Either this() or super() must be the very first statement in a constructor",
                "Compiler inserts default super() if neither is specified",
                "Cannot call both this() and super() in the same constructor",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_074",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Why Java Omits Operator Overloading",
            question = "Why did Java's creators intentionally exclude operator overloading (unlike C++ and Python)?",
            shortAnswer = "Java excluded user-defined operator overloading to maintain language simplicity, readability, and predictability. In C++, overloading operators like + or * for complex matrices or pointer arithmetic often obscured what code actually did and created cryptic bugs. Java reserves the + operator solely for numeric addition and String concatenation, preferring explicit, readable method names (e.g. bigDecimal.add()).",
            keyPoints = listOf(
                "Prioritizes code readability and maintenance across large enterprise teams",
                "Eliminates cryptic bugs and non-obvious hidden method invocations",
                "Only + is overloaded internally by the language for String concatenation",
                "BigDecimal and BigInteger use clear explicit method names (.add(), .multiply())",
                "Keeps the compiler and type system clean and straightforward",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_075",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Type Casting: Upcasting vs Downcasting",
            question = "Differentiate between Upcasting and Downcasting in Java. What causes ClassCastException?",
            shortAnswer = "Upcasting is casting a subclass reference to a superclass type (e.g. Animal a = new Dog()); it is safe, implicit, and widening. Downcasting is casting a superclass reference back down to a specific subclass type (e.g. Dog d = (Dog) a); it must be explicit. ClassCastException occurs at runtime when downcasting an object to a subclass that the actual heap instance does not belong to (e.g. casting a Cat instance to Dog). Always verify using 'instanceof' before downcasting.",
            keyPoints = listOf(
                "Upcasting is implicit, safe, and widening (subclass to superclass)",
                "Downcasting is explicit, narrowing, and carries runtime risk (superclass to subclass)",
                "ClassCastException thrown when actual heap type does not match target downcast type",
                "Guarded using 'if (a instanceof Dog d)' pattern matching",
                "Upcasting enables polymorphic collections (List<Animal> storing Dogs and Cats)",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_076",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "High Cohesion vs Loose Coupling",
            question = "What is the difference between High Cohesion and Loose Coupling in Java software architecture?",
            shortAnswer = "High Cohesion means a single class or module has a focused, well-defined responsibility; all its methods and fields relate directly to that single objective (Single Responsibility Principle). Loose Coupling means modules interact through well-defined abstractions/interfaces with minimal dependency on each other's internal implementation details. Systems with high cohesion and loose coupling are modular, easy to test, and resilient to refactoring.",
            keyPoints = listOf(
                "High Cohesion: class does one thing well with focused responsibility",
                "Loose Coupling: classes interact via interfaces with minimal mutual knowledge",
                "High cohesion simplifies unit testing and code comprehension",
                "Loose coupling allows modifying or replacing a component without cascading breaks",
                "Dependency Injection and Interfaces are primary tools for achieving loose coupling",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_077",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Initialization Order in Java Classes",
            question = "What is the exact execution order of static blocks, instance initializer blocks, constructors, and parent class initializers?",
            shortAnswer = "The order is: 1) Parent class static blocks and static field initializers (in source code order). 2) Subclass static blocks and static field initializers. 3) Parent class instance initializer blocks and instance field initializers. 4) Parent class constructor. 5) Subclass instance initializer blocks and instance field initializers. 6) Subclass constructor. Static blocks execute only on first class loading; instance blocks and constructors execute on every new instance creation.",
            keyPoints = listOf(
                "Parent static initializers execute before child static initializers",
                "Static initializers run once upon first class access",
                "Parent instance blocks and constructors execute before child instance blocks",
                "Instance initializer blocks run immediately before the constructor body",
                "Field initialization assignments execute at the same time as instance blocks",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_078",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "String.join() and StringJoiner in Java 8+",
            question = "How do String.join() and StringJoiner simplify constructing delimited strings like CSVs in Java?",
            shortAnswer = "StringJoiner (Java 8) constructs sequences of characters separated by a delimiter, with optional prefix and suffix (e.g. '[a, b, c]'). It eliminates manual trailing delimiter stripping required by StringBuilder. String.join(delimiter, elements) is a convenience wrapper around StringJoiner that joins arrays, iterables, or varargs with a delimiter in a single clean line.",
            keyPoints = listOf(
                "StringJoiner accepts delimiter, prefix, and suffix in constructor",
                "Eliminates boilerplate loops that strip the trailing comma/delimiter",
                "String.join(',', list) provides clean one-line joining",
                "Collectors.joining(delimiter, prefix, suffix) integrates with Stream API",
                "Internally uses StringBuilder with pre-calculated capacities",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_079",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "Text Blocks in Java 15+ (JEP 378)",
            question = "What are Text Blocks (triple quotes \"\"\") in Java 15+? How does incidental whitespace stripping work?",
            shortAnswer = "Text Blocks (JEP 378) allow multi-line string literals without explicit escape sequences (like \\n or \\\"). A text block opens with triple quotes \"\"\" followed by a mandatory newline. The Java compiler automatically strips 'incidental whitespace' (the common leading indentation shared by every non-empty line) while preserving essential indentation. This makes embedding JSON, SQL queries, and HTML clean and legible in Java code.",
            keyPoints = listOf(
                "Multi-line string literal enclosed in triple double quotes (\"\"\")",
                "Opening quotes must be followed by a newline",
                "Compiler automatically removes incidental leading indentation",
                "Escapes like \\n and \\\" are optional inside text blocks",
                "Supports line continuation escape (\\) to keep long single-line strings formatted",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_080",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "String.formatted() vs String.format()",
            question = "What is the difference between String.format() and the modern String.formatted() method introduced in Java 15?",
            shortAnswer = "String.format(formatString, args...) is a static method that parses format specifiers (%s, %d). Java 15 introduced string.formatted(args...) as an instance method on String that operates directly on the template string. It is functionally identical to String.format() but is more ergonomic and readable, especially when chained with Text Blocks: \"\"\"SELECT * FROM %s\"\"\".formatted(tableName).",
            keyPoints = listOf(
                "String.formatted() is an instance method introduced in Java 15",
                "Functionally delegates directly to String.format(this, args)",
                "Pairs cleanly with multi-line Text Blocks without static wrapping",
                "Throws IllegalFormatException if specifiers do not match argument types",
                "Formatting carries parsing overhead; use simple concatenation for high-frequency loops",
            ),
            difficulty = "Beginner"
        ),
    )

    private fun part5(): List<InterviewQuestion> = listOf(
        InterviewQuestion(
            id = "iq_java_081",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "String.split() Regex Trailing Empty Gotchas",
            question = "What is the difference between string.split(regex) and string.split(regex, limit)? What happens to trailing empty strings?",
            shortAnswer = "By default, string.split(regex) delegates to split(regex, 0), which discards all trailing empty strings. For example, 'a,b,c,,'.split(',') produces an array of length 3 (['a', 'b', 'c']), silently dropping the trailing empty elements. To retain trailing empty elements, pass a negative limit: 'a,b,c,,'.split(',', -1) produces an array of length 5. Also note that split() parses the argument as a regular expression, so splitting by '.' or '|' requires escaping (\\\\. or \\\\|).",
            keyPoints = listOf(
                "Default split(regex) uses limit 0, silently discarding trailing empty tokens",
                "Passing limit -1 (split(regex, -1)) preserves all trailing empty strings",
                "Positive limit specifies maximum number of tokens returned",
                "First argument is a compiled regex; characters like . and | must be escaped",
                "For single-character delimiters, Pattern.compile(delim).split() is faster",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_082",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "parseInt() vs valueOf() in Integer",
            question = "What is the difference between Integer.parseInt(\"123\") and Integer.valueOf(\"123\")?",
            shortAnswer = "Integer.parseInt(\"123\") parses the string and returns a primitive int. Integer.valueOf(\"123\") returns an Integer object wrapper and leverages the internal IntegerCache (reusing cached instances for values between -128 and 127). If a primitive int is needed, parseInt() is preferred because it avoids unnecessary autoboxing and object allocation.",
            keyPoints = listOf(
                "parseInt() returns primitive int",
                "valueOf() returns Integer object wrapper",
                "valueOf() utilizes IntegerCache for values between -128 and 127",
                "Both throw NumberFormatException on non-numeric input or overflow",
                "Use parseInt() for arithmetic primitives to eliminate heap allocation",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_083",
            trackId = "java_interview",
            conceptId = "java_strings",
            conceptName = "Strings & Memory Internals",
            title = "String.strip() vs String.trim() in Java 11",
            question = "Why did Java 11 introduce String.strip() to replace String.trim()? How do they handle Unicode whitespace?",
            shortAnswer = "String.trim() is a legacy method that only strips characters with ASCII code <= U+0020 (space), failing to strip Unicode whitespace characters (such as non-breaking space \\u00A0, em-space \\u2003, or zero-width space). String.strip() (Java 11) is Unicode-aware: it uses Character.isWhitespace() to detect and strip all Unicode whitespace definitions. Java 11 also added stripLeading() and stripTrailing().",
            keyPoints = listOf(
                "trim() only removes ASCII characters with code <= 32 (U+0020)",
                "strip() is Unicode-aware, using Character.isWhitespace(codePoint)",
                "Removes advanced Unicode whitespaces (em-space, ideographic space)",
                "stripLeading() and stripTrailing() provide directional stripping",
                "Prefer strip() in all modern Java applications",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_084",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Card Tables and Remembered Sets (RSet) in G1",
            question = "How do Remembered Sets (RSet) and Card Tables allow G1 GC to collect individual heap regions independently without full-heap scans?",
            shortAnswer = "G1 GC divides the heap into thousands of regions. To collect a Young or Mixed region without scanning the entire Old Generation to find pointers into this region, G1 uses Remembered Sets (RSet) and Card Tables. The heap is divided into 512-byte memory 'cards'. Whenever an Old Gen object writes a reference to a Young Gen object, a JIT-compiled write barrier marks the corresponding card as 'dirty' in the card table. During GC, G1 scans only the dirty cards in the RSet to discover cross-region references, enabling isolated region collection.",
            keyPoints = listOf(
                "Card Table maps 512-byte heap segments to 1-byte card entries",
                "Write barrier intercepts reference writes, marking cards 'dirty'",
                "Remembered Set (RSet) tracks which external regions contain references to this region",
                "Eliminates the need to scan the entire Old Generation during minor collections",
                "Enables G1's incremental and mixed region collection capabilities",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_085",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Survivor Spaces and Tenuring Threshold",
            question = "How do objects transition between Eden, Survivor S0/S1, and Tenured spaces? What is dynamic tenuring thresholding?",
            shortAnswer = "Objects are allocated in Eden. During Minor GC, alive objects from Eden and one Survivor space (e.g. FromSpace S0) are copied to the second Survivor space (ToSpace S1), and their age counter in the object header is incremented by 1. The roles of S0 and S1 alternate every collection. When an object's age reaches the tenuring threshold (-XX:MaxTenuringThreshold, default 15), it is promoted to Tenured (Old) Gen. If the total size of objects in Survivor space exceeds TargetSurvivorRatio (default 50%), the JVM dynamically calculates a lower age threshold to promote objects earlier, preventing Survivor overflow.",
            keyPoints = listOf(
                "New objects allocated in Eden; survived objects copied to S0/S1",
                "Object header age bits increment on each survived minor GC (max 15)",
                "Promoted to Old Gen upon reaching MaxTenuringThreshold",
                "Dynamic threshold: calculated if survivor space exceeds TargetSurvivorRatio (50%)",
                "Prevents copying long-lived objects repeatedly between survivor spaces",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_086",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Safepoints and Stop-the-World Pauses",
            question = "What is a JVM Safepoint? How do application threads bring themselves to a stop during GC pauses?",
            shortAnswer = "A Safepoint is a designated location in code where a thread's execution state is fully consistent, allowing the JVM to safely inspect registers, stack frames, and object references. The JVM places safepoint polling checks at method exits and loop backedges. During a Stop-the-World event (e.g. GC pause, deoptimization, thread dump), the JVM disarms the safepoint memory page. Next time each application thread reaches a safepoint poll instruction, it faults and parks itself, ensuring all threads pause safely.",
            keyPoints = listOf(
                "Consistent execution point where thread stacks can be safely read and mutated",
                "Inserted by JIT at method return instructions and loop backedges",
                "All threads must reach a safepoint for Stop-the-World operations",
                "Safepoint poll uses page protection faults or polling test instructions",
                "Long-running uncounted loops (int loops) can delay safepoints, causing latency spikes",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_087",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Compressed OOPs and 32GB Heap Boundary",
            question = "What are Compressed OOPs (Ordinary Object Pointers)? Why is a 31.9GB heap often faster than a 33GB heap in Java?",
            shortAnswer = "On 64-bit architectures, reference pointers require 8 bytes (64 bits), consuming 30% more memory and filling CPU caches faster. Compressed OOPs (-XX:+UseCompressedOops) allow the JVM to represent 64-bit heap addresses using 32-bit pointers. Because Java objects are aligned to 8-byte boundaries (3 lowest bits are always zero), the JVM shifts 32-bit pointers left by 3 bits at runtime to address up to 2^32 * 8 bytes = 32GB of heap. If heap size exceeds 32GB, compressed OOPs are disabled; references swell to 64 bits, inflating object sizes and degrading cache efficiency.",
            keyPoints = listOf(
                "Encodes 64-bit object references into 32 bits using 8-byte object alignment",
                "Effective up to 32GB heap size (2^32 * 8 bytes)",
                "Heaps between 32GB and 40GB frequently hold fewer objects than a 31GB heap",
                "Reduces memory footprint and increases CPU L1/L2 cache hit rates",
                "Enabled automatically on 64-bit JVMs when -Xmx < 32GB",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_088",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Direct Buffer Memory & Netty Off-Heap Allocations",
            question = "What is Direct Buffer Memory (ByteBuffer.allocateDirect()), and what causes java.lang.OutOfMemoryError: Direct buffer memory?",
            shortAnswer = "Direct ByteBuffers allocate memory outside the standard JVM heap directly from OS native memory (via malloc). This enables Zero-Copy network and disk I/O: the OS kernel can transfer data directly between network sockets and direct buffers without copying data into the JVM heap. An OutOfMemoryError: Direct buffer memory occurs when direct memory consumption exceeds the limit set by -XX:MaxDirectMemorySize (defaults to -Xmx). In frameworks like Netty, failing to release pooled ByteBuffers (ReferenceCounted.release()) leaks native memory.",
            keyPoints = listOf(
                "Allocates memory in native OS memory outside the garbage-collected Java heap",
                "Enables true Zero-Copy network and disk socket transfers",
                "Capped via JVM flag: -XX:MaxDirectMemorySize",
                "Reclaimed via Cleaners (PhantomReferences) when the Java ByteBuffer wrapper is GC-ed",
                "Netty reference counting (ReferenceCountUtil.release()) prevents direct memory leaks",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_089",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Why Set -Xms Equal to -Xmx in Production",
            question = "Why is it an enterprise best practice to set initial heap size (-Xms) equal to maximum heap size (-Xmx) in production servers?",
            shortAnswer = "Setting -Xms equal to -Xmx prevents dynamic heap resizing during production traffic. When -Xms is lower than -Xmx, the JVM starts with a small heap and must repeatedly pause to allocate additional physical memory pages from the OS and trigger Full GC cycles before expanding the heap. Pre-allocating the entire heap at startup eliminates resizing latency spikes, prevents memory fragmentation, and uncovers server memory shortfalls at boot time rather than during peak traffic.",
            keyPoints = listOf(
                "Eliminates runtime heap expansion latency spikes and memory allocation pauses",
                "Prevents expensive Stop-the-World Full GCs triggered solely to evaluate heap resizing",
                "Fails fast at boot time if the operating system lacks sufficient physical RAM",
                "Prevents OS page table fragmentation and virtual memory thrashing",
                "Standard configuration for microservices, Elasticsearch, and Cassandra nodes",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_090",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "TLAB (Thread-Local Allocation Buffer)",
            question = "What is a TLAB (Thread-Local Allocation Buffer) in the JVM, and how does it make new Object() synchronization-free?",
            shortAnswer = "In a multithreaded application, if every thread allocated objects directly into a shared Eden space, global synchronization (or atomic CAS operations) would be required on every 'new' keyword, causing massive lock contention. A TLAB is a dedicated buffer within Eden allocated exclusively to an individual thread. Each thread allocates objects inside its own private TLAB using simple, lock-free bump-the-pointer pointer incrementation (bump pointer). Only when a thread exhausts its TLAB does it acquire a synchronized lock to request a new TLAB chunk from Eden.",
            keyPoints = listOf(
                "Private buffer inside Eden space dedicated to a specific thread",
                "Enables lock-free bump-the-pointer object allocation in a few CPU cycles",
                "Completely eliminates heap synchronization contention during object creation",
                "Large objects exceeding TLAB size are allocated directly into shared Eden",
                "Enabled by default via -XX:+UseTLAB",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_091",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "ConcurrentSkipListMap vs ConcurrentHashMap",
            question = "When should you use ConcurrentSkipListMap instead of ConcurrentHashMap in concurrent Java applications?",
            shortAnswer = "ConcurrentHashMap is hash-based: it provides O(1) expected time for get/put but does not maintain any sorted order. ConcurrentSkipListMap is based on a Skip List data structure: it is a thread-safe, lock-free implementation of ConcurrentNavigableMap that keeps keys sorted in natural order (or custom Comparator). You must use ConcurrentSkipListMap when concurrent threads require sorted iteration, range queries (subMap(), headMap(), tailMap()), or ceiling/floor key searches in O(log n) time.",
            keyPoints = listOf(
                "ConcurrentHashMap: unordered O(1) lookups via hashing and bucket locks",
                "ConcurrentSkipListMap: sorted O(log n) lookups via probabilistic multi-level Skip Lists",
                "Supports concurrent range queries (subMap, tailMap) without blocking writers",
                "Lock-free implementation based on atomic CAS operations",
                "Ideal for concurrent time-series data indexing and leaderboards",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_092",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "EnumMap and EnumSet High Performance Internals",
            question = "Why are EnumSet and EnumMap dramatically faster and more memory-efficient than HashSet and HashMap?",
            shortAnswer = "Because all possible enum constants are known at compile time with dense sequential ordinal values: 1) EnumSet is internally implemented as a bit-vector (RegularEnumSet uses a single 64-bit long primitive for enums with <=64 elements). Set operations (contains, union, intersection) compile down to single-cycle bitwise CPU instructions (AND, OR, NOT), requiring zero object allocation. 2) EnumMap is backed by a simple contiguous Object[] array indexed directly by the enum's ordinal() (key.ordinal()), providing direct O(1) array access without hashing, hash collisions, or linked nodes.",
            keyPoints = listOf(
                "EnumSet backed by a single long bit-mask (for <=64 elements)",
                "Set operations execute in a single CPU cycle via bitwise bit manipulation",
                "EnumMap backed by a contiguous array indexed by enum.ordinal()",
                "Zero hash collisions, zero linked node wrappers, and minimal memory footprint",
                "Always use EnumMap/EnumSet when keys or elements belong to an enum type",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_093",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "Arrays.asList() vs List.of() vs new ArrayList()",
            question = "Compare Arrays.asList(), List.of() (Java 9), and new ArrayList<>(). What are their mutability and null acceptance rules?",
            shortAnswer = "1) Arrays.asList(arr): returns a fixed-size list backed directly by the original array. You can replace elements (set() works), but cannot add or remove elements (throws UnsupportedOperationException). Changes write through to the backing array. Permits null. 2) List.of() (Java 9): returns a truly immutable, compact list. Any modification (add, remove, set) throws UnsupportedOperationException. Rejects null (throws NullPointerException on creation/contains). 3) new ArrayList<>(): returns a standard, fully mutable list supporting addition, removal, and null elements.",
            keyPoints = listOf(
                "Arrays.asList(): fixed-size view of backing array; set() allowed, add/remove forbidden",
                "List.of(): completely immutable; set, add, remove all forbidden",
                "List.of(): disallows null elements strictly (throws NPE)",
                "new ArrayList<>(): fully mutable; supports add, remove, set, and null",
                "List.of() uses specialized compact arrayless classes for 0, 1, and 2 elements",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_094",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "SubList View and ConcurrentModificationException",
            question = "Why does modifying an original List cause its subList() view to throw ConcurrentModificationException?",
            shortAnswer = "list.subList(from, to) does not create a new independent list copy; it returns an internal View (SubList) that directly references the parent list and records the parent's modCount. If any structural modification (add or remove) is made directly to the parent list after the subList is created, the subList's recorded modCount becomes out of sync with the parent's modCount. Any subsequent read or write on the subList detects this discrepancy and immediately throws ConcurrentModificationException.",
            keyPoints = listOf(
                "subList() returns a live window/view over the backing parent list",
                "Modifications made through the subList update the parent list safely",
                "Direct modifications to the parent list invalidate the subList",
                "Throws ConcurrentModificationException on next subList access",
                "To create an independent copy, use new ArrayList<>(list.subList(from, to))",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_095",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "ArrayBlockingQueue vs LinkedBlockingQueue",
            question = "Compare ArrayBlockingQueue and LinkedBlockingQueue. Which provides higher throughput under concurrent producer-consumer loads?",
            shortAnswer = "ArrayBlockingQueue is backed by a fixed-size circular array and uses a single ReentrantLock shared by both producers (put) and consumers (take). LinkedBlockingQueue is optionally bounded, backed by linked nodes, and uses 'Two Independent Locks': putLock (for producers) and takeLock (for consumers). Because producers and consumers can operate concurrently without locking each other out, LinkedBlockingQueue typically achieves higher throughput under high concurrent traffic, though it allocates a Node object per element.",
            keyPoints = listOf(
                "ArrayBlockingQueue: bounded circular array; single lock for both put and take",
                "LinkedBlockingQueue: linked nodes; two independent locks (putLock and takeLock)",
                "Two locks allow simultaneous producing and consuming without lock contention",
                "ArrayBlockingQueue has superior CPU cache locality and zero node allocation overhead",
                "LinkedBlockingQueue without capacity is unbounded (Integer.MAX_VALUE), risking OOM",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_096",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "Heap Pollution and @SafeVarargs in Generics",
            question = "What is Heap Pollution in Java Generics? When is the @SafeVarargs annotation required?",
            shortAnswer = "Heap Pollution occurs when a variable of a parameterized generic type points to an object that is not of that generic type, usually caused by mixing raw types or combining generics with varargs (which creates a generic array under the hood). Because arrays are reified at runtime but generics are erased, generic varargs (e.g. void foo(List<String>... lists)) trigger compiler warnings. @SafeVarargs asserts that the developer's method body does not store unsafe types into the varargs array or leak the array reference, suppressing compiler warnings.",
            keyPoints = listOf(
                "Occurs when a parameterized reference refers to an object of a different runtime type",
                "Generic varargs create generic arrays, leading to potential ClassCastExceptions",
                "@SafeVarargs suppresses compiler unchecked warnings on varargs methods",
                "Can only be applied to static, final, or private methods and constructors",
                "Developer must ensure the method does not write to or expose the varargs array",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_097",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "Lock Escalation: Biased, Lightweight, Heavyweight",
            question = "Explain JVM Lock Escalation (Inflation): Biased Locking, Lightweight Locking, and Heavyweight Locking.",
            shortAnswer = "To minimize synchronization costs, the HotSpot JVM inflates locks progressively inside the object's Mark Word: 1) Biased Locking: if a lock is acquired by only one thread repeatedly, the JVM biases the lock to that thread ID; subsequent acquisitions require zero synchronization or CAS. 2) Lightweight Locking (Thin Lock): if another thread attempts acquisition without contention, the lock inflates to lightweight locking using a CAS loop to store a pointer to the thread's stack frame. 3) Heavyweight Locking: under actual contention (threads spin and fail CAS), the lock inflates to an OS Mutex (ObjectMonitor) and contending threads are suspended (BLOCKED).",
            keyPoints = listOf(
                "Locks start as Biased, inflate to Lightweight, then Heavyweight (cannot deflate easily)",
                "Biased locking records thread ID in Mark Word; zero CAS overhead for single thread",
                "Lightweight locking uses CAS to push displaced mark word to thread stack",
                "Contention triggers spin-locking before escalating to heavyweight OS mutex",
                "Heavyweight locking involves kernel context switches and thread parking",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_098",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "Thread.interrupt() vs isInterrupted() vs interrupted()",
            question = "How does thread interruption work in Java? Compare interrupt(), isInterrupted(), and static Thread.interrupted().",
            shortAnswer = "Interruption is a cooperative cancellation mechanism; it does NOT abruptly kill a thread. 1) thread.interrupt(): sets the interrupt status flag on the target thread. If the thread is blocked in sleep(), wait(), or join(), it wakes up and throws InterruptedException (clearing the flag). 2) thread.isInterrupted(): instance method that tests whether the thread has been interrupted, without clearing the flag. 3) Thread.interrupted(): static method that tests whether the current thread has been interrupted AND clears the interrupt status flag.",
            keyPoints = listOf(
                "Cooperative signaling; does not forcefully terminate running threads",
                "thread.interrupt() sets interrupt flag or interrupts sleep/wait with InterruptedException",
                "thread.isInterrupted() checks flag without resetting it",
                "Thread.interrupted() checks current thread flag AND resets it to false",
                "Never swallow InterruptedException; restore flag via Thread.currentThread().interrupt()",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_099",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "Daemon Threads and JVM Shutdown Lifecycle",
            question = "What is a Daemon Thread in Java? How do daemon threads interact with JVM termination?",
            shortAnswer = "A Daemon Thread (thread.setDaemon(true)) is a background service provider thread (e.g. Garbage Collector thread, timer threads). The JVM automatically terminates and exits as soon as all non-daemon (user) threads have finished executing, abruptly halting any remaining daemon threads. When the JVM exits because user threads completed, finally blocks inside running daemon threads are NOT executed, and stack frames are not unwound. Never run critical I/O or database writes in daemon threads.",
            keyPoints = listOf(
                "Background service thread created with thread.setDaemon(true) before start()",
                "JVM exits when all user (non-daemon) threads terminate",
                "Running daemon threads are terminated abruptly upon JVM shutdown",
                "finally blocks in daemon threads may not execute during JVM exit",
                "Never use daemon threads for file writing or transactional database updates",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_100",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "CAS and the ABA Problem (AtomicStampedReference)",
            question = "What is the ABA Problem in lock-free CAS (Compare-And-Swap) algorithms? How does AtomicStampedReference solve it?",
            shortAnswer = "The ABA Problem occurs when a thread reads memory location V as 'A', gets preempted, and meanwhile other threads change V from 'A' to 'B' and back to 'A'. When the original thread resumes, CAS sees V still contains 'A' and succeeds, even though the underlying state changed in between (hazardous in node-reusing data structures like lock-free stacks). AtomicStampedReference solves this by pairing the object reference with an integer version/stamp: CAS only succeeds if BOTH the expected reference and the expected integer stamp match simultaneously.",
            keyPoints = listOf(
                "CAS succeeds if value matches expected value, blind to intermediate state changes (A -> B -> A)",
                "Causes silent data corruption in lock-free linked list memory reuse algorithms",
                "AtomicStampedReference couples reference with integer stamp / version number",
                "compareAndSet(expectedRef, newRef, expectedStamp, newStamp) validates both",
                "AtomicMarkableReference provides boolean versioning for deletion marking",
            ),
            difficulty = "Staff"
        ),
    )

    private fun part6(): List<InterviewQuestion> = listOf(
        InterviewQuestion(
            id = "iq_java_101",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "ThreadPool Graceful Shutdown: shutdown vs shutdownNow",
            question = "How do you gracefully shut down an ExecutorService? Compare shutdown(), shutdownNow(), and awaitTermination().",
            shortAnswer = "shutdown() initiates an orderly shutdown: previously submitted tasks continue executing, but no new tasks are accepted. shutdownNow() attempts to halt all actively executing tasks by calling Thread.interrupt() on worker threads, drains the waiting task queue, and returns a List<Runnable> of unexecuted tasks. Production graceful shutdown idiom: call shutdown(), call awaitTermination(timeout, unit) to allow in-flight tasks to complete; if timeout expires, call shutdownNow() and awaitTermination() again to catch unresponsive tasks.",
            keyPoints = listOf(
                "shutdown(): stops accepting new tasks; drains and finishes queued tasks",
                "shutdownNow(): cancels queued tasks and sends interrupt() to running worker threads",
                "awaitTermination(timeout, unit) blocks until all tasks complete or timeout expires",
                "Standard two-phase shutdown idiom combines shutdown(), await(), and shutdownNow()",
                "Always shut down custom thread pools on application container teardown",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_102",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Four Kinds of Method References in Java 8",
            question = "What are the 4 distinct kinds of Method References in Java 8? Provide syntax examples for each.",
            shortAnswer = "The 4 kinds are: 1) Reference to a Static Method: ContainingClass::staticMethod (e.g. Math::max). 2) Reference to an Instance Method of a Particular Object: existingInstance::instanceMethod (e.g. System.out::println). 3) Reference to an Instance Method of an Arbitrary Object of a Particular Type: ContainingType::instanceMethod (e.g. String::toLowerCase, where the first lambda argument becomes the target object). 4) Reference to a Constructor: ClassName::new (e.g. ArrayList::new or Person::new).",
            keyPoints = listOf(
                "Static method: ClassName::staticMethodName",
                "Bound instance method: instanceRef::methodName",
                "Unbound instance method: ClassName::instanceMethodName (first arg is receiver)",
                "Constructor reference: ClassName::new",
                "Compact syntactic sugar for simple single-method lambda delegations",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_103",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Stream reduce() vs collect()",
            question = "What is the difference between reduce() and collect() in the Java Stream API? Why is reduce() inefficient for mutable containers?",
            shortAnswer = "reduce() is designed for functional reduction that folds stream elements into a single immutable value (e.g. sum, min, max, concatenation) by repeatedly creating a new value: (acc, elem) -> newAcc. collect() is designed for mutable reduction that accumulates elements into a single shared mutable container (e.g. ArrayList, StringBuilder, HashMap) without allocating new objects on each step: (container, elem) -> container.add(elem). Using reduce() to build collections causes catastrophic O(n^2) performance due to creating new collection copies on every element.",
            keyPoints = listOf(
                "reduce() folds elements into an immutable accumulator",
                "collect() accumulates elements into a mutable container (ArrayList, Set, Map)",
                "Using reduce() with mutable collections creates new collections repeatedly (O(n^2))",
                "collect() uses Collector<T, A, R> (supplier, accumulator, combiner, finisher)",
                "collect() operates efficiently in parallel streams using concurrent combiners",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_104",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Stream flatMap() Mechanics",
            question = "How does Stream.flatMap() work? How does it differ from map() when handling nested collections or Optionals?",
            shortAnswer = "map() applies a 1-to-1 transformation, wrapping the returned object into the stream (e.g. transforming a Stream<List<String>> with map yields Stream<List<String>>). flatMap() applies a 1-to-many transformation: each element is transformed into a Stream of elements, and all generated streams are flattened into a single combined output stream (e.g. transforming Stream<List<String>> with flatMap(List::stream) yields Stream<String>). It is used to unnest lists, split sentences into words, and unwrap nested Optionals.",
            keyPoints = listOf(
                "map() performs 1-to-1 transformation (Stream<T> -> Stream<R>)",
                "flatMap() performs 1-to-many transformation and flattens nested streams (Stream<T> -> Stream<R>)",
                "Replaces nested loops for processing collections of collections",
                "Essential for flattening Optional<Optional<T>> into Optional<T>",
                "Ensures stream elements are processed in encounter order",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_105",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Local Variable Type Inference (var) in Java 10",
            question = "What are the rules and constraints of 'var' in Java 10? Where is var forbidden?",
            shortAnswer = "'var' (JEP 286) enables compile-time type inference for local variables with initializers; it is NOT dynamic typing (the variable remains statically typed). Rules: 1) Allowed only for local variables inside methods, constructors, and for/try loops with an immediate initializer. 2) Forbidden for: class fields (instance or static), method return types, method parameter declarations, catch formal parameters, and uninitialized variables (var x; is illegal). 3) Cannot be initialized with null (cannot infer type).",
            keyPoints = listOf(
                "Compile-time static type inference; does not introduce dynamic typing",
                "Allowed strictly for local variables with immediate initializers",
                "Forbidden for class fields, method parameters, and method return types",
                "Cannot be initialized to null or used with lambda expressions directly",
                "Improves readability when variable type is redundantly repeated on the right side",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_106",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Structured Concurrency in Java 21 (JEP 453)",
            question = "What problem does Structured Concurrency solve in Java 21? How does StructuredTaskScope prevent thread leaks?",
            shortAnswer = "Unstructured concurrency (CompletableFuture, ExecutorService) treats tasks running in different threads as completely independent: if a subtask fails, parent and sibling tasks continue running orphaned, leaking threads and resources. Structured Concurrency treats multi-threaded subtasks as a single cohesive unit of work bound by syntactic block scope. Using StructuredTaskScope (ShutdownOnFailure / ShutdownOnSuccess), if any subtask fails, sibling subtasks are automatically cancelled via Thread.interrupt(), and the parent thread waits cleanly for all tasks to complete.",
            keyPoints = listOf(
                "Binds multi-threaded task lifecycles to structured syntactic code blocks",
                "Eliminates thread leaks, orphaned subtasks, and cancellation misses",
                "ShutdownOnFailure: cancels sibling subtasks as soon as any subtask throws an exception",
                "ShutdownOnSuccess: returns first successful result and cancels remaining subtasks",
                "Pairs seamlessly with Java 21 Virtual Threads for massively concurrent workflows",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_107",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "Checked vs Unchecked Exceptions Philosophy",
            question = "Explain the design philosophy behind Checked vs Unchecked exceptions in Java. When should each be used?",
            shortAnswer = "Checked exceptions (subclasses of Exception excluding RuntimeException) represent recoverable contingency conditions outside the program's control (e.g. FileNotFoundException, SQLException); the compiler forces callers to handle or declare them (try-catch or throws). Unchecked exceptions (subclasses of RuntimeException and Error) represent programming bugs, invalid arguments, or unrecoverable system failures (e.g. NullPointerException, IllegalArgumentException); they do not require explicit declaration. Modern Java frameworks favor Unchecked exceptions to reduce clutter.",
            keyPoints = listOf(
                "Checked: must be caught or declared in throws clause (compile-time enforced)",
                "Unchecked: subclasses of RuntimeException; no mandatory throws declaration",
                "Use Checked for recoverable business contingencies callers can reasonably fix",
                "Use Unchecked for programming logic errors, precondition violations, and unrecoverable errors",
                "Error represents fatal JVM conditions (OutOfMemoryError, StackOverflowError)",
            ),
            difficulty = "Beginner"
        ),
        InterviewQuestion(
            id = "iq_java_108",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "Try-With-Resources and Suppressed Exceptions",
            question = "How does try-with-resources handle exceptions thrown during AutoCloseable.close()? What are Suppressed Exceptions?",
            shortAnswer = "In try-with-resources, resources implementing AutoCloseable are closed automatically in reverse order of their declaration. If an exception is thrown in the try block AND another exception is thrown when close() executes, the exception from the try block is thrown to the caller (retaining primary cause), while the exception from close() is attached as a 'Suppressed Exception' accessible via throwable.getSuppressed(). In legacy try-finally, close() exceptions overwrote and hid the original try block exception.",
            keyPoints = listOf(
                "Resources closed in reverse order of declaration automatically",
                "Requires classes implementing java.lang.AutoCloseable (close() method)",
                "Original try block exception is preserved as the primary thrown exception",
                "Exceptions thrown during close() are attached via addSuppressed()",
                "Inspected via throwable.getSuppressed() array",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_109",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "Java NIO vs Classic Blocking I/O",
            question = "What is the architectural difference between Java I/O (java.io) and Java NIO (java.nio)? What is a Selector?",
            shortAnswer = "Classic java.io is stream-oriented and blocking: one thread reads byte-by-byte from an InputStream; if no data is available, the thread blocks, requiring 1 thread per client socket (unscalable for 10k connections). Java NIO is buffer-oriented and non-blocking: data is read from Channels into ByteBuffers. A Selector multiplexes multiple non-blocking Channels on a single thread using OS event loops (epoll/kqueue). One worker thread monitors thousands of network sockets for read/write readiness, enabling massive C10K concurrency (used by Netty).",
            keyPoints = listOf(
                "java.io: stream-oriented, synchronous, blocking (1 thread per socket)",
                "java.nio: buffer-oriented, channel-based, non-blocking multiplexing",
                "Selector monitors multiple Channels using OS epoll / kqueue primitives",
                "A single thread can handle thousands of concurrent client connections",
                "Underlies high-performance reactive networking engines (Netty, Undertow)",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_110",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "serialVersionUID and IncompatibleClassChangeError",
            question = "What is the purpose of serialVersionUID in Java Serialization? What happens if it is omitted?",
            shortAnswer = "serialVersionUID is a version hash used during object deserialization to verify that the sender and receiver of a serialized object have loaded compatible classes. If serialVersionUID is omitted, the JVM runtime calculates a default hash at runtime based on all class fields, methods, and interfaces. Any tiny modification (adding a field, changing a method signature) alters the calculated hash, causing deserialization to throw an InvalidClassException. Declaring explicit 'private static final long serialVersionUID' ensures backward compatibility.",
            keyPoints = listOf(
                "Version identifier verifying class compatibility during deserialization",
                "Omission forces runtime hash generation based on class structure",
                "Minor class edits change computed hash, triggering InvalidClassException",
                "Always declare explicit 'private static final long serialVersionUID = 1L;'",
                "transient fields are excluded from serialization and initialized to default values",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_111",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "Explain Type Erasure and Synthetic Bridge Methods",
            question = "What is Type Erasure in Java generics, and why does the compiler generate synthetic bridge methods?",
            shortAnswer = "Type erasure removes all generic type parameters at compile-time, replacing them with their raw bounds (usually Object) and inserting appropriate type casts to ensure backward compatibility with older JVM versions. Bridge methods are synthetic methods created by javac when a class inherits or overrides a parameterized method, ensuring polymorphic dispatch still works at the bytecode level.",
            keyPoints = listOf(
                "Compile-time removal of generic parameters for backward compatibility",
                "Replacement with unbounded (Object) or bounded type",
                "Bridge methods guarantee proper polymorphic invocation at runtime",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_112",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "PECS Principle: Producer Extends, Consumer Super",
            question = "What is the PECS rule in Java Generics, and when should you use <? extends T> versus <? super T>?",
            shortAnswer = "PECS stands for 'Producer Extends, Consumer Super'. Use <? extends T> when your generic collection only produces/reads elements of type T (covariance). Use <? super T> when the collection consumes/writes elements of type T (contravariance). If you do both read and write, use an exact invariant type T.",
            keyPoints = listOf(
                "Producer extends for reading (read-only covariance)",
                "Consumer super for writing (contravariance)",
                "Invariant T when both reading and writing are required",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_113",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "EnumSet and EnumMap Performance Internals",
            question = "Why are EnumSet and EnumMap significantly faster and more memory-efficient than HashSet and HashMap?",
            shortAnswer = "EnumSet is implemented internally using bit vectors (a single long in RegularEnumSet or a long array in JumboEnumSet), making operations extremely fast via bitwise operators with minimal memory footprint. EnumMap is backed by a compact array indexed directly by the enum ordinal, eliminating hashing, buckets, collision chains, and equals() invocations.",
            keyPoints = listOf(
                "EnumSet uses bitwise operations over primitive bit vectors",
                "EnumMap uses array indexing via ordinal() without hash calculation",
                "Zero bucket collisions and zero node wrapper overhead",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_114",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "PriorityQueue Implementation and Ordering Guarantees",
            question = "How does PriorityQueue work internally in Java, and what guarantees does it make about element ordering?",
            shortAnswer = "PriorityQueue is an unbounded priority queue based on a binary min-heap stored in a dynamic resizable array. It guarantees that the head (peek/poll) is always the smallest element according to natural or Comparator ordering in O(log n) time. It does NOT guarantee sorted traversal over an iterator, which yields elements in arbitrary heap order.",
            keyPoints = listOf(
                "Binary min-heap data structure backed by array",
                "O(log n) enqueue and dequeue operations",
                "Iterator does not provide elements in sorted order",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_115",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "ArrayBlockingQueue vs LinkedBlockingQueue",
            question = "Compare ArrayBlockingQueue and LinkedBlockingQueue in terms of memory, throughput, and lock architecture.",
            shortAnswer = "ArrayBlockingQueue is backed by a bounded array using a single ReentrantLock shared between put and take threads, which can limit concurrency under high contention. LinkedBlockingQueue is optionally bounded, allocates linked nodes per element causing more GC overhead, but employs two separate locks (putLock and takeLock), allowing concurrent insertion and consumption for higher throughput.",
            keyPoints = listOf(
                "Single lock vs dual locks (putLock and takeLock)",
                "Fixed pre-allocated array vs dynamically allocated linked nodes",
                "Throughput vs GC allocation pressure trade-offs",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_116",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "IdentityHashMap vs Standard HashMap",
            question = "What is IdentityHashMap, and how does its equality and hashing behavior differ from HashMap?",
            shortAnswer = "IdentityHashMap compares keys and values using reference equality (k1 == k2) rather than value equality (k.equals()), and computes hash codes using System.identityHashCode() rather than key.hashCode(). It uses open addressing with linear probing in a flat array rather than chained buckets, commonly used in object graph serialization and cycle detection.",
            keyPoints = listOf(
                "Reference equality (==) instead of equals()",
                "System.identityHashCode() instead of hashCode()",
                "Open addressing flat array structure",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_117",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "WeakHashMap and Garbage Collection Lifecycle",
            question = "How does WeakHashMap work, and when are its entries evicted?",
            shortAnswer = "WeakHashMap stores keys wrapped inside WeakReference objects associated with a ReferenceQueue. When a key is no longer strongly reachable elsewhere, GC clears the reference. On subsequent map operations (or table resize), the map polls its ReferenceQueue and discards the corresponding entry from the hash table.",
            keyPoints = listOf(
                "Keys stored as WeakReference instances",
                "ReferenceQueue tracking unreachable keys",
                "Lazy eviction of expired entries during subsequent map access",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_118",
            trackId = "java_interview",
            conceptId = "java_collections_hash",
            conceptName = "Collections, Generics & Hashing",
            title = "Sequenced Collections in Java 21",
            question = "What problem do Sequenced Collections solve in Java 21, and what are their core interfaces?",
            shortAnswer = "Before Java 21, Java lacked a unified hierarchy for collections with defined encounter orders (e.g. List, Deque, LinkedHashSet had inconsistent first/last access). Java 21 introduced SequencedCollection, SequencedSet, and SequencedMap, providing uniform methods like addFirst(), addLast(), getFirst(), getLast(), and reversed() views.",
            keyPoints = listOf(
                "Unified hierarchy for collections with explicit encounter orders",
                "Consistent first/last element access and mutation",
                "Standard reversed() view across lists, deques, and linked sets",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_119",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "JVM Safepoints and Stop-the-World Pauses",
            question = "What is a JVM Safepoint, and why are safepoints required for operations like Garbage Collection?",
            shortAnswer = "A Safepoint is a designated program state where all application threads are brought to a safe halt with known, consistent execution registers and stack roots. The JVM requires threads at safepoints to accurately traverse object references during GC root scanning, perform thread dumps, deoptimize code, or execute bias revocations without running into race conditions.",
            keyPoints = listOf(
                "Execution point where all thread state and roots are known and paused",
                "Required for accurate GC root scanning, thread dumps, and deoptimization",
                "Safepoint polling checks placed in loops and method returns",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_120",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Compressed OOPs (Ordinary Object Pointers)",
            question = "What are Compressed OOPs (-XX:+UseCompressedOops), and how do they optimize 64-bit JVM memory usage?",
            shortAnswer = "On 64-bit JVMs, pointers normally consume 8 bytes. Compressed OOPs represent 64-bit object references using 32-bit integers by exploiting 8-byte object alignment (the lowest 3 bits of an address are always 0). The JVM left-shifts the 32-bit address by 3 bits to recover the real memory address, allowing heaps up to 32GB to save substantial memory and CPU cache bandwidth.",
            keyPoints = listOf(
                "32-bit pointer representation on 64-bit architectures",
                "Exploits 8-byte boundary alignment via 3-bit left shift",
                "Effective for heap sizes up to 32GB",
            ),
            difficulty = "Mid-Level"
        ),
    )

    private fun part7(): List<InterviewQuestion> = listOf(
        InterviewQuestion(
            id = "iq_java_121",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Direct Memory vs Heap Memory in Java NIO",
            question = "What is the difference between Direct ByteBuffers and Heap ByteBuffers in Java NIO?",
            shortAnswer = "Heap ByteBuffers reside inside the JVM heap and are subject to GC management, requiring an intermediate copy to native OS memory during native I/O syscalls. Direct ByteBuffers allocate off-heap memory via Unsafe.allocateMemory() or malloc, enabling zero-copy OS kernel socket/channel transfers, but have higher allocation/deallocation overhead and escape GC monitoring.",
            keyPoints = listOf(
                "Heap buffers are inside JVM heap and subject to GC",
                "Direct buffers allocate off-heap for zero-copy OS native I/O",
                "Allocation cost of direct buffers is higher",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_122",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "JIT Method Inlining and Escape Analysis",
            question = "How do JIT Method Inlining and Escape Analysis improve Java application execution performance?",
            shortAnswer = "Method inlining replaces a method call instruction directly with the target method's bytecode body, eliminating call overhead and opening up further optimizations. Escape Analysis determines if an object's lifetime is confined to the declaring method; if it never escapes, the JIT can allocate the object on the stack or eliminate it entirely via scalar replacement.",
            keyPoints = listOf(
                "Inlining removes method call overhead and facilitates optimization",
                "Escape analysis verifies if object references escape the thread or stack frame",
                "Scalar replacement transforms object fields into CPU registers or stack primitives",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_123",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Metaspace vs PermGen Internals and Configuration",
            question = "How does Java 8+ Metaspace differ from Java 7 PermGen, and how is out-of-memory handled in Metaspace?",
            shortAnswer = "PermGen was part of the contiguous JVM heap with fixed max size (-XX:MaxPermSize), frequently throwing java.lang.OutOfMemoryError: PermGen space on dynamic class loading. Metaspace resides in native process memory, growing dynamically up to available OS memory unless bounded by -XX:MaxMetaspaceSize. Class metadata GC occurs only when Metaspace usage hits its High Watermark.",
            keyPoints = listOf(
                "Metaspace resides in native memory vs contiguous heap for PermGen",
                "Dynamic auto-sizing up to available OS memory",
                "Boundable via -XX:MaxMetaspaceSize with GC triggered at high-water marks",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_124",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "ZGC and Shenandoah Ultra-Low Latency Collectors",
            question = "How do modern collectors like ZGC and Shenandoah achieve sub-millisecond GC pause times?",
            shortAnswer = "ZGC and Shenandoah perform nearly all GC phases concurrently with application threads, including marking, evacuation (relocation), and reference updating. ZGC uses colored pointers and load barriers to intercept reference reads and heal relocated pointers on the fly, keeping stop-the-world pauses consistently below 1 millisecond regardless of heap size.",
            keyPoints = listOf(
                "Concurrent evacuation and compaction while threads run",
                "Load barriers intercept reference reads to redirect or heal moved objects",
                "Sub-millisecond pause times independent of heap size",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_125",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Diagnosing Java Memory Leaks and Heap Dumps",
            question = "What systematic steps and tools would you use to diagnose an OutOfMemoryError in production?",
            shortAnswer = "First, configure JVM flags -XX:+HeapDumpOnOutOfMemoryError and -XX:HeapDumpPath to automatically capture memory state at the crash moment. Analyze the .hprof dump using Eclipse Memory Analyzer (MAT) or VisualVM, identifying the Dominator Tree, leak suspects, and GC roots holding onto dead instances. Inspect thread dumps to correlate leaking objects with active tasks.",
            keyPoints = listOf(
                "Automatic heap dump on OOM flag configuration",
                "Dominator tree analysis and GC root path inspection via Eclipse MAT",
                "Distinguishing retained heap vs shallow heap sizes",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_126",
            trackId = "java_interview",
            conceptId = "java_jvm_memory",
            conceptName = "JVM Architecture & Memory Management",
            title = "Classloader Hierarchy and Delegation Model",
            question = "Explain the Java ClassLoader delegation model and how parent-delegation prevents namespace conflicts.",
            shortAnswer = "Java uses a hierarchical delegation model: Bootstrap ClassLoader (JVM core), Platform/Extension ClassLoader, and Application/System ClassLoader. When a class loader is requested to load a class, it delegates first to its parent; only if the parent fails (ClassNotFoundException) does the child attempt to load it. This prevents user code from spoofing trusted core classes like java.lang.Object.",
            keyPoints = listOf(
                "Bootstrap, Platform/Extension, and Application classloader tiers",
                "Parent-first delegation before self-loading",
                "Security guarantees preserving trusted core Java runtime namespace",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_127",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "StampedLock vs ReadWriteLock",
            question = "Why was StampedLock introduced in Java 8, and how does optimistic reading improve read throughput?",
            shortAnswer = "ReentrantReadWriteLock suffers from write starvation and overhead because even read locks mutate internal state via CAS. StampedLock introduces optimistic reads via tryOptimisticRead() which returns a stamp without acquiring an actual lock. The thread reads the state and then calls validate(stamp); if no write occurred, no locking overhead was incurred, drastically boosting read throughput.",
            keyPoints = listOf(
                "Optimistic read does not acquire a lock or mutate state",
                "Validation stamp check detects intervening writes",
                "Not reentrant and lacks Condition variable support",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_128",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "ThreadLocal Memory Leaks in Application Servers",
            question = "Why can ThreadLocal cause silent memory leaks in thread pools, and how do you prevent them?",
            shortAnswer = "In web servers and thread pools, worker threads never terminate. ThreadLocal stores values in a ThreadLocalMap held directly by the Thread instance. While the keys are WeakReferences, the values are strong references. If ThreadLocal.remove() is not called in a finally block, the value remains pinned in memory for the life of the pooled thread, causing severe memory leaks.",
            keyPoints = listOf(
                "Thread instances in pools live indefinitely",
                "Keys are WeakReferences but values are strongly referenced in ThreadLocalMap",
                "Always invoke remove() in a try-finally block",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_129",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "LongAdder vs AtomicLong under High Contention",
            question = "Why does LongAdder outperform AtomicLong in high-throughput concurrent counters?",
            shortAnswer = "AtomicLong uses a single memory location with CAS loops (compareAndSet). Under intense thread contention, multiple threads repeatedly fail their CAS attempts and spin, degrading CPU efficiency. LongAdder maintains an array of cell counters, hashing contending threads to separate cells and summing them only when sum() is called, eliminating contention bottlenecks.",
            keyPoints = listOf(
                "AtomicLong suffers from CAS retry thrashing under contention",
                "LongAdder distributes updates across an internal Cell array",
                "Final sum aggregates cell values when read",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_130",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "ABA Problem and AtomicStampedReference",
            question = "What is the ABA problem in lock-free concurrency, and how does AtomicStampedReference resolve it?",
            shortAnswer = "The ABA problem occurs when thread 1 reads value A, thread 2 changes it to B and then back to A, and thread 1 executes a CAS expecting no change occurred. If pointer state or underlying resources were recycled, this causes subtle corruption. AtomicStampedReference pairs an integer version stamp with the reference, updating both atomically so CAS fails if the stamp changed.",
            keyPoints = listOf(
                "Value changes from A to B and back to A unnoticed by basic CAS",
                "AtomicStampedReference pairs reference with an integer version/stamp",
                "CAS validates both expected reference and expected stamp",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_131",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "Lock Biasing, Thin Locks, and Heavyweight Inflation",
            question = "How does the HotSpot JVM optimize synchronized locks from biased to heavyweight?",
            shortAnswer = "The JVM optimizes synchronized blocks progressively: 1) Biased Lock: biased towards the first thread without atomic instructions (deprecated in recent JDKs). 2) Lightweight (Thin) Lock: acquired via a CAS pointer into the thread's stack frame when uncontended. 3) Heavyweight Lock: if contention occurs, the lock inflates into an OS mutex/monitor, parking waiting threads.",
            keyPoints = listOf(
                "Biased locking for single-thread lock reuse",
                "Lightweight CAS stack-displaced lock records",
                "Inflation to OS mutex and thread parking under contention",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_132",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "ForkJoinPool and Work-Stealing Algorithm",
            question = "How does ForkJoinPool's work-stealing algorithm work, and how does it prevent thread starvation?",
            shortAnswer = "ForkJoinPool maintains a double-ended queue (deque) for every worker thread. A worker pushes subtasks resulting from fork() to the head of its own deque and pops from the head in LIFO order for cache locality. When a worker runs out of work, it steals tasks from the tail (FIFO) of other busy workers' deques, balancing thread loads without central lock contention.",
            keyPoints = listOf(
                "Dedicated deque per worker thread",
                "LIFO execution of own tasks for CPU cache locality",
                "Stealing from the tail (FIFO) of other workers' deques to balance load",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_133",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "Graceful Shutdown of ExecutorService",
            question = "What is the standard recommended pattern for gracefully shutting down an ExecutorService?",
            shortAnswer = "The recommended idiom is a two-phase shutdown: first call shutdown() to reject new tasks while letting running tasks complete. Await termination for a reasonable timeout using awaitTermination(). If the timeout expires, call shutdownNow() to interrupt running threads and cancel pending tasks, followed by a second awaitTermination() to confirm exit.",
            keyPoints = listOf(
                "shutdown() to reject new tasks without aborting active work",
                "awaitTermination() with reasonable timeout",
                "shutdownNow() to interrupt stalled threads if timeout expires",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_134",
            trackId = "java_interview",
            conceptId = "java_concurrency",
            conceptName = "Concurrency & Multithreading",
            title = "CompletableFuture Exception Handling: exceptionally vs handle",
            question = "Compare exceptionally(), handle(), and whenComplete() in CompletableFuture error recovery.",
            shortAnswer = "exceptionally() only triggers when an exception occurs, allowing you to return a fallback default value. handle() always executes regardless of success or failure, receiving both result and throwable, allowing you to transform either into a new value. whenComplete() executes bi-consumer callback for side-effects or logging without altering the pipeline result.",
            keyPoints = listOf(
                "exceptionally() catches errors and provides fallback values",
                "handle() processes (result, error) and can transform the return type",
                "whenComplete() performs side-effects without altering future outcome",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_135",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Method Reference Flavors and Types",
            question = "What are the four distinct types of Method References in Java, and how do they map to lambdas?",
            shortAnswer = "1) Static method reference: Class::staticMethod, mapping to (args) -> Class.staticMethod(args). 2) Bound instance method reference: instance::method, mapping to (args) -> instance.method(args). 3) Unbound instance method reference: Class::method, mapping to (instance, args) -> instance.method(args). 4) Constructor reference: Class::new, mapping to (args) -> new Class(args).",
            keyPoints = listOf(
                "Static method reference (Class::staticMethod)",
                "Bound instance reference (instance::method)",
                "Unbound instance reference (Class::instanceMethod)",
                "Constructor reference (Class::new)",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_136",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Stream reduce() vs collect() Mechanics",
            question = "What is the difference between Stream.reduce() and Stream.collect() in terms of mutability and performance?",
            shortAnswer = "reduce() is designed for immutable accumulation, repeatedly combining two values into a new value via an associative BinaryOperator (e.g. sum, min, max). collect() performs mutable reduction, accumulating elements into an existing mutable container (e.g., ArrayList, StringBuilder) via a Collector, avoiding the massive object allocation penalty of repeated reduction.",
            keyPoints = listOf(
                "reduce() produces new immutable values at each step",
                "collect() accumulates into a single mutable container",
                "collect() avoids quadratic allocation overhead for collections/strings",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_137",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Stream flatMap() vs map() Use Cases",
            question = "When should you use Stream.flatMap() instead of map()?",
            shortAnswer = "map() performs a one-to-one transformation, converting each element of type T into an element of type R, resulting in Stream<R>. flatMap() performs a one-to-many transformation, taking an element and mapping it to a Stream<R>, then flattening all individual streams into a single unified Stream<R>, ideal for unnesting lists, optional unwrapping, and word tokenization.",
            keyPoints = listOf(
                "map() is 1-to-1 transformation returning single object",
                "flatMap() is 1-to-N transformation flattening nested streams",
                "Unnesting hierarchical structures (e.g. List of Lists)",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_138",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Local-Variable Type Inference (var) Rules and Limits",
            question = "What are the restrictions and best practices when using 'var' for local-variable type inference in Java 10+?",
            shortAnswer = "'var' can only be used for initialized local variables inside method bodies, for-loop counters, and try-with-resources statements. It CANNOT be used for class fields, method parameter types, method return types, uninitialized variables, or null literals. Best practice is to use it when the type is obvious from the right-hand side (e.g. var list = new ArrayList<String>()).",
            keyPoints = listOf(
                "Restricted to initialized local variables inside methods",
                "Disallowed on fields, method parameters, and return types",
                "Preserves strong static typing at compile-time",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_139",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Pattern Matching for instanceof and Switch",
            question = "How does pattern matching for instanceof and switch simplify code and improve type safety?",
            shortAnswer = "Pattern matching for instanceof combines type checking and cast extraction into a single statement (if (obj instanceof String s) { s.length(); }). In Java 21, pattern matching for switch enables branching directly on types, guarded patterns with 'when' clauses, exhaustive enum/sealed class verification, and automatic record deconstruction without verbose casting.",
            keyPoints = listOf(
                "Elimination of redundant type casts after instanceof",
                "Switch pattern matching on types with guarded 'when' clauses",
                "Compiler-enforced exhaustiveness checks on sealed types",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_140",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Record Patterns and Deconstruction in Java 21",
            question = "What are Record Patterns in Java 21, and how do they enable nested destructuring?",
            shortAnswer = "Record patterns deconstruct record instances directly into their individual component variables. In switch statements or instanceof checks, you can write 'case Point(int x, int y)' or nest patterns like 'case Order(Customer(String name, _), double total)', extracting inner fields in one step with type safety and null validation.",
            keyPoints = listOf(
                "Direct deconstruction of record components without accessor calls",
                "Support for nested pattern matching inside complex object graphs",
                "Type-safe compile-time structural matching",
            ),
            difficulty = "Mid-Level"
        ),
    )

    private fun part8(): List<InterviewQuestion> = listOf(
        InterviewQuestion(
            id = "iq_java_141",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Virtual Threads vs Reactive Programming",
            question = "How do Project Loom Virtual Threads change the trade-offs between imperative thread-per-request code and Reactive programming (WebFlux)?",
            shortAnswer = "Reactive programming was adopted to overcome OS thread scalability limits, but at the cost of complex asynchronous pipelines, fragmented stack traces, and difficult debugging. Virtual Threads provide the same extreme scalability (millions of concurrent tasks) using straightforward synchronous, blocking imperative code, making standard blocking APIs scalable without reactive complexity.",
            keyPoints = listOf(
                "Reactive frameworks solve OS thread starvation at high complexity cost",
                "Virtual threads allow writing blocking code with native-like throughput",
                "Simplifies debugging, tracing, and stack trace readability",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_142",
            trackId = "java_interview",
            conceptId = "java_modern_features",
            conceptName = "Modern Java (8 to 21 Features)",
            title = "Scoped Values vs ThreadLocal in Java 21+",
            question = "Why are Scoped Values introduced to replace ThreadLocal in virtual thread applications?",
            shortAnswer = "ThreadLocal has mutability overhead, memory leak risks if uncleaned, and high memory replication cost when millions of virtual threads inherit values. Scoped Values allow immutable data to be shared safely within a bounded execution scope across child threads with zero leak potential and optimized JVM stack frame sharing.",
            keyPoints = listOf(
                "Immutable data sharing bounded to explicit execution lifetime",
                "Eliminates memory leak risks of unbound ThreadLocal instances",
                "Extremely lightweight and memory-efficient for millions of virtual threads",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_143",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "Suppressed Exceptions in Try-With-Resources",
            question = "What happens when both the try-block and the AutoCloseable.close() method throw exceptions?",
            shortAnswer = "In try-with-resources, the primary exception thrown by the try block is preserved and rethrown. Any secondary exceptions thrown while closing the AutoCloseable resources are suppressed and attached to the primary exception, accessible via Throwable.getSuppressed(). In classic try-finally, the finally block exception would overwrite the original error.",
            keyPoints = listOf(
                "Primary exception from try block is preserved",
                "Exceptions from close() methods are attached as suppressed",
                "Retrieved programmatically via getSuppressed() array",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_144",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "Checked vs Unchecked Exceptions Architecture",
            question = "When should you design an exception as checked versus unchecked in modern enterprise applications?",
            shortAnswer = "Use checked exceptions (extending Exception) for anticipated, recoverable business scenarios that a caller is explicitly expected to handle (e.g., InsufficientFundsException). Use unchecked exceptions (extending RuntimeException) for programming errors, unrecoverable system faults, or infrastructure errors (e.g., DatabaseConnectionTimeoutException) to avoid method signature pollution.",
            keyPoints = listOf(
                "Checked for recoverable conditions expected in normal flow",
                "Unchecked for runtime faults and unrecoverable bugs",
                "Modern framework best practice leans towards unchecked with centralized handlers",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_145",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "Java NIO Selector Multiplexing Architecture",
            question = "How does Java NIO's Selector enable non-blocking I/O multiplexing across thousands of connections?",
            shortAnswer = "A Selector allows a single thread to monitor multiple selectable Channel objects (like SocketChannel) by registering interest in events (OP_READ, OP_WRITE, OP_ACCEPT). The OS kernel (using epoll on Linux, kqueue on macOS, IOCP on Windows) notifies the selector when I/O is ready, allowing one thread to handle thousands of concurrent client connections without thread-per-client overhead.",
            keyPoints = listOf(
                "Single thread multiplexes multiple non-blocking channels",
                "Registers interest in I/O events (read, write, accept, connect)",
                "Delegates to OS kernel event mechanisms (epoll/kqueue)",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_146",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "Byte Streams vs Character Streams in Java I/O",
            question = "Explain the architectural difference between InputStream/OutputStream and Reader/Writer.",
            shortAnswer = "InputStream and OutputStream are byte-oriented streams (8-bit bytes) used for raw binary data such as images, audio, network payloads, or compiled classes. Reader and Writer are character-oriented streams (16-bit Unicode chars) that automatically handle character encoding/decoding (such as UTF-8) via Charset encoders, preventing text corruption.",
            keyPoints = listOf(
                "Byte streams (InputStream/OutputStream) process raw 8-bit binary data",
                "Character streams (Reader/Writer) process 16-bit Unicode characters",
                "Charset translation prevents character encoding corruption",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_147",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "Memory-Mapped Files via FileChannel.map()",
            question = "What are memory-mapped files in Java NIO, and what are their performance advantages?",
            shortAnswer = "FileChannel.map() creates a MappedByteBuffer that maps a region of a disk file directly into the application virtual memory address space. The OS kernel's virtual memory page cache manages reading and flushing pages directly between disk and memory, bypassing JVM heap user-space buffer copies for blazing fast high-volume I/O operations.",
            keyPoints = listOf(
                "Maps disk file directly into virtual address space",
                "Bypasses user-space buffer copies via OS page cache",
                "Ideal for ultra-fast reading/writing of large binary files",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_148",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "Java Serialization Vulnerabilities and serialVersionUID",
            question = "What is the purpose of serialVersionUID, and why is standard Java serialization considered a security risk?",
            shortAnswer = "serialVersionUID ensures version compatibility between the serialized bytecode and the deserializing class definition. Java serialization is inherently dangerous because readObject() instantiates arbitrary class graphs from untrusted byte streams before field validation, enabling gadget-chain Remote Code Execution (RCE) attacks. Modern architectures prefer JSON, Protocol Buffers, or record serialization.",
            keyPoints = listOf(
                "serialVersionUID validates version compatibility during deserialization",
                "readObject() creates arbitrary objects leading to gadget chain RCE vulnerabilities",
                "Industry consensus favors JSON, Protobuf, or secure object serialization filters",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_149",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "Transient Keyword and Custom Serialization",
            question = "How does the transient keyword work, and how can writeObject and readObject be customized?",
            shortAnswer = "The transient keyword marks a field to be omitted from standard serialization, useful for sensitive data (passwords) or non-serializable resources (file handles). Classes can override private writeObject(ObjectOutputStream) and readObject(ObjectInputStream) to perform defaultWriteObject() while adding custom encryption, decryption, or field validation.",
            keyPoints = listOf(
                "transient excludes fields from default byte stream serialization",
                "Useful for security-sensitive or non-serializable references",
                "Custom readObject/writeObject enables encryption and validation",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_150",
            trackId = "java_interview",
            conceptId = "java_exceptions_io",
            conceptName = "Exception Handling & I/O Systems",
            title = "Files.walk() vs Files.list() and Resource Management",
            question = "Why must you close the Stream returned by Files.walk() or Files.lines()?",
            shortAnswer = "Unlike in-memory collection streams, the Streams returned by Files.walk(), Files.list(), and Files.lines() hold open underlying OS file descriptors and directory iterators. Because Streams implement AutoCloseable, failing to enclose them in a try-with-resources statement leads to operating system file handle leaks and subsequent 'Too many open files' errors.",
            keyPoints = listOf(
                "NIO stream holds an open OS file descriptor or directory stream",
                "Must be wrapped in try-with-resources to trigger close()",
                "Prevents OS file handle exhaustion",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_151",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Double-Checked Locking Singleton Pattern",
            question = "Why is the volatile keyword mandatory in the Double-Checked Locking Singleton pattern in Java?",
            shortAnswer = "Object construction in Java involves three steps: allocating memory, invoking the constructor, and assigning the reference to the variable. Without the volatile keyword, the JIT compiler or CPU can reorder steps 2 and 3, causing another thread to observe a non-null but partially initialized object. volatile introduces a happens-before memory barrier preventing instruction reordering.",
            keyPoints = listOf(
                "Three-step instantiation: allocate, initialize, assign reference",
                "Instruction reordering can publish partially constructed instance",
                "volatile establishes happens-before barrier preventing reordering",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_152",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Bill Pugh Singleton Design Pattern",
            question = "How does the Bill Pugh Singleton implementation achieve thread-safe lazy initialization without synchronization?",
            shortAnswer = "The Bill Pugh pattern uses an inner static helper class to hold the singleton instance. The inner class is not loaded into memory when the outer class is loaded; it is loaded only when getInstance() is called, triggering JVM class loading. The JVM guarantees that class initialization (<clinit>) is strictly thread-safe, providing lazy loading with zero synchronization overhead.",
            keyPoints = listOf(
                "Inner static helper class is loaded lazily on first access",
                "JVM guarantees thread-safe class initialization (<clinit>)",
                "Zero locking or synchronization overhead on subsequent reads",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_153",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Java Dynamic Proxies vs CGLIB Bytecode Generation",
            question = "What is the difference between JDK Dynamic Proxies and CGLIB (or ByteBuddy) proxies?",
            shortAnswer = "JDK Dynamic Proxies (java.lang.reflect.Proxy) work only on interfaces, generating proxy classes at runtime that implement the target interfaces via InvocationHandler. CGLIB or ByteBuddy generate subclasses of the target class at the bytecode level, allowing proxies for concrete classes without interfaces, but cannot proxy final classes or final methods.",
            keyPoints = listOf(
                "JDK dynamic proxy requires target to implement interfaces",
                "CGLIB/ByteBuddy subclasses target concrete class via bytecode generation",
                "Cannot proxy final classes or final methods with subclassing",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_154",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Reflection API Overhead and Java 9 Module Encapsulation",
            question = "What is the performance overhead of Java Reflection, and how does the Java Platform Module System (JPMS) restrict it?",
            shortAnswer = "Reflection incurs performance overhead due to dynamic method lookup, argument boxing/unboxing, security checks, and inability of JIT to inline calls. In Java 9+, JPMS enforces strong encapsulation: packages inside modules are not accessible via reflection from external modules unless explicitly declared with 'opens' or 'exports', rendering setAccessible(true) invalid across strict module boundaries.",
            keyPoints = listOf(
                "Runtime lookup, argument boxing, and prevention of JIT inlining",
                "JPMS enforces strong encapsulation across module boundaries",
                "Requires explicit 'opens' directive in module-info to permit deep reflection",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_155",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Deep Copy vs Shallow Copy in Java",
            question = "Explain the difference between Shallow Copy and Deep Copy, and how to safely implement a Deep Copy.",
            shortAnswer = "A shallow copy duplicates the object's top-level structure, but nested object references still point to the original memory instances, meaning modifying child fields affects both copies. A deep copy recursively duplicates all nested objects, creating completely independent object graphs. Deep copying can be implemented via copy constructors, cloning libraries, or serialization frameworks.",
            keyPoints = listOf(
                "Shallow copy shares references to nested objects",
                "Deep copy creates independent replicas of all referenced objects",
                "Implemented via copy constructors, deep clone libraries, or serialization",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_156",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Fail-Fast vs Fail-Safe Iterators",
            question = "What is the difference between Fail-Fast and Fail-Safe iterators in Java collections?",
            shortAnswer = "Fail-fast iterators (e.g., ArrayList, HashMap) track a modCount counter and immediately throw ConcurrentModificationException if the collection is structurally modified during iteration. Fail-safe (or weakly consistent) iterators (e.g., CopyOnWriteArrayList, ConcurrentHashMap) operate on a snapshot or tolerate concurrent updates without throwing exceptions.",
            keyPoints = listOf(
                "Fail-fast detects structural modification via modCount and throws exception",
                "Fail-safe operates on snapshot or weakly consistent iterator state",
                "Concurrent collections avoid ConcurrentModificationException",
            ),
            difficulty = "Staff"
        ),
        InterviewQuestion(
            id = "iq_java_157",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Clean Architecture and Separation of Concerns in Java",
            question = "How do you apply Clean Architecture principles in enterprise Java backend services?",
            shortAnswer = "Clean Architecture structures the application in concentric layers: Domain Entities at the center (pure business logic, no framework dependencies), Use Cases / Application Services, Interface Adapters (controllers, repositories), and Frameworks & Drivers (Spring, database drivers). Dependencies point strictly inward, ensuring business rules can be tested in isolation without starting a container or database.",
            keyPoints = listOf(
                "Domain entities isolated from external framework dependencies",
                "Strict inward dependency rule towards core business logic",
                "Use cases interact with persistence and external APIs through interfaces (ports and adapters)",
            ),
            difficulty = "Senior"
        ),
        InterviewQuestion(
            id = "iq_java_158",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Immutable Objects: Thread Safety and Construction Rules",
            question = "What are the essential requirements to guarantee an object is truly immutable in Java?",
            shortAnswer = "1) Declare the class final or use private constructors so it cannot be subclassed. 2) Make all fields private and final. 3) Do not provide any setter or mutator methods. 4) If fields reference mutable objects, perform defensive copying in constructors and return defensive copies in getter methods. 5) Ensure the 'this' reference does not escape during construction.",
            keyPoints = listOf(
                "Final class or closed hierarchy preventing subclass modification",
                "Private and final fields initialized in constructor",
                "Defensive copying for all mutable object fields",
                "No this-escape during construction",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_159",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Flyweight Design Pattern in Java APIs",
            question = "How is the Flyweight pattern implemented in core Java APIs like Integer and String?",
            shortAnswer = "The Flyweight pattern minimizes memory usage by sharing common immutable data among multiple objects. Java implements this via Integer.valueOf() caching values between -128 and 127 (IntegerCache), and the String Constant Pool where string literals are deduplicated and shared across the entire JVM runtime.",
            keyPoints = listOf(
                "Shares immutable state to minimize memory footprint",
                "Integer.valueOf() caching range (-128 to 127)",
                "JVM String constant pool sharing literal instances",
            ),
            difficulty = "Mid-Level"
        ),
        InterviewQuestion(
            id = "iq_java_160",
            trackId = "java_interview",
            conceptId = "java_oop_internals",
            conceptName = "Object-Oriented Programming & Architecture",
            title = "Template Method vs Strategy Pattern in Java",
            question = "Compare the Template Method pattern and Strategy pattern in terms of coupling and flexibility.",
            shortAnswer = "Template Method uses inheritance: an abstract base class defines the skeleton of an algorithm in a final template method, and subclasses override specific primitive steps (tight compile-time coupling). Strategy pattern uses composition: the algorithm is encapsulated in an interface, and interchangeable concrete implementations are injected at runtime, providing greater flexibility and easier unit testing.",
            keyPoints = listOf(
                "Template Method relies on class inheritance and abstract hook methods",
                "Strategy relies on object composition and interface polymorphism",
                "Strategy allows dynamic algorithm swapping at runtime and loose coupling",
            ),
            difficulty = "Senior"
        ),
    )

    fun getQuestions(): List<InterviewQuestion> =
        part1() + part2() + part3() + part4() + part5() + part6() + part7() + part8()
}
