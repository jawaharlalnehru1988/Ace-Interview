package com.example.data.local.questions

import com.example.data.local.entity.QuestionEntity
import com.example.data.local.questions.QuestionHelper.q

object SqlBeginnerQuestions {

    fun getAll(): List<QuestionEntity> {
        return part1() + part2() + part3() + part4()
    }

    private fun part1(): List<QuestionEntity> = listOf(
        q(
            id = "sql_beg_001",
            categoryId = "sql",
            title = "Relational Model: Primary Key Definition",
            prompt = "What are the two mandatory properties of a Primary Key constraint in a relational database?",
            opt0 = "It must be an auto-incrementing integer and contain letters",
            opt1 = "It must uniquely identify every row in the table, and it can NEVER contain NULL values",
            opt2 = "It must be encrypted and have a foreign key attached",
            opt3 = "It must be updated every time a row is modified",
            correctIndex = 1,
            explanation = "A primary key uniquely identifies each record in a table. By definition of the relational model and SQL standards, primary keys enforce uniqueness and strictly disallow NULL values.",
            difficulty = "Beginner",
            tags = "SQL,Fundamentals,PrimaryKey,Constraints"
        ),
        q(
            id = "sql_beg_002",
            categoryId = "sql",
            title = "Foreign Key and Referential Integrity",
            prompt = "What is the primary role of a Foreign Key constraint in a relational database?",
            opt0 = "To translate table names into foreign languages",
            opt1 = "To enforce referential integrity by ensuring that values in the referencing column must match an existing primary key or unique key in the referenced parent table, or be NULL",
            opt2 = "To speed up SELECT queries by 100x",
            opt3 = "To compress data stored on disk",
            correctIndex = 1,
            explanation = "Foreign keys preserve referential integrity: they prevent orphaned child records by verifying that referenced parent rows actually exist in the related table.",
            difficulty = "Beginner",
            tags = "SQL,Fundamentals,ForeignKey,ReferentialIntegrity"
        ),
        q(
            id = "sql_beg_003",
            categoryId = "sql",
            title = "SQL Sub-Languages: DDL vs DML",
            prompt = "Which SQL command category do CREATE, ALTER, and DROP belong to, versus INSERT, UPDATE, and DELETE?",
            opt0 = "CREATE is DML; INSERT is DDL",
            opt1 = "CREATE, ALTER, and DROP are DDL (Data Definition Language); INSERT, UPDATE, and DELETE are DML (Data Manipulation Language)",
            opt2 = "Both categories are DCL (Data Control Language)",
            opt3 = "Both categories are TCL (Transaction Control Language)",
            correctIndex = 1,
            explanation = "DDL defines and modifies database schema structures (tables, views, indexes). DML manipulates the actual data rows stored inside those structures.",
            difficulty = "Beginner",
            tags = "SQL,Fundamentals,DDL,DML"
        ),
        q(
            id = "sql_beg_004",
            categoryId = "sql",
            title = "SQL Sub-Languages: DCL and TCL",
            prompt = "What do GRANT and REVOKE represent, versus COMMIT and ROLLBACK?",
            opt0 = "GRANT/REVOKE are DDL; COMMIT/ROLLBACK are DML",
            opt1 = "GRANT and REVOKE are DCL (Data Control Language) for permissions; COMMIT, ROLLBACK, and SAVEPOINT are TCL (Transaction Control Language) for transaction management",
            opt2 = "All four commands are DQL (Data Query Language)",
            opt3 = "GRANT is TCL; ROLLBACK is DCL",
            correctIndex = 1,
            explanation = "DCL commands (GRANT, REVOKE) manage user security permissions. TCL commands (COMMIT, ROLLBACK, SAVEPOINT) control transactional boundaries.",
            difficulty = "Beginner",
            tags = "SQL,Fundamentals,DCL,TCL"
        ),
        q(
            id = "sql_beg_005",
            categoryId = "sql",
            title = "Natural Key vs Surrogate Key",
            prompt = "What is the difference between a Natural Key and a Surrogate Key?",
            opt0 = "Natural keys are numbers; Surrogate keys are strings",
            opt1 = "A Natural Key is an attribute with real-world business meaning (e.g. SSN, VIN, Email); a Surrogate Key is an artificially generated unique identifier with zero business meaning (e.g. auto-increment ID or UUID)",
            opt2 = "Surrogate keys are stored in memory; Natural keys are on disk",
            opt3 = "Natural keys cannot be primary keys",
            correctIndex = 1,
            explanation = "Natural keys carry business meaning and can change if external business rules change. Surrogate keys (IDs, UUIDs) are artificial and immutable, providing stable primary keys.",
            difficulty = "Beginner",
            tags = "SQL,DataModeling,NaturalKey,SurrogateKey"
        ),
        q(
            id = "sql_beg_006",
            categoryId = "sql",
            title = "Composite Primary Key Definition",
            prompt = "What is a Composite Primary Key in relational table design?",
            opt0 = "A primary key made of compressed text",
            opt1 = "A primary key that consists of two or more columns combined together to uniquely identify each row in a table (e.g. order_id and item_id in order_items)",
            opt2 = "A primary key that links to two different databases",
            opt3 = "A primary key that changes every month",
            correctIndex = 1,
            explanation = "When no single column is unique on its own, multiple columns are combined (e.g. student_id + course_id in an enrollment junction table) to form a composite primary key.",
            difficulty = "Beginner",
            tags = "SQL,Fundamentals,CompositeKey,DataModeling"
        ),
        q(
            id = "sql_beg_007",
            categoryId = "sql",
            title = "Foreign Key Action: ON DELETE CASCADE",
            prompt = "What occurs when a parent row is deleted from the `customers` table if the `orders` table defines `FOREIGN KEY (customer_id) REFERENCES customers(id) ON DELETE CASCADE`?",
            opt0 = "The delete statement throws a foreign key constraint violation error",
            opt1 = "All related orders belonging to that deleted customer in the child `orders` table are automatically deleted",
            opt2 = "The `customer_id` column in `orders` is set to NULL",
            opt3 = "The database stops accepting new orders",
            correctIndex = 1,
            explanation = "`ON DELETE CASCADE` automatically purges dependent child records when the parent record is deleted, preventing orphan rows.",
            difficulty = "Beginner",
            tags = "SQL,ForeignKey,Cascade,ReferentialIntegrity"
        ),
        q(
            id = "sql_beg_008",
            categoryId = "sql",
            title = "Foreign Key Action: ON DELETE SET NULL vs RESTRICT",
            prompt = "How does `ON DELETE RESTRICT` behave differently from `ON DELETE SET NULL` when attempting to delete a referenced parent row?",
            opt0 = "RESTRICT sets the column to zero; SET NULL deletes the row",
            opt1 = "RESTRICT rejects and aborts the parent row deletion with an error if any child rows reference it; SET NULL allows the parent deletion and updates the foreign key in child rows to NULL",
            opt2 = "There is no difference in relational databases",
            opt3 = "RESTRICT deletes child rows; SET NULL does nothing",
            correctIndex = 1,
            explanation = "`RESTRICT` (or `NO ACTION`) blocks deletion of the parent if child rows exist. `SET NULL` permits the deletion but sets the foreign key column in the child rows to NULL (requiring the column to be nullable).",
            difficulty = "Beginner",
            tags = "SQL,ForeignKey,Restrict,SetNull"
        ),
        q(
            id = "sql_beg_009",
            categoryId = "sql",
            title = "CHAR vs VARCHAR Data Types",
            prompt = "What is the key storage difference between `CHAR(10)` and `VARCHAR(10)`?",
            opt0 = "CHAR stores numbers; VARCHAR stores letters",
            opt1 = "CHAR(10) is fixed-length: storing 'cat' pads it with 7 spaces to always consume 10 characters of storage; VARCHAR(10) is variable-length: storing 'cat' consumes only 3 characters plus a 1-byte length prefix",
            opt2 = "VARCHAR is limited to 10 bytes; CHAR can hold 4GB",
            opt3 = "CHAR is deprecated in modern SQL",
            correctIndex = 1,
            explanation = "CHAR is fixed-length (ideal for fixed codes like ISO currency codes 'USD' or country codes 'US'). VARCHAR dynamically allocates only the length of the string plus 1-2 length bytes.",
            difficulty = "Beginner",
            tags = "SQL,DataTypes,CHAR,VARCHAR"
        ),
        q(
            id = "sql_beg_010",
            categoryId = "sql",
            title = "DECIMAL / NUMERIC vs FLOAT for Monetary Values",
            prompt = "Why MUST financial currency amounts (e.g. bank balances, invoice totals) be stored using `DECIMAL(12, 2)` or `NUMERIC` rather than `FLOAT` or `REAL`?",
            opt0 = "DECIMAL values can only be positive",
            opt1 = "FLOAT uses IEEE 754 binary floating-point representation which introduces binary rounding inaccuracies (e.g. 0.1 + 0.2 = 0.30000000000000004); DECIMAL uses exact base-10 fixed-point arithmetic with zero rounding error",
            opt2 = "FLOAT takes up 100x more disk space than DECIMAL",
            opt3 = "SQL databases forbid mathematical operations on FLOAT",
            correctIndex = 1,
            explanation = "Binary floats cannot represent base-10 fractional decimals like 0.1 or 0.01 accurately. In financial accounting, this causes pennies to appear or disappear. Fixed-point DECIMAL is mathematically exact.",
            difficulty = "Beginner",
            tags = "SQL,DataTypes,DECIMAL,FLOAT,Fintech"
        ),
        q(
            id = "sql_beg_011",
            categoryId = "sql",
            title = "DATE vs DATETIME vs TIMESTAMP",
            prompt = "What is the primary difference between `DATETIME` and `TIMESTAMP` in databases like MySQL and PostgreSQL?",
            opt0 = "DATE stores time; DATETIME stores years",
            opt1 = "DATETIME stores an absolute date and time as-is with no timezone conversion; TIMESTAMP is converted to UTC on storage and converted back to the connection's active timezone on retrieval",
            opt2 = "TIMESTAMP can only store years up to 2000",
            opt3 = "DATETIME requires a 64-bit CPU",
            correctIndex = 1,
            explanation = "TIMESTAMP is timezone-aware (converts to UTC internally). DATETIME represents a calendar date and clock time that does not adjust with server or client timezone offsets.",
            difficulty = "Beginner",
            tags = "SQL,DataTypes,DATETIME,TIMESTAMP,Timezones"
        ),
        q(
            id = "sql_beg_012",
            categoryId = "sql",
            title = "SELECT DISTINCT Behavior",
            prompt = "What does `SELECT DISTINCT country, city FROM customers;` return?",
            opt0 = "Only rows where country is unique, ignoring city",
            opt1 = "Unique combinations of country AND city (duplicate pairs of identical country and city are eliminated)",
            opt2 = "All rows sorted in alphabetical order",
            opt3 = "Only rows with city names starting with the letter D",
            correctIndex = 1,
            explanation = "DISTINCT applies across all columns in the SELECT projection list. It eliminates rows that have duplicate values across the combination of all projected columns.",
            difficulty = "Beginner",
            tags = "SQL,DQL,SELECT,DISTINCT"
        ),
        q(
            id = "sql_beg_013",
            categoryId = "sql",
            title = "Column Aliasing with AS Keyword",
            prompt = "What is the purpose of the `AS` keyword in `SELECT salary * 12 AS annual_salary FROM employees;`?",
            opt0 = "To save the calculation permanently into a new database table",
            opt1 = "To provide a temporary, descriptive alias name for the computed result column in the query output",
            opt2 = "To sort the salary column in ascending order",
            opt3 = "To convert the number into an ASCII string",
            correctIndex = 1,
            explanation = "The AS keyword creates a temporary column or table alias, making query output readable and allowing callers to reference calculated expressions cleanly.",
            difficulty = "Beginner",
            tags = "SQL,DQL,Aliasing,Syntax"
        ),
        q(
            id = "sql_beg_014",
            categoryId = "sql",
            title = "WHERE Clause: IN vs Multiple OR Conditions",
            prompt = "Which query is syntactically equivalent to: `WHERE department_id = 10 OR department_id = 20 OR department_id = 30`?",
            opt0 = "WHERE department_id BETWEEN 10 AND 30",
            opt1 = "WHERE department_id IN (10, 20, 30)",
            opt2 = "WHERE department_id LIKE '%10%20%30%'",
            opt3 = "WHERE department_id = (10, 20, 30)",
            correctIndex = 1,
            explanation = "The `IN (val1, val2, ...)` operator is shorthand for multiple `OR` equality comparisons on the same column, improving readability.",
            difficulty = "Beginner",
            tags = "SQL,DQL,WHERE,INOperator"
        ),
        q(
            id = "sql_beg_015",
            categoryId = "sql",
            title = "WHERE Clause: BETWEEN ... AND Operator",
            prompt = "Is the `BETWEEN ... AND ...` operator inclusive or exclusive in SQL (e.g. `WHERE age BETWEEN 18 AND 25`)?",
            opt0 = "It is strictly exclusive (19 through 24 only)",
            opt1 = "It is inclusive of both boundary endpoints (equivalent to `age >= 18 AND age <= 25`)",
            opt2 = "It includes 18 but excludes 25",
            opt3 = "It includes 25 but excludes 18",
            correctIndex = 1,
            explanation = "In SQL, `BETWEEN a AND b` is inclusive on both ends: it evaluates as `val >= a AND val <= b`.",
            difficulty = "Beginner",
            tags = "SQL,DQL,BETWEEN,Operators"
        ),
        q(
            id = "sql_beg_016",
            categoryId = "sql",
            title = "Pattern Matching: LIKE Wildcards '%' and '_'",
            prompt = "In SQL `LIKE` pattern matching, what do the `%` (percent) and `_` (underscore) wildcards represent?",
            opt0 = "% matches exactly 1 character; _ matches any string",
            opt1 = "% matches zero or more characters of any length; _ matches exactly one single character",
            opt2 = "% matches only numbers; _ matches only letters",
            opt3 = "% matches spaces; _ matches punctuation",
            correctIndex = 1,
            explanation = "In standard SQL: `%` matches any sequence of zero or more characters (e.g. 'J%' matches 'John', 'Jane', 'J'). `_` matches exactly one character (e.g. 'h_t' matches 'hat', 'hot', 'hit').",
            difficulty = "Beginner",
            tags = "SQL,DQL,LIKE,Wildcards"
        ),
        q(
            id = "sql_beg_017",
            categoryId = "sql",
            title = "Three-Valued Logic: Why `col = NULL` Always Fails",
            prompt = "Why does the query `SELECT * FROM users WHERE middle_name = NULL;` return zero rows, even when users have NULL middle names?",
            opt0 = "Because NULL is a reserved keyword in C++",
            opt1 = "In SQL's Three-Valued Logic (3VL), NULL represents unknown data; comparing anything to NULL with `=` evaluates to UNKNOWN (not TRUE); you must use `IS NULL` instead",
            opt2 = "Because the database automatically converts NULL to empty string",
            opt3 = "Because middle_name must be a primary key",
            correctIndex = 1,
            explanation = "NULL means 'unknown value'. Unknown cannot equal unknown. Therefore, `NULL = NULL` is UNKNOWN (falsy in a WHERE clause). You must use `IS NULL` or `IS NOT NULL`.",
            difficulty = "Beginner",
            tags = "SQL,NULL,ThreeValuedLogic,Operators"
        ),
        q(
            id = "sql_beg_018",
            categoryId = "sql",
            title = "Three-Valued Logic: NOT IN with NULL Hazard",
            prompt = "What fatal trap occurs when executing `SELECT * FROM employees WHERE department_id NOT IN (1, 2, NULL);`?",
            opt0 = "The query runs 10x faster",
            opt1 = "It returns zero rows (empty result set), because `NOT IN` expands to `department_id != 1 AND department_id != 2 AND department_id != NULL`; any comparison with NULL evaluates to UNKNOWN, making the entire AND expression evaluate to UNKNOWN/FALSE",
            opt2 = "It deletes all departments",
            opt3 = "It automatically replaces NULL with 0",
            correctIndex = 1,
            explanation = "If a subquery or list inside `NOT IN` contains a single NULL, the entire expression evaluates to UNKNOWN/FALSE for every row, returning zero rows. Use `NOT EXISTS` instead.",
            difficulty = "Beginner",
            tags = "SQL,NULL,NOTIN,Hazard,Pitfalls"
        ),
        q(
            id = "sql_beg_019",
            categoryId = "sql",
            title = "ORDER BY: Multi-Column Sorting",
            prompt = "How does `ORDER BY department_id ASC, salary DESC;` sort the result set?",
            opt0 = "It alternates between sorting by department and sorting by salary",
            opt1 = "It sorts first by department_id in ascending order; for rows with the identical department_id, it sorts by salary in descending order",
            opt2 = "It sorts by salary first because DESC has higher priority than ASC",
            opt3 = "It sorts only by salary, ignoring department_id",
            correctIndex = 1,
            explanation = "Multi-column sorting evaluates columns from left to right. Primary sort is department_id ASC; ties are broken by salary DESC.",
            difficulty = "Beginner",
            tags = "SQL,DQL,ORDERBY,Sorting"
        ),
        q(
            id = "sql_beg_020",
            categoryId = "sql",
            title = "ORDER BY: NULLS FIRST vs NULLS LAST",
            prompt = "In SQL standard and PostgreSQL/Oracle, where do NULL values appear by default when sorting with `ORDER BY col ASC` vs `ORDER BY col DESC`?",
            opt0 = "NULLs are excluded from the result set completely",
            opt1 = "In SQL standard: ASC places NULLs LAST; DESC places NULLs FIRST (explicitly configurable using `NULLS FIRST` or `NULLS LAST`)",
            opt2 = "NULLs always appear at the top regardless of direction",
            opt3 = "NULLs appear in random positions",
            correctIndex = 1,
            explanation = "Standard SQL treats NULLs as larger than any value. Thus ASC puts NULLs last; DESC puts them first. PostgreSQL and Oracle support explicit `NULLS FIRST` / `NULLS LAST` modifiers.",
            difficulty = "Beginner",
            tags = "SQL,ORDERBY,NULLS,Sorting"
        ),
        q(
            id = "sql_beg_021",
            categoryId = "sql",
            title = "Pagination: LIMIT and OFFSET",
            prompt = "What does `SELECT * FROM products ORDER BY id LIMIT 20 OFFSET 40;` accomplish in database pagination?",
            opt0 = "It returns products with IDs between 20 and 40",
            opt1 = "It skips the first 40 rows and returns the next 20 rows (Page 3 when displaying 20 items per page)",
            opt2 = "It limits query execution to 20 milliseconds",
            opt3 = "It returns 40 rows from page 20",
            correctIndex = 1,
            explanation = "OFFSET skips N rows; LIMIT takes M rows. OFFSET 40 LIMIT 20 returns items 41 through 60 (Page 3 of size 20).",
            difficulty = "Beginner",
            tags = "SQL,DQL,LIMIT,OFFSET,Pagination"
        ),
        q(
            id = "sql_beg_022",
            categoryId = "sql",
            title = "String Functions: CONCAT and COALESCE",
            prompt = "What does `SELECT CONCAT(first_name, ' ', last_name) FROM users;` do?",
            opt0 = "It compares first_name and last_name for equality",
            opt1 = "It joins the first_name, a space string, and the last_name together into a single combined string",
            opt2 = "It counts the number of characters in both names",
            opt3 = "It capitalizes the first letter of each name",
            correctIndex = 1,
            explanation = "CONCAT concatenates two or more string expressions together into a single string. (In SQLite and PostgreSQL, `first_name || ' ' || last_name` is also standard).",
            difficulty = "Beginner",
            tags = "SQL,StringFunctions,CONCAT"
        ),
        q(
            id = "sql_beg_023",
            categoryId = "sql",
            title = "Handling NULLs: COALESCE Function",
            prompt = "What does the `COALESCE(phone, email, 'No Contact Info')` function return?",
            opt0 = "A concatenated string of all 3 arguments",
            opt1 = "The first non-null expression from the argument list; if phone is present, returns phone; if phone is null but email exists, returns email; if both are null, returns 'No Contact Info'",
            opt2 = "It throws an error if any parameter is NULL",
            opt3 = "It returns the number of NULL arguments",
            correctIndex = 1,
            explanation = "COALESCE is an ANSI SQL standard function that returns the first non-null value from a list of arguments, evaluating from left to right.",
            difficulty = "Beginner",
            tags = "SQL,Functions,COALESCE,NULLHandling"
        ),
        q(
            id = "sql_beg_024",
            categoryId = "sql",
            title = "Date Extraction: EXTRACT Function",
            prompt = "How do you extract the birth year from a `birth_date` column in standard SQL?",
            opt0 = "SELECT YEAR(birth_date) -- non-standard MySQL only",
            opt1 = "SELECT EXTRACT(YEAR FROM birth_date) FROM users;",
            opt2 = "SELECT SUBSTRING(birth_date, 1, 4)",
            opt3 = "SELECT birth_date.year FROM users;",
            correctIndex = 1,
            explanation = "`EXTRACT(field FROM source)` is standard ANSI SQL for retrieving subfields (YEAR, MONTH, DAY, HOUR) from dates and timestamps.",
            difficulty = "Beginner",
            tags = "SQL,DateFunctions,EXTRACT"
        ),
        q(
            id = "sql_beg_025",
            categoryId = "sql",
            title = "Logical Operator Precedence: AND vs OR",
            prompt = "In the expression `WHERE status = 'Active' OR status = 'Pending' AND balance > 100`, which operator binds with higher precedence?",
            opt0 = "OR has higher precedence than AND",
            opt1 = "AND has higher precedence than OR; therefore `status = 'Pending' AND balance > 100` evaluates first, unless parentheses are used to override precedence",
            opt2 = "Operators are evaluated strictly from left to right with equal precedence",
            opt3 = "Precedence depends on the database manufacturer",
            correctIndex = 1,
            explanation = "In SQL (as in boolean logic), AND has higher precedence than OR. Always use parentheses `(status = 'Active' OR status = 'Pending') AND balance > 100` to prevent unintended logic bugs.",
            difficulty = "Beginner",
            tags = "SQL,DQL,Precedence,LogicalOperators"
        )
    )

    private fun part2(): List<QuestionEntity> = listOf(
        q(
            id = "sql_beg_026",
            categoryId = "sql",
            title = "Aggregate Functions: COUNT(*) vs COUNT(column)",
            prompt = "What is the crucial difference between `COUNT(*)` and `COUNT(commission_pct)`?",
            opt0 = "COUNT(*) is 100x slower than COUNT(column)",
            opt1 = "COUNT(*) counts all rows in the table/group including rows with NULLs; COUNT(commission_pct) counts only rows where commission_pct is NOT NULL",
            opt2 = "COUNT(column) counts distinct values automatically",
            opt3 = "There is no difference; they are exact aliases",
            correctIndex = 1,
            explanation = "COUNT(*) counts total tuples regardless of content. COUNT(column_name) ignores NULL values, counting only non-null occurrences.",
            difficulty = "Beginner",
            tags = "SQL,Aggregation,COUNT,NULLHandling"
        ),
        q(
            id = "sql_beg_027",
            categoryId = "sql",
            title = "Aggregate Functions: SUM and AVG with NULLs",
            prompt = "If a column `bonus` has values [100, 200, NULL], what does `AVG(bonus)` return?",
            opt0 = "100 (averaging across 3 rows as [100, 200, 0] / 3)",
            opt1 = "150 (it ignores NULL and computes (100 + 200) / 2 = 150)",
            opt2 = "NULL (any calculation with NULL yields NULL)",
            opt3 = "It throws a NullPointerException error",
            correctIndex = 1,
            explanation = "SQL aggregate functions (SUM, AVG, MIN, MAX) automatically ignore NULL values before computing the aggregate. (100 + 200) / 2 = 150.",
            difficulty = "Beginner",
            tags = "SQL,Aggregation,AVG,NULLHandling"
        ),
        q(
            id = "sql_beg_028",
            categoryId = "sql",
            title = "GROUP BY Clause: Rule of Grouping",
            prompt = "Why does the following query fail with an error in standard SQL? `SELECT department_id, employee_name, AVG(salary) FROM employees GROUP BY department_id;`",
            opt0 = "AVG cannot be used with GROUP BY",
            opt1 = "Every non-aggregated column in the SELECT list (like `employee_name`) MUST be included in the GROUP BY clause; otherwise the database does not know which employee name to return for each grouped department",
            opt2 = "GROUP BY requires at least 3 columns",
            opt3 = "Salary must be converted to an integer first",
            correctIndex = 1,
            explanation = "A grouped query produces 1 row per group. If department 10 has 50 employees with different names, returning a single unaggregated `employee_name` is ambiguous and illegal in standard SQL.",
            difficulty = "Beginner",
            tags = "SQL,Aggregation,GROUPBY,SyntaxRules"
        ),
        q(
            id = "sql_beg_029",
            categoryId = "sql",
            title = "HAVING vs WHERE Clause",
            prompt = "What is the primary difference between the `WHERE` clause and the `HAVING` clause?",
            opt0 = "WHERE is for numbers; HAVING is for strings",
            opt1 = "WHERE filters individual rows BEFORE grouping and aggregation occurs; HAVING filters aggregated groups AFTER `GROUP BY` has executed (and can filter on aggregate functions like `HAVING COUNT(*) > 5`)",
            opt2 = "HAVING is faster than WHERE",
            opt3 = "WHERE cannot be used if HAVING is present",
            correctIndex = 1,
            explanation = "WHERE filters input rows before aggregation. Aggregate functions like `COUNT(*)` or `SUM(salary)` cannot appear in a WHERE clause. Aggregates are filtered in HAVING.",
            difficulty = "Beginner",
            tags = "SQL,Aggregation,HAVING,WHERE"
        ),
        q(
            id = "sql_beg_030",
            categoryId = "sql",
            title = "Conditional Aggregation with CASE WHEN",
            prompt = "How can you count how many employees are in 'Engineering' and how many are in 'Sales' in a single query using Conditional Aggregation?",
            opt0 = "Run two separate queries and add them in Excel",
            opt1 = "SELECT COUNT(CASE WHEN dept = 'Engineering' THEN 1 END) AS eng_count, COUNT(CASE WHEN dept = 'Sales' THEN 1 END) AS sales_count FROM employees;",
            opt2 = "SELECT * FROM employees GROUP BY dept HAVING dept = 'Engineering' AND dept = 'Sales';",
            opt3 = "SELECT COUNT(*) FROM employees WHERE dept = 'Engineering' OR dept = 'Sales';",
            correctIndex = 1,
            explanation = "Inside `COUNT()`, when the condition is false, CASE returns NULL (which COUNT ignores). When true, it returns 1 (which COUNT tallies), enabling horizontal pivoting of counts.",
            difficulty = "Beginner",
            tags = "SQL,Aggregation,CASEWHEN,ConditionalAggregation"
        ),
        q(
            id = "sql_beg_031",
            categoryId = "sql",
            title = "CASE Expression: Simple vs Searched CASE",
            prompt = "What is the difference between a Simple CASE and a Searched CASE expression in SQL?",
            opt0 = "Simple CASE is in MySQL; Searched CASE is in PostgreSQL",
            opt1 = "Simple CASE tests an expression against discrete equality values (`CASE status WHEN 1 THEN 'A' WHEN 2 THEN 'B' END`); Searched CASE evaluates arbitrary boolean expressions (`CASE WHEN score >= 90 THEN 'A' WHEN score >= 80 THEN 'B' END`)",
            opt2 = "Searched CASE can only search text strings",
            opt3 = "Simple CASE cannot use ELSE",
            correctIndex = 1,
            explanation = "Simple CASE compares a single expression against fixed literals. Searched CASE provides maximum flexibility with full boolean expressions (`WHEN x > 10 AND y = 'Active' THEN ...`).",
            difficulty = "Beginner",
            tags = "SQL,Functions,CASEWHEN,Syntax"
        ),
        q(
            id = "sql_beg_032",
            categoryId = "sql",
            title = "INNER JOIN Definition",
            prompt = "What rows are returned by an `INNER JOIN` between `orders` and `customers` on `orders.customer_id = customers.id`?",
            opt0 = "All orders, plus customers who have never placed an order",
            opt1 = "Only rows that have matching keys in BOTH tables (orders with a valid customer, and customers who have placed an order)",
            opt2 = "All customers, regardless of whether they have orders",
            opt3 = "The cartesian cross product of all orders and customers",
            correctIndex = 1,
            explanation = "INNER JOIN returns only the intersecting rows where the join predicate evaluates to TRUE in both tables. Unmatched rows from either table are discarded.",
            difficulty = "Beginner",
            tags = "SQL,Joins,INNERJOIN"
        ),
        q(
            id = "sql_beg_033",
            categoryId = "sql",
            title = "LEFT (OUTER) JOIN Definition",
            prompt = "What does `SELECT * FROM customers c LEFT JOIN orders o ON c.id = o.customer_id;` return?",
            opt0 = "Only customers who placed orders",
            opt1 = "ALL rows from the left table (`customers`), along with matching rows from the right table (`orders`); if a customer has no orders, order columns are filled with NULLs",
            opt2 = "All orders, but only customers who live on the left side of town",
            opt3 = "Only orders without customers",
            correctIndex = 1,
            explanation = "LEFT JOIN guarantees that every record from the left table appears in the output at least once. If no match exists on the right, right-side columns populate as NULL.",
            difficulty = "Beginner",
            tags = "SQL,Joins,LEFTJOIN"
        ),
        q(
            id = "sql_beg_034",
            categoryId = "sql",
            title = "RIGHT (OUTER) JOIN vs LEFT JOIN",
            prompt = "Why do most engineering teams prefer using `LEFT JOIN` over `RIGHT JOIN` in codebases?",
            opt0 = "RIGHT JOIN is deprecated and will be removed from SQL",
            opt1 = "Any RIGHT JOIN can be rewritten as a LEFT JOIN simply by swapping table order; reading from left-to-right (preserving primary table first) improves query readability and mental modeling",
            opt2 = "RIGHT JOIN is 50% slower than LEFT JOIN in all database engines",
            opt3 = "RIGHT JOIN cannot use indexes",
            correctIndex = 1,
            explanation = "`A RIGHT JOIN B` is semantically identical to `B LEFT JOIN A`. Standardizing on LEFT JOIN keeps queries reading naturally from left to right.",
            difficulty = "Beginner",
            tags = "SQL,Joins,RIGHTJOIN,BestPractices"
        ),
        q(
            id = "sql_beg_035",
            categoryId = "sql",
            title = "FULL (OUTER) JOIN Definition",
            prompt = "What does a `FULL OUTER JOIN` return?",
            opt0 = "Only rows with matching keys in both tables",
            opt1 = "All rows from BOTH tables: matching rows are joined; unmatched rows from the left table have right columns as NULL; unmatched rows from the right table have left columns as NULL",
            opt2 = "A Cartesian product of all rows without checking keys",
            opt3 = "A list of table column names",
            correctIndex = 1,
            explanation = "FULL OUTER JOIN is the union of LEFT JOIN and RIGHT JOIN: no data is lost from either table. Unmatched records on either side are padded with NULLs.",
            difficulty = "Beginner",
            tags = "SQL,Joins,FULLJOIN"
        ),
        q(
            id = "sql_beg_036",
            categoryId = "sql",
            title = "CROSS JOIN (Cartesian Product)",
            prompt = "If table A has 10 rows and table B has 50 rows, how many rows are produced by `SELECT * FROM A CROSS JOIN B;`?",
            opt0 = "60 rows (10 + 50)",
            opt1 = "500 rows (10 * 50)",
            opt2 = "50 rows (maximum of the two)",
            opt3 = "10 rows (minimum of the two)",
            correctIndex = 1,
            explanation = "A CROSS JOIN produces a Cartesian product: every single row in Table A is paired with every single row in Table B ($10 \\times 50 = 500$ rows).",
            difficulty = "Beginner",
            tags = "SQL,Joins,CROSSJOIN,CartesianProduct"
        ),
        q(
            id = "sql_beg_037",
            categoryId = "sql",
            title = "Self Join Definition",
            prompt = "When is a Self Join used in relational database queries?",
            opt0 = "When a database connects to itself over the internet",
            opt1 = "When a table is joined with itself to model hierarchical or parent-child relationships within the same entity (e.g. matching an employee to their manager where both reside in the `employees` table)",
            opt2 = "When comparing two identical database backup copies",
            opt3 = "When a table has only one column",
            correctIndex = 1,
            explanation = "A Self Join joins a table to itself using two different table aliases (e.g. `FROM employees emp LEFT JOIN employees mgr ON emp.manager_id = mgr.id`).",
            difficulty = "Beginner",
            tags = "SQL,Joins,SelfJoin,Hierarchies"
        ),
        q(
            id = "sql_beg_038",
            categoryId = "sql",
            title = "Finding Unmatched Rows: Anti-Join Pattern",
            prompt = "How do you find all customers who have NEVER placed an order using a LEFT JOIN?",
            opt0 = "SELECT * FROM customers c LEFT JOIN orders o ON c.id = o.customer_id WHERE o.order_id = 0;",
            opt1 = "SELECT * FROM customers c LEFT JOIN orders o ON c.id = o.customer_id WHERE o.customer_id IS NULL;",
            opt2 = "SELECT * FROM customers c INNER JOIN orders o ON c.id = o.customer_id WHERE c.id = NULL;",
            opt3 = "SELECT * FROM customers c LEFT JOIN orders o ON c.id = o.customer_id WHERE COUNT(o.id) = 0;",
            correctIndex = 1,
            explanation = "This is the classic 'Anti-Join' pattern: LEFT JOIN to the child table and filter `WHERE child.primary_key IS NULL`. Only parent rows with zero child records survive.",
            difficulty = "Beginner",
            tags = "SQL,Joins,AntiJoin,LEFTJOIN"
        ),
        q(
            id = "sql_beg_039",
            categoryId = "sql",
            title = "UNION vs UNION ALL",
            prompt = "What is the functional and performance difference between `UNION` and `UNION ALL`?",
            opt0 = "UNION ALL removes duplicates; UNION keeps all rows",
            opt1 = "UNION combines result sets and performs an expensive deduplication sort to remove duplicate rows; UNION ALL combines result sets directly without deduplication, making it significantly faster",
            opt2 = "UNION can only combine 2 tables; UNION ALL can combine 10",
            opt3 = "UNION is for numbers; UNION ALL is for strings",
            correctIndex = 1,
            explanation = "UNION runs an implicit sort / hash deduplication to eliminate duplicate rows. UNION ALL simply appends results without checking for duplicates, making it vastly faster when duplicates are impossible or acceptable.",
            difficulty = "Beginner",
            tags = "SQL,SetOperators,UNION,UNIONALL,Performance"
        ),
        q(
            id = "sql_beg_040",
            categoryId = "sql",
            title = "Set Operations: INTERSECT and EXCEPT",
            prompt = "What does `SELECT user_id FROM beta_testers EXCEPT SELECT user_id FROM banned_users;` return?",
            opt0 = "All users who are both beta testers and banned users",
            opt1 = "All user_ids that exist in `beta_testers` but do NOT exist in `banned_users`",
            opt2 = "All banned users",
            opt3 = "The total count of beta testers",
            correctIndex = 1,
            explanation = "EXCEPT (or MINUS in Oracle) returns distinct rows from the first query that do not appear in the second query. (INTERSECT returns rows present in both).",
            difficulty = "Beginner",
            tags = "SQL,SetOperators,EXCEPT,INTERSECT"
        ),
        q(
            id = "sql_beg_041",
            categoryId = "sql",
            title = "Subqueries: Scalar Subquery in SELECT",
            prompt = "What is a 'Scalar Subquery' in SQL?",
            opt0 = "A subquery that runs on a scalar processor",
            opt1 = "A subquery that returns exactly ONE single value (one column of one row), allowing it to be used anywhere a literal constant or expression is valid (e.g. in SELECT or WHERE)",
            opt2 = "A subquery that returns multiple tables",
            opt3 = "A subquery that can only contain numbers between 1 and 10",
            correctIndex = 1,
            explanation = "A scalar subquery evaluates to a single cell (1 row, 1 column). If it returns multiple rows or multiple columns where a scalar is expected, the query engine throws a runtime error.",
            difficulty = "Beginner",
            tags = "SQL,Subqueries,ScalarSubquery"
        ),
        q(
            id = "sql_beg_042",
            categoryId = "sql",
            title = "Subqueries: Correlated vs Non-Correlated",
            prompt = "What makes a subquery 'Correlated'?",
            opt0 = "It uses correlation statistics from the query planner",
            opt1 = "The subquery references columns from the outer query, meaning the subquery cannot be executed independently and conceptually re-evaluates for every candidate row evaluated by the outer query",
            opt2 = "It is written in Python instead of SQL",
            opt3 = "It joins two tables using the CORRELATE keyword",
            correctIndex = 1,
            explanation = "A correlated subquery depends on values from the current outer row (e.g. `WHERE salary > (SELECT AVG(salary) FROM emp e2 WHERE e2.dept_id = e1.dept_id)`). Non-correlated subqueries can run independently once.",
            difficulty = "Beginner",
            tags = "SQL,Subqueries,CorrelatedSubquery"
        ),
        q(
            id = "sql_beg_043",
            categoryId = "sql",
            title = "EXISTS vs IN Subquery",
            prompt = "How does the `EXISTS` operator evaluate a subquery condition?",
            opt0 = "It checks if the subquery returns the number 1",
            opt1 = "It returns TRUE as soon as the subquery yields at least one matching row (short-circuiting immediately without scanning the rest of the subquery table), and FALSE if zero rows match",
            opt2 = "It counts all matching rows and checks if count > 0",
            opt3 = "It verifies that the table file exists on the hard drive",
            correctIndex = 1,
            explanation = "`EXISTS (SELECT ...)` short-circuits on the first matching row found. Because it only checks for row presence, writing `SELECT 1` or `SELECT *` inside EXISTS produces identical execution plans.",
            difficulty = "Beginner",
            tags = "SQL,Subqueries,EXISTS,IN"
        ),
        q(
            id = "sql_beg_044",
            categoryId = "sql",
            title = "INSERT INTO ... SELECT Pattern",
            prompt = "What does `INSERT INTO archived_orders SELECT * FROM orders WHERE order_date < '2025-01-01';` accomplish?",
            opt0 = "It exports orders into a CSV file",
            opt1 = "It copies all matching rows from the `orders` table and inserts them directly into the `archived_orders` table in a single atomic statement",
            opt2 = "It deletes old orders permanently",
            opt3 = "It creates a new table named archived_orders",
            correctIndex = 1,
            explanation = "INSERT INTO ... SELECT inserts query results into an existing destination table without roundtripping through application memory, enabling fast bulk batch transfers.",
            difficulty = "Beginner",
            tags = "SQL,DML,INSERT,BulkOperations"
        ),
        q(
            id = "sql_beg_045",
            categoryId = "sql",
            title = "UPDATE with WHERE Hazard",
            prompt = "What happens if a developer executes `UPDATE users SET is_active = false;` without a `WHERE` clause?",
            opt0 = "The database displays an error asking for confirmation",
            opt1 = "EVERY single row in the entire `users` table is updated to `is_active = false`, potentially causing an catastrophic production outage",
            opt2 = "Only the first row in the table is updated",
            opt3 = "Nothing happens because WHERE is required by SQL syntax",
            correctIndex = 1,
            explanation = "In SQL, an UPDATE or DELETE statement without a WHERE clause operates unconditionally on every single row in the table. Always write WHERE clauses carefully or use transactions with ROLLBACK.",
            difficulty = "Beginner",
            tags = "SQL,DML,UPDATE,Hazards,Safety"
        ),
        q(
            id = "sql_beg_046",
            categoryId = "sql",
            title = "DELETE vs TRUNCATE TABLE",
            prompt = "What are the core differences between `DELETE FROM table;` and `TRUNCATE TABLE table;`?",
            opt0 = "DELETE is faster than TRUNCATE",
            opt1 = "DELETE is DML: it removes rows one by one, writes full undo/redo logs per row, triggers ON DELETE triggers, and can be rolled back; TRUNCATE is DDL: it deallocates data pages instantly, cannot fire triggers, and resets identity seeds",
            opt2 = "TRUNCATE deletes the table schema definition permanently",
            opt3 = "DELETE only works on tables with fewer than 100 rows",
            correctIndex = 1,
            explanation = "TRUNCATE deallocates the underlying data pages directly: it is lightning fast, minimally logged, resets auto-increment sequences, and bypasses row-level DELETE triggers.",
            difficulty = "Beginner",
            tags = "SQL,DDL,DML,DELETE,TRUNCATE"
        ),
        q(
            id = "sql_beg_047",
            categoryId = "sql",
            title = "DROP TABLE vs TRUNCATE TABLE",
            prompt = "How does `DROP TABLE users;` differ from `TRUNCATE TABLE users;`?",
            opt0 = "DROP deletes data but keeps the table structure; TRUNCATE deletes everything",
            opt1 = "TRUNCATE deletes all data rows inside the table but leaves the empty table definition intact; DROP TABLE destroys the table definition, schema metadata, indexes, permissions, and all data permanently",
            opt2 = "DROP TABLE moves the table to a Recycle Bin",
            opt3 = "There is no difference",
            correctIndex = 1,
            explanation = "TRUNCATE empties the container (table still exists). DROP destroys the container itself (the table no longer exists in the database schema).",
            difficulty = "Beginner",
            tags = "SQL,DDL,DROP,TRUNCATE"
        ),
        q(
            id = "sql_beg_048",
            categoryId = "sql",
            title = "CREATE TABLE with Constraints",
            prompt = "What does the `CHECK` constraint enforce in `CREATE TABLE accounts (balance DECIMAL(10,2) CHECK (balance >= 0));`?",
            opt0 = "It checks if the user has a valid credit card",
            opt1 = "It enforces a business rule at the database level, automatically rejecting any INSERT or UPDATE statement that would result in a negative balance with a constraint violation error",
            opt2 = "It formats the balance with a dollar sign",
            opt3 = "It prints the balance to the terminal",
            correctIndex = 1,
            explanation = "CHECK constraints enforce domain integrity rules on column values: mutations violating the condition are rejected immediately by the database engine.",
            difficulty = "Beginner",
            tags = "SQL,DDL,CHECKConstraint,Constraints"
        ),
        q(
            id = "sql_beg_049",
            categoryId = "sql",
            title = "ALTER TABLE: Adding Columns",
            prompt = "What is the standard SQL syntax to add a new column `email_verified BOOLEAN DEFAULT FALSE NOT NULL` to an existing table `users`?",
            opt0 = "UPDATE TABLE users ADD email_verified BOOLEAN;",
            opt1 = "ALTER TABLE users ADD COLUMN email_verified BOOLEAN DEFAULT FALSE NOT NULL;",
            opt2 = "INSERT INTO users (email_verified) VALUES (FALSE);",
            opt3 = "CREATE COLUMN email_verified ON users;",
            correctIndex = 1,
            explanation = "`ALTER TABLE table_name ADD COLUMN column_name data_type [constraints]` is standard SQL for schema evolution.",
            difficulty = "Beginner",
            tags = "SQL,DDL,ALTERTABLE,SchemaMigration"
        ),
        q(
            id = "sql_beg_050",
            categoryId = "sql",
            title = "SQL Views: Definition and Purpose",
            prompt = "What is a standard database View (`CREATE VIEW v_active_users AS ...`)?",
            opt0 = "A physical copy of table data stored in RAM",
            opt1 = "A saved, named SQL query stored in the database catalog (a 'virtual table'); querying the view executes the underlying SELECT statement dynamically without duplicating physical table data on disk",
            opt2 = "A screenshot of database tables",
            opt3 = "A temporary table that expires after 1 hour",
            correctIndex = 1,
            explanation = "A View is a virtual table defined by a query. It encapsulates complex joins and security filters. Standard views do not store data; they run the underlying query when accessed.",
            difficulty = "Beginner",
            tags = "SQL,DDL,Views,VirtualTables"
        )
    )

    private fun part3(): List<QuestionEntity> = listOf(
        q(
            id = "sql_beg_051",
            categoryId = "sql",
            title = "Database Indexes: Primary Purpose",
            prompt = "What is the primary motivation for creating an Index on a database column (e.g. `CREATE INDEX idx_users_email ON users(email);`)?",
            opt0 = "To compress text data by 50%",
            opt1 = "To enable rapid locating of matching rows in O(log N) time without scanning every single page of the table (avoiding full table scans), at the cost of slower writes and extra disk space",
            opt2 = "To prevent users from entering invalid emails",
            opt3 = "To automatically sort the physical hard drive",
            correctIndex = 1,
            explanation = "Indexes act like book indexes: instead of reading all 10M rows (Full Table Scan), the B-Tree index locates keys in ~3-4 page reads. However, every INSERT/UPDATE/DELETE must also update the index.",
            difficulty = "Beginner",
            tags = "SQL,Indexing,Performance,BTree"
        ),
        q(
            id = "sql_beg_052",
            categoryId = "sql",
            title = "Unique Index vs Unique Constraint",
            prompt = "In modern relational databases, what is the practical difference between creating a `UNIQUE` constraint on a column and creating a `UNIQUE INDEX` on it?",
            opt0 = "A Unique Constraint allows duplicates; a Unique Index does not",
            opt1 = "They are functionally equivalent: creating a Unique Constraint automatically creates a Unique Index behind the scenes to enforce the uniqueness invariant during write operations",
            opt2 = "Unique Indexes cannot be used in WHERE clauses",
            opt3 = "Unique Constraints are only available in MySQL",
            correctIndex = 1,
            explanation = "Relational engines enforce Unique Constraints by automatically creating a backing Unique B-Tree Index. The two are semantically and physically intertwined.",
            difficulty = "Beginner",
            tags = "SQL,Indexing,UniqueIndex,Constraints"
        ),
        q(
            id = "sql_beg_053",
            categoryId = "sql",
            title = "Composite Index Column Order",
            prompt = "Given a composite index `CREATE INDEX idx_name ON employees(last_name, first_name);`, which query CANNOT take advantage of this index?",
            opt0 = "SELECT * FROM employees WHERE last_name = 'Smith' AND first_name = 'John';",
            opt1 = "SELECT * FROM employees WHERE first_name = 'John'; (filtering ONLY on the second column)",
            opt2 = "SELECT * FROM employees WHERE last_name = 'Smith';",
            opt3 = "SELECT * FROM employees WHERE last_name LIKE 'Smi%';",
            correctIndex = 1,
            explanation = "Leftmost Prefix Rule: a composite index on (A, B) can satisfy queries filtering on (A) or (A, B), but CANNOT be used efficiently for queries filtering ONLY on (B), just as a phonebook ordered by LastName, FirstName cannot help find people named 'John'.",
            difficulty = "Beginner",
            tags = "SQL,Indexing,CompositeIndex,LeftmostPrefix"
        ),
        q(
            id = "sql_beg_054",
            categoryId = "sql",
            title = "ACID Properties: Atomicity",
            prompt = "What does the 'A' (Atomicity) in the ACID transaction model guarantee?",
            opt0 = "Queries execute at the atomic nuclear speed of light",
            opt1 = "'All-or-Nothing': all operations within the transaction must succeed and commit together; if ANY operation fails, the entire transaction is rolled back and the database reverts to its exact state before the transaction began",
            opt2 = "All variables must be 32-bit integers",
            opt3 = "Transactions cannot be accessed by other threads",
            correctIndex = 1,
            explanation = "Atomicity prevents partial state mutations (e.g. money deducted from Account A, but transfer crashes before crediting Account B). On failure, all intermediate writes are rolled back.",
            difficulty = "Beginner",
            tags = "SQL,ACID,Atomicity,Transactions"
        ),
        q(
            id = "sql_beg_055",
            categoryId = "sql",
            title = "ACID Properties: Consistency",
            prompt = "What does 'Consistency' (C in ACID) ensure during a database transaction?",
            opt0 = "All servers in a cluster have identical data at the same millisecond",
            opt1 = "The transaction transitions the database from one valid state to another valid state, strictly satisfying all declared schema constraints, foreign keys, cascades, unique rules, and triggers",
            opt2 = "All queries return identical results",
            opt3 = "Data cannot be edited after creation",
            correctIndex = 1,
            explanation = "In ACID, Consistency means database invariants (primary keys, foreign keys, CHECK constraints) must hold true before and after the transaction commits. If a constraint is violated, the transaction aborts.",
            difficulty = "Beginner",
            tags = "SQL,ACID,Consistency,Transactions"
        ),
        q(
            id = "sql_beg_056",
            categoryId = "sql",
            title = "ACID Properties: Isolation",
            prompt = "What does 'Isolation' (I in ACID) guarantee for concurrently running transactions?",
            opt0 = "The database is isolated from the internet with a firewall",
            opt1 = "Concurrent transactions execute without interfering with one another; intermediate uncommitted state from Transaction A is hidden from concurrent Transaction B according to the configured isolation level",
            opt2 = "Only one user can connect to the database at a time",
            opt3 = "Data is stored on isolated optical discs",
            correctIndex = 1,
            explanation = "Isolation controls concurrency anomalies (dirty reads, non-repeatable reads, phantom reads). To concurrent transactions, execution appears as though transactions ran serially.",
            difficulty = "Beginner",
            tags = "SQL,ACID,Isolation,Transactions"
        ),
        q(
            id = "sql_beg_057",
            categoryId = "sql",
            title = "ACID Properties: Durability",
            prompt = "What does 'Durability' (D in ACID) ensure once a transaction successfully commits?",
            opt0 = "Hard drives last for at least 10 years",
            opt1 = "Once a transaction commits, its mutations are permanently recorded in non-volatile storage (via Write-Ahead Logging); updates will NOT be lost even in the event of an immediate total power outage or OS system crash",
            opt2 = "Queries run without timing out",
            opt3 = "Tables cannot be dropped",
            correctIndex = 1,
            explanation = "Durability guarantees persistence. Once `COMMIT` returns success, writes survive power loss, crashes, or reboot via Write-Ahead Logs (WAL) flushed to non-volatile disk.",
            difficulty = "Beginner",
            tags = "SQL,ACID,Durability,WAL"
        ),
        q(
            id = "sql_beg_058",
            categoryId = "sql",
            title = "Transaction Control: SAVEPOINT and ROLLBACK TO",
            prompt = "What is the purpose of a `SAVEPOINT` within a transaction?",
            opt0 = "To backup the database to AWS S3",
            opt1 = "To create a checkpoint within a transaction, allowing partial rollback (`ROLLBACK TO savepoint_name`) of subsequent failed statements without aborting the entire transaction",
            opt2 = "To pause query execution for 5 seconds",
            opt3 = "To save memory by compressing logs",
            correctIndex = 1,
            explanation = "SAVEPOINT sets a rollback marker. If step 3 of a complex transaction fails, `ROLLBACK TO sp1` undoes step 3 while preserving steps 1 and 2, allowing recovery without total transaction abort.",
            difficulty = "Beginner",
            tags = "SQL,TCL,SAVEPOINT,ROLLBACK"
        ),
        q(
            id = "sql_beg_059",
            categoryId = "sql",
            title = "Database Normalization: First Normal Form (1NF)",
            prompt = "What are the core requirements for a table to satisfy First Normal Form (1NF)?",
            opt0 = "All column names must be in lowercase",
            opt1 = "Each column must contain atomic (indivisible) values (no repeating groups, arrays, or comma-separated lists like 'red, green, blue' in a single cell), and each row must have a unique identity (Primary Key)",
            opt2 = "The table must have at least 10 columns",
            opt3 = "All tables must have foreign keys",
            correctIndex = 1,
            explanation = "1NF mandates atomic values: no comma-separated lists or nested records in a single cell. Each row must be uniquely identifiable via a primary key.",
            difficulty = "Beginner",
            tags = "SQL,Normalization,1NF,DataModeling"
        ),
        q(
            id = "sql_beg_060",
            categoryId = "sql",
            title = "Database Normalization: Second Normal Form (2NF)",
            prompt = "What does Second Normal Form (2NF) require beyond 1NF?",
            opt0 = "The table must have two primary keys",
            opt1 = "The table must be in 1NF AND have NO Partial Functional Dependencies: every non-key column must depend on the WHOLE primary key, not just a subset of a composite primary key",
            opt2 = "All strings must be converted to VARCHAR",
            opt3 = "Tables must have fewer than 1,000 rows",
            correctIndex = 1,
            explanation = "2NF eliminates partial dependencies. If composite PK is (order_id, product_id), storing `product_name` in this table violates 2NF because product_name depends only on product_id, not order_id.",
            difficulty = "Beginner",
            tags = "SQL,Normalization,2NF,DataModeling"
        ),
        q(
            id = "sql_beg_061",
            categoryId = "sql",
            title = "Database Normalization: Third Normal Form (3NF)",
            prompt = "What does Third Normal Form (3NF) eliminate beyond 2NF?",
            opt0 = "It eliminates foreign keys",
            opt1 = "The table must be in 2NF AND have NO Transitive Dependencies: non-key columns must not depend on other non-key columns ('every non-key attribute must provide a fact about the key, the whole key, and nothing but the key')",
            opt2 = "It eliminates NULL values",
            opt3 = "It limits tables to 3 columns",
            correctIndex = 1,
            explanation = "3NF eliminates transitive dependencies. If employee table has `dept_id` and `dept_name`, `dept_name` depends on `dept_id`, which depends on `emp_id`. `dept_name` belongs in a separate departments table.",
            difficulty = "Beginner",
            tags = "SQL,Normalization,3NF,DataModeling"
        ),
        q(
            id = "sql_beg_062",
            categoryId = "sql",
            title = "Denormalization: Trade-offs",
            prompt = "Why do high-throughput analytical and read-heavy systems intentionally 'Denormalize' relational schemas?",
            opt0 = "Because normalization is forbidden in production systems",
            opt1 = "To eliminate expensive multi-table JOIN operations during read queries by duplicating specific data across tables, trading increased storage and write update complexity for sub-second read query latency",
            opt2 = "To reduce database backup file sizes",
            opt3 = "Because denormalized tables cannot have bugs",
            correctIndex = 1,
            explanation = "Denormalization stores pre-joined or redundant data (e.g. copying customer_name into orders). Reads avoid expensive 5-way joins, but updates must write to multiple places to keep data consistent.",
            difficulty = "Beginner",
            tags = "SQL,DataModeling,Denormalization,Tradeoffs"
        ),
        q(
            id = "sql_beg_063",
            categoryId = "sql",
            title = "SQL Injection Vulnerability Mechanism",
            prompt = "How does raw string concatenation in SQL queries (e.g. `\"SELECT * FROM users WHERE user = '\" + input + \"'\"`) cause an SQL Injection vulnerability?",
            opt0 = "It causes the compiler to run out of RAM",
            opt1 = "Malicious user input containing quotes (e.g. `' OR '1'='1`) alters the SQL syntax and query structure parsed by the database engine, allowing attackers to bypass authentication or extract entire databases",
            opt2 = "It slows down internet connection speeds",
            opt3 = "It deletes SQL Server from the operating system",
            correctIndex = 1,
            explanation = "Concatenation blurs code and untrusted data. The SQL parser treats input quotes and commands (`' OR '1'='1' --`) as executable SQL grammar rather than literal data values.",
            difficulty = "Beginner",
            tags = "SQL,Security,SQLInjection,Vulnerabilities"
        ),
        q(
            id = "sql_beg_064",
            categoryId = "sql",
            title = "Preventing SQL Injection: Parameterized Prepared Statements",
            prompt = "Why do Parameterized Queries (Prepared Statements with placeholders `?` or `:param`) completely neutralize SQL Injection?",
            opt0 = "They encrypt the database disk with AES-256",
            opt1 = "The database compiles the SQL syntax template FIRST into an execution plan before binding parameters; parameters are treated strictly as literal data values and can never be interpreted as executable SQL syntax",
            opt2 = "They strip all quotes from user input automatically",
            opt3 = "They force the user to enter numbers only",
            correctIndex = 1,
            explanation = "Prepared statements separate code compilation from data evaluation. Even if an attacker passes `'; DROP TABLE users; --`, the database treats the entire string as a literal username value.",
            difficulty = "Beginner",
            tags = "SQL,Security,PreparedStatement,SQLInjectionPrevention"
        ),
        q(
            id = "sql_beg_065",
            categoryId = "sql",
            title = "SQL Functions: SUBSTRING / SUBSTR",
            prompt = "In standard SQL, what does `SUBSTRING('DATABASE', 5, 4)` return? (Note: SQL strings are 1-indexed)",
            opt0 = "'DATA'",
            opt1 = "'BASE'",
            opt2 = "'ATAB'",
            opt3 = "'TABA'",
            correctIndex = 1,
            explanation = "In standard SQL, string indexes are 1-indexed (not 0-indexed). Position 5 is 'B'. Taking length 4 yields 'BASE'.",
            difficulty = "Beginner",
            tags = "SQL,StringFunctions,SUBSTRING,1Indexed"
        ),
        q(
            id = "sql_beg_066",
            categoryId = "sql",
            title = "SQL Functions: LENGTH vs CHAR_LENGTH / OCTET_LENGTH",
            prompt = "When dealing with multi-byte UTF-8 international characters (like emojis '🚀' or accents 'é'), what is the difference between `CHAR_LENGTH` and `OCTET_LENGTH`?",
            opt0 = "CHAR_LENGTH measures line breaks; OCTET_LENGTH measures words",
            opt1 = "CHAR_LENGTH returns the count of actual human-visible characters (e.g. 1 for '🚀'); OCTET_LENGTH returns the physical byte storage count (e.g. 4 bytes in UTF-8)",
            opt2 = "OCTET_LENGTH is only used in network routers",
            opt3 = "There is no difference in Unicode",
            correctIndex = 1,
            explanation = "CHAR_LENGTH counts logical Unicode codepoints. OCTET_LENGTH measures raw byte length. Multi-byte characters take 2-4 bytes each in UTF-8.",
            difficulty = "Beginner",
            tags = "SQL,StringFunctions,CHAR_LENGTH,UTF8"
        ),
        q(
            id = "sql_beg_067",
            categoryId = "sql",
            title = "SQL Functions: ROUND vs TRUNCATE / TRUNC",
            prompt = "What do `ROUND(123.456, 2)` and `TRUNC(123.456, 2)` return in relational databases?",
            opt0 = "Both return 123.45",
            opt1 = "ROUND returns 123.46 (rounds up because next digit is 6); TRUNC returns 123.45 (chops off digits after 2 decimal places without rounding)",
            opt2 = "Both return 123.50",
            opt3 = "ROUND returns 123.00; TRUNC returns 123.46",
            correctIndex = 1,
            explanation = "ROUND rounds mathematically ($6 \\ge 5$ rounds up to .46). TRUNC / TRUNCATE strictly truncates toward zero without rounding.",
            difficulty = "Beginner",
            tags = "SQL,MathFunctions,ROUND,TRUNC"
        ),
        q(
            id = "sql_beg_068",
            categoryId = "sql",
            title = "SQL Data Types: BOOLEAN Support",
            prompt = "How does MySQL traditionally represent the `BOOLEAN` data type compared to PostgreSQL?",
            opt0 = "MySQL uses true/false text strings; PostgreSQL uses 0/1",
            opt1 = "MySQL has no native boolean type: `BOOLEAN` is an alias for `TINYINT(1)` (where 0 is false, non-zero is true); PostgreSQL has a native `BOOLEAN` type with true, false, and NULL",
            opt2 = "Both engines forbid boolean columns",
            opt3 = "PostgreSQL stores booleans as 64-bit floats",
            correctIndex = 1,
            explanation = "MySQL historically treats BOOLEAN as TINYINT(1). PostgreSQL implements a full first-class 3-valued BOOLEAN type (true, false, NULL).",
            difficulty = "Beginner",
            tags = "SQL,DataTypes,BOOLEAN,MySQL,PostgreSQL"
        ),
        q(
            id = "sql_beg_069",
            categoryId = "sql",
            title = "Database Transactions: Autocommit Mode",
            prompt = "What does 'Autocommit Mode' do in relational database client connections?",
            opt0 = "It commits code to Git on every query",
            opt1 = "Every individual SQL statement (INSERT, UPDATE, DELETE) is automatically treated as its own independent single-statement transaction and committed immediately without needing explicit `COMMIT`",
            opt2 = "It rejects manual COMMIT commands with an error",
            opt3 = "It automatically rolls back queries that take longer than 1 second",
            correctIndex = 1,
            explanation = "By default in many SQL clients, autocommit is enabled: every single statement commits immediately upon execution. Disabling autocommit requires explicit `COMMIT` to persist writes.",
            difficulty = "Beginner",
            tags = "SQL,Transactions,Autocommit,TCL"
        ),
        q(
            id = "sql_beg_070",
            categoryId = "sql",
            title = "SQL Execution Order: Logical Query Processing",
            prompt = "In what logical order does a relational database engine evaluate the clauses of a SELECT query?",
            opt0 = "SELECT -> FROM -> WHERE -> GROUP BY -> HAVING -> ORDER BY",
            opt1 = "FROM -> WHERE -> GROUP BY -> HAVING -> SELECT -> ORDER BY -> LIMIT",
            opt2 = "ORDER BY -> SELECT -> FROM -> WHERE -> HAVING -> GROUP BY",
            opt3 = "LIMIT -> SELECT -> WHERE -> FROM -> GROUP BY -> HAVING",
            correctIndex = 1,
            explanation = "Databases evaluate FROM first (locating tables and joins), then WHERE (filtering rows), GROUP BY, HAVING, SELECT (computing expressions and aliases), ORDER BY (sorting), and finally LIMIT/OFFSET.",
            difficulty = "Beginner",
            tags = "SQL,LogicalQueryProcessing,ExecutionOrder,Fundamentals"
        ),
        q(
            id = "sql_beg_071",
            categoryId = "sql",
            title = "Why Column Aliases Cannot Be Used in WHERE Clauses",
            prompt = "Why does the query `SELECT salary * 12 AS annual_sal FROM emp WHERE annual_sal > 50000;` fail with an 'unknown column' error?",
            opt0 = "annual_sal is a reserved keyword in SQL",
            opt1 = "Because the `WHERE` clause is logically evaluated BEFORE the `SELECT` clause in the query execution lifecycle; the alias `annual_sal` has not yet been defined when WHERE executes",
            opt2 = "Calculations cannot exceed 50,000 in SQL",
            opt3 = "Aliases can only be used with tables, not columns",
            correctIndex = 1,
            explanation = "Logical order: FROM -> WHERE -> SELECT. Because WHERE executes before SELECT, column aliases created in SELECT do not exist yet. You must repeat the expression in WHERE.",
            difficulty = "Beginner",
            tags = "SQL,LogicalQueryProcessing,Aliasing,WHERE"
        ),
        q(
            id = "sql_beg_072",
            categoryId = "sql",
            title = "Why Column Aliases ARE Allowed in ORDER BY",
            prompt = "Why DOES `SELECT salary * 12 AS annual_sal FROM emp ORDER BY annual_sal DESC;` work perfectly?",
            opt0 = "ORDER BY is faster than WHERE",
            opt1 = "Because `ORDER BY` is logically evaluated AFTER the `SELECT` clause; by the time sorting occurs, the `annual_sal` alias is fully computed and accessible",
            opt2 = "Because annual_sal is automatically indexed",
            opt3 = "It only works if the column is a primary key",
            correctIndex = 1,
            explanation = "Logical order: SELECT evaluates before ORDER BY. Therefore, expressions and column aliases defined in SELECT are completely visible and valid inside ORDER BY.",
            difficulty = "Beginner",
            tags = "SQL,LogicalQueryProcessing,ORDERBY,Aliasing"
        ),
        q(
            id = "sql_beg_073",
            categoryId = "sql",
            title = "SQL Functions: UPPER, LOWER, and TRIM",
            prompt = "What does `SELECT TRIM('  hello world  ');` return?",
            opt0 = "'helloworld' (removes all spaces including inside words)",
            opt1 = "'hello world' (removes leading and trailing whitespace while preserving internal spaces)",
            opt2 = "It truncates the string to 5 characters",
            opt3 = "It capitalizes all letters",
            correctIndex = 1,
            explanation = "TRIM strips leading and trailing spaces from a string. (LTRIM strips only leading; RTRIM strips only trailing). Internal spaces are preserved.",
            difficulty = "Beginner",
            tags = "SQL,StringFunctions,TRIM"
        ),
        q(
            id = "sql_beg_074",
            categoryId = "sql",
            title = "Finding Duplicates: GROUP BY with HAVING COUNT(*) > 1",
            prompt = "How do you write an SQL query to identify all email addresses that appear more than once in a `users` table?",
            opt0 = "SELECT email FROM users WHERE COUNT(email) > 1;",
            opt1 = "SELECT email, COUNT(*) FROM users GROUP BY email HAVING COUNT(*) > 1;",
            opt2 = "SELECT DISTINCT email FROM users WHERE email IS DUPLICATE;",
            opt3 = "SELECT email FROM users ORDER BY email LIMIT 1;",
            correctIndex = 1,
            explanation = "The canonical pattern for duplicate detection: `GROUP BY column HAVING COUNT(*) > 1`. Aggregating by email and filtering groups with count > 1 isolates duplicates instantly.",
            difficulty = "Beginner",
            tags = "SQL,Aggregation,Duplicates,HAVING"
        ),
        q(
            id = "sql_beg_075",
            categoryId = "sql",
            title = "SQL Comments Syntax",
            prompt = "What are the standard single-line and multi-line comment delimiters in SQL?",
            opt0 = "// for single-line; /* */ for multi-line",
            opt1 = "-- (double dash) for single-line; /* ... */ for multi-line",
            opt2 = "# for single-line; <!-- --> for multi-line",
            opt3 = "; for single-line; REM for multi-line",
            correctIndex = 1,
            explanation = "ANSI SQL specifies `-- comment` for single-line comments and `/* comment */` for multi-line block comments.",
            difficulty = "Beginner",
            tags = "SQL,Syntax,Comments"
        )
    )

    private fun part4(): List<QuestionEntity> = listOf(
        q(
            id = "sql_beg_076",
            categoryId = "sql",
            title = "Database Views: Updatable Views",
            prompt = "Under what conditions can a developer execute an `UPDATE` statement directly against a database View?",
            opt0 = "Views can never be updated under any circumstances",
            opt1 = "The view must be based on a single base table without aggregate functions (SUM, COUNT), GROUP BY, HAVING, DISTINCT, or UNION, mapping directly 1-to-1 to underlying table rows",
            opt2 = "The view must have an index attached to it",
            opt3 = "Any view can be updated at any time",
            correctIndex = 1,
            explanation = "Updatable views must unambiguously map to a single underlying base table row. If a view contains aggregates, joins, or DISTINCT, the database cannot determine which underlying rows to mutate.",
            difficulty = "Beginner",
            tags = "SQL,Views,UpdatableViews,DML"
        ),
        q(
            id = "sql_beg_077",
            categoryId = "sql",
            title = "Temporary Tables vs Regular Tables",
            prompt = "What is the lifecycle and scope of a Temporary Table (e.g. `CREATE TEMPORARY TABLE temp_orders ...`)?",
            opt0 = "It persists forever until manually deleted with DROP TABLE",
            opt1 = "It is visible ONLY to the current database session/connection that created it, and it is automatically dropped by the database engine when the connection closes or disconnects",
            opt2 = "It is visible to all users across the company",
            opt3 = "It is stored in the browser cache",
            correctIndex = 1,
            explanation = "Temporary tables are session-scoped scratchpads. They are isolated from other concurrent sessions and are destroyed automatically when the client disconnects.",
            difficulty = "Beginner",
            tags = "SQL,DDL,TemporaryTables,SessionScope"
        ),
        q(
            id = "sql_beg_078",
            categoryId = "sql",
            title = "Common Table Expressions (CTE): WITH Clause",
            prompt = "What is a Common Table Expression (CTE) defined using the `WITH` keyword?",
            opt0 = "A permanent table created inside the operating system",
            opt1 = "A named temporary result set defined within the scope of a single query (e.g. `WITH RegionalSales AS (...) SELECT * FROM RegionalSales;`), improving query readability and modularity over nested subqueries",
            opt2 = "A trigger that executes before INSERT",
            opt3 = "A stored procedure written in Java",
            correctIndex = 1,
            explanation = "CTEs (`WITH cte_name AS (...)`) act like named inline views scoped to that single query. They make complex multi-step queries vastly more readable than deeply nested subqueries.",
            difficulty = "Beginner",
            tags = "SQL,DQL,CTE,WITHClause"
        ),
        q(
            id = "sql_beg_079",
            categoryId = "sql",
            title = "Window Functions: ROW_NUMBER() vs RANK()",
            prompt = "If three employees tie with identical salaries, how do `ROW_NUMBER()` and `RANK()` assign ranks?",
            opt0 = "Both assign the identical rank to tied employees",
            opt1 = "ROW_NUMBER() assigns strictly unique, consecutive numbers (e.g. 1, 2, 3); RANK() assigns the same rank to ties and leaves gaps (e.g. 1, 1, 1, followed by 4)",
            opt2 = "RANK() assigns numbers in reverse order",
            opt3 = "ROW_NUMBER() throws an error on ties",
            correctIndex = 1,
            explanation = "ROW_NUMBER() never ties (always 1, 2, 3, 4). RANK() gives ties the same rank but leaves gaps (1, 1, 1, 4). DENSE_RANK() gives ties the same rank without gaps (1, 1, 1, 2).",
            difficulty = "Beginner",
            tags = "SQL,WindowFunctions,ROW_NUMBER,RANK"
        ),
        q(
            id = "sql_beg_080",
            categoryId = "sql",
            title = "Window Functions: DENSE_RANK() Behavior",
            prompt = "What sequence of rank numbers does `DENSE_RANK()` produce for values [100, 100, 80, 70]?",
            opt0 = "1, 2, 3, 4",
            opt1 = "1, 1, 2, 3 (ties share rank 1, and the next distinct value receives rank 2 with no numerical gaps)",
            opt2 = "1, 1, 3, 4",
            opt3 = "1, 2, 2, 3",
            correctIndex = 1,
            explanation = "DENSE_RANK() does not skip ranks after ties. Tied values receive rank 1, and the next distinct value immediately receives rank 2.",
            difficulty = "Beginner",
            tags = "SQL,WindowFunctions,DENSE_RANK"
        ),
        q(
            id = "sql_beg_081",
            categoryId = "sql",
            title = "Finding Nth Highest Salary Pattern",
            prompt = "How can you find the 2nd highest salary in an `employees` table using `DENSE_RANK()`?",
            opt0 = "SELECT MAX(salary) - 1 FROM employees;",
            opt1 = "WITH RankedSalaries AS (SELECT salary, DENSE_RANK() OVER (ORDER BY salary DESC) as rnk FROM employees) SELECT salary FROM RankedSalaries WHERE rnk = 2 LIMIT 1;",
            opt2 = "SELECT salary FROM employees ORDER BY salary DESC LIMIT 2;",
            opt3 = "SELECT AVG(salary) FROM employees WHERE salary < MAX(salary);",
            correctIndex = 1,
            explanation = "Using DENSE_RANK() in a CTE or subquery handles ties gracefully: if 5 employees share the top salary ($100k), all 5 get rank 1, and the true 2nd highest salary ($90k) receives rank 2.",
            difficulty = "Beginner",
            tags = "SQL,WindowFunctions,NthHighestSalary,InterviewPatterns"
        ),
        q(
            id = "sql_beg_082",
            categoryId = "sql",
            title = "Window Functions: OVER(PARTITION BY ...)",
            prompt = "What does `AVG(salary) OVER(PARTITION BY department_id)` calculate for each employee row?",
            opt0 = "The average salary across all employees in the entire company",
            opt1 = "The average salary of the employee's specific department, returned alongside each individual employee's row WITHOUT collapsing rows like a GROUP BY would",
            opt2 = "It divides the salary by the department ID number",
            opt3 = "It sorts the employees by department",
            correctIndex = 1,
            explanation = "PARTITION BY divides the window into groups. Unlike GROUP BY (which collapses rows into a single summary row), window functions append the group calculation to every original row.",
            difficulty = "Beginner",
            tags = "SQL,WindowFunctions,PARTITIONBY,Analytics"
        ),
        q(
            id = "sql_beg_083",
            categoryId = "sql",
            title = "Stored Procedures vs Functions",
            prompt = "What is a primary distinction between a Stored Function and a Stored Procedure in SQL?",
            opt0 = "Procedures are in Java; Functions are in SQL",
            opt1 = "A Function must return a value and can be invoked directly inside a SELECT query expression; a Procedure is executed via CALL/EXEC, can execute DDL/TCL transaction control, and may return zero or multiple values",
            opt2 = "Procedures cannot take input parameters",
            opt3 = "Functions can modify database schemas",
            correctIndex = 1,
            explanation = "Stored Functions return a single value and can be embedded in SQL expressions (`SELECT my_func(col) FROM t`). Stored Procedures execute standalone procedural code and manage transaction boundaries.",
            difficulty = "Beginner",
            tags = "SQL,Programmability,StoredProcedures,Functions"
        ),
        q(
            id = "sql_beg_084",
            categoryId = "sql",
            title = "Database Triggers: Definition and Types",
            prompt = "What is a Database Trigger?",
            opt0 = "A physical button on the database server hardware",
            opt1 = "A special stored procedure that automatically executes (fires) in response to specific events (INSERT, UPDATE, DELETE) on a particular table (e.g. BEFORE INSERT or AFTER UPDATE)",
            opt2 = "An index that speeds up searches",
            opt3 = "A network firewall rule",
            correctIndex = 1,
            explanation = "Triggers execute automatically upon DML events. BEFORE triggers can validate or modify incoming data; AFTER triggers commonly write audit history logs.",
            difficulty = "Beginner",
            tags = "SQL,Programmability,Triggers,Auditing"
        ),
        q(
            id = "sql_beg_085",
            categoryId = "sql",
            title = "Triggers: BEFORE vs AFTER",
            prompt = "When should a `BEFORE INSERT` trigger be chosen over an `AFTER INSERT` trigger?",
            opt0 = "To write audit logs to an external database table",
            opt1 = "When you need to validate, sanitize, or modify incoming column values (e.g. trimming strings or setting default timestamps) before the row is actually written to the table",
            opt2 = "When sending email notifications to users",
            opt3 = "BEFORE triggers are 100x faster than AFTER triggers",
            correctIndex = 1,
            explanation = "BEFORE triggers inspect and alter the `NEW` row values before persistence. AFTER triggers fire after data is safely written, making them ideal for cascading audit logging.",
            difficulty = "Beginner",
            tags = "SQL,Programmability,Triggers,BEFORE,AFTER"
        ),
        q(
            id = "sql_beg_086",
            categoryId = "sql",
            title = "Database Sequences and AUTO_INCREMENT",
            prompt = "What is the purpose of an SQL `SEQUENCE` (or MySQL `AUTO_INCREMENT`)?",
            opt0 = "To enforce alphabetical sorting on strings",
            opt1 = "To generate a monotonically increasing sequence of unique integer numbers, commonly used to populate surrogate primary key columns automatically across concurrent transactions",
            opt2 = "To count how many users are logged in",
            opt3 = "To track query execution times",
            correctIndex = 1,
            explanation = "Sequences generate guaranteed unique numbers outside transaction boundaries, ensuring concurrent inserts receive distinct primary key IDs without lock contention.",
            difficulty = "Beginner",
            tags = "SQL,DDL,Sequence,AutoIncrement"
        ),
        q(
            id = "sql_beg_087",
            categoryId = "sql",
            title = "Database Transactions: Dirty Read Anomaly",
            prompt = "What is a 'Dirty Read' anomaly in concurrent database transaction processing?",
            opt0 = "Reading data from a corrupted hard drive sector",
            opt1 = "Transaction A modifies a row but has NOT committed yet; concurrent Transaction B reads this uncommitted row; Transaction A then rolls back, meaning Transaction B based decisions on data that never existed",
            opt2 = "Reading data without having read permissions",
            opt3 = "Reading a table that has no primary key",
            correctIndex = 1,
            explanation = "A dirty read occurs under the lowest isolation level (READ UNCOMMITTED): reading dirty uncommitted data that is subsequently rolled back and erased.",
            difficulty = "Beginner",
            tags = "SQL,Concurrency,DirtyRead,IsolationLevels"
        ),
        q(
            id = "sql_beg_088",
            categoryId = "sql",
            title = "Database Transactions: Non-Repeatable Read Anomaly",
            prompt = "What is a 'Non-Repeatable Read' anomaly?",
            opt0 = "A query that can only be executed once per day",
            opt1 = "Transaction A reads a row; concurrent Transaction B modifies and COMMITS that row; Transaction A re-reads the row and finds that its values have changed during the same transaction",
            opt2 = "An infinite loop in recursive queries",
            opt3 = "When an index is corrupted",
            correctIndex = 1,
            explanation = "Under READ COMMITTED, reading the same row twice inside one transaction can return different values if another transaction committed updates in between.",
            difficulty = "Beginner",
            tags = "SQL,Concurrency,NonRepeatableRead,IsolationLevels"
        ),
        q(
            id = "sql_beg_089",
            categoryId = "sql",
            title = "Database Transactions: Phantom Read Anomaly",
            prompt = "What is a 'Phantom Read' anomaly?",
            opt0 = "Reading data from a deleted database user account",
            opt1 = "Transaction A runs a range query (e.g. `WHERE age > 30`, returning 5 rows); concurrent Transaction B inserts a new person aged 35 and COMMITS; Transaction A re-runs the query and finds 6 rows (a phantom row appeared)",
            opt2 = "A query that runs on invisible hidden tables",
            opt3 = "When primary keys change automatically",
            correctIndex = 1,
            explanation = "Phantom reads occur on range queries: a concurrent transaction commits new matching rows, causing subsequent executions of the range query to observe 'phantom' rows.",
            difficulty = "Beginner",
            tags = "SQL,Concurrency,PhantomRead,IsolationLevels"
        ),
        q(
            id = "sql_beg_090",
            categoryId = "sql",
            title = "SQL Four ANSI Isolation Levels",
            prompt = "What are the four standard ANSI SQL transaction isolation levels in order from lowest (least isolated) to highest (most isolated)?",
            opt0 = "Low, Medium, High, and Ultra",
            opt1 = "READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, and SERIALIZABLE",
            opt2 = "Snapshot, Optimistic, Pessimistic, and Distributed",
            opt3 = "Single, Shared, Exclusive, and Deadlock",
            correctIndex = 1,
            explanation = "The 4 ANSI isolation levels balance concurrency vs safety: READ UNCOMMITTED (fastest, permits dirty reads) to SERIALIZABLE (safest, eliminates all concurrency anomalies).",
            difficulty = "Beginner",
            tags = "SQL,Transactions,IsolationLevels,ANSI"
        ),
        q(
            id = "sql_beg_091",
            categoryId = "sql",
            title = "Default Isolation Level: MySQL vs PostgreSQL/Oracle",
            prompt = "What are the default transaction isolation levels in MySQL (InnoDB) versus PostgreSQL / Oracle?",
            opt0 = "Both default to SERIALIZABLE",
            opt1 = "MySQL InnoDB defaults to REPEATABLE READ; PostgreSQL and Oracle default to READ COMMITTED",
            opt2 = "MySQL defaults to READ UNCOMMITTED; PostgreSQL defaults to READ UNCOMMITTED",
            opt3 = "Both default to no isolation",
            correctIndex = 1,
            explanation = "MySQL InnoDB defaults to REPEATABLE READ (using Next-Key locking to prevent phantoms). PostgreSQL, Oracle, and SQL Server default to READ COMMITTED.",
            difficulty = "Beginner",
            tags = "SQL,Databases,MySQL,PostgreSQL,IsolationLevels"
        ),
        q(
            id = "sql_beg_092",
            categoryId = "sql",
            title = "Database Deadlock Definition",
            prompt = "What is a Database Deadlock and how does the database engine resolve it?",
            opt0 = "A permanent failure requiring the server hard drive to be replaced",
            opt1 = "Two transactions each hold a lock that the other transaction needs to proceed (circular wait); the engine's deadlock detector identifies the cycle and aborts/rolls back one transaction (the victim) with an error",
            opt2 = "When a query runs for more than 60 seconds",
            opt3 = "When a database reaches 100% disk capacity",
            correctIndex = 1,
            explanation = "Transaction 1 locks Row A and waits for Row B; Transaction 2 locks Row B and waits for Row A. The database engine detects the circular wait and terminates one transaction so the other can proceed.",
            difficulty = "Beginner",
            tags = "SQL,Concurrency,Deadlock,Locking"
        ),
        q(
            id = "sql_beg_093",
            categoryId = "sql",
            title = "Pessimistic vs Optimistic Locking",
            prompt = "What is the difference between Pessimistic Locking (`SELECT ... FOR UPDATE`) and Optimistic Locking (`version` column)?",
            opt0 = "Pessimistic is for bad developers; Optimistic is for good developers",
            opt1 = "Pessimistic locking immediately locks the database row, blocking concurrent writers until commit; Optimistic locking allows concurrent reads and writes without locks, checking an integer version number on update (`WHERE id = ? AND version = ?`) to detect conflicts",
            opt2 = "Optimistic locking runs in RAM; Pessimistic locking runs on disk",
            opt3 = "Pessimistic locking eliminates the need for transactions",
            correctIndex = 1,
            explanation = "Pessimistic locking prevents conflicts by locking rows upfront. Optimistic locking assumes conflicts are rare: it updates `version = version + 1 WHERE version = old_version`, failing if another user updated first.",
            difficulty = "Beginner",
            tags = "SQL,Concurrency,PessimisticLocking,OptimisticLocking"
        ),
        q(
            id = "sql_beg_094",
            categoryId = "sql",
            title = "Clustered vs Non-Clustered Index",
            prompt = "What is the primary architectural difference between a Clustered Index and a Non-Clustered (Secondary) Index?",
            opt0 = "Clustered indexes are in the cloud; Non-clustered are on-premises",
            opt1 = "A Clustered Index dictates the physical on-disk storage order of the actual table rows (a table can have ONLY ONE clustered index); a Non-Clustered Index is a separate secondary B-Tree holding pointers back to the row",
            opt2 = "Non-clustered indexes cannot be used for sorting",
            opt3 = "Clustered indexes can only index strings",
            correctIndex = 1,
            explanation = "A table can have only one physical storage order, so only ONE Clustered Index can exist per table (typically the Primary Key). Secondary indexes store index keys and point to the clustered key.",
            difficulty = "Beginner",
            tags = "SQL,Indexing,ClusteredIndex,NonClusteredIndex"
        ),
        q(
            id = "sql_beg_095",
            categoryId = "sql",
            title = "EXPLAIN Plan: Purpose in Query Tuning",
            prompt = "What does prefixing a query with `EXPLAIN` (e.g. `EXPLAIN SELECT * FROM orders WHERE customer_id = 42;`) do?",
            opt0 = "It prints a natural language English explanation of what the query does",
            opt1 = "It reveals the Query Execution Plan chosen by the database optimizer, detailing table access methods (Index Scan vs Full Table Scan), join algorithms, estimated row counts, and cost metrics without executing the query",
            opt2 = "It formats the SQL code neatly",
            opt3 = "It executes the query 10 times and takes the average time",
            correctIndex = 1,
            explanation = "EXPLAIN shows the query execution plan generated by the cost-based optimizer (CBO): whether it uses indexes or table scans, join types (Nested Loop, Hash Join, Merge Join), and estimated costs.",
            difficulty = "Beginner",
            tags = "SQL,Performance,EXPLAIN,QueryOptimization"
        ),
        q(
            id = "sql_beg_096",
            categoryId = "sql",
            title = "Full Table Scan vs Index Scan",
            prompt = "Why does an `EXPLAIN` plan showing a 'Seq Scan' / 'Full Table Scan' on a table with 10 Million rows represent a severe performance problem for point lookups?",
            opt0 = "Because table scans cause hardware CPU overheating",
            opt1 = "The database engine must physically read every single disk page and evaluate all 10 Million records sequentially from disk, causing massive disk I/O latency and CPU consumption instead of a sub-millisecond B-Tree lookup",
            opt2 = "Because table scans lock the entire database",
            opt3 = "Because table scans delete the table index",
            correctIndex = 1,
            explanation = "A Full Table Scan reads all data blocks sequentially. For a single row lookup, this is disastrous. Adding an index allows the engine to jump directly to the target record in O(log N).",
            difficulty = "Beginner",
            tags = "SQL,Performance,FullTableScan,Indexing"
        ),
        q(
            id = "sql_beg_097",
            categoryId = "sql",
            title = "Write-Ahead Logging (WAL) / Redo Log Purpose",
            prompt = "What is the primary role of the Write-Ahead Log (WAL in PostgreSQL/SQLite, Redo Log in MySQL/Oracle)?",
            opt0 = "To track developer login attempts",
            opt1 = "To guarantee durability: before any data page modifications are written to the main table files on disk, changes are appended sequentially to the WAL log; on crash recovery, unwritten committed changes are replayed",
            opt2 = "To format SQL queries in HTML",
            opt3 = "To compress old database records",
            correctIndex = 1,
            explanation = "Random disk writes to table files are slow. Appending sequentially to the WAL is lightning fast. Changes MUST be in WAL before dirty memory pages are flushed, guaranteeing crash recovery.",
            difficulty = "Beginner",
            tags = "SQL,StorageEngines,WAL,RedoLog,Durability"
        ),
        q(
            id = "sql_beg_098",
            categoryId = "sql",
            title = "Undo Log Purpose (Rollback and MVCC)",
            prompt = "What is the purpose of the Undo Log in relational engines like MySQL InnoDB?",
            opt0 = "To undo accidental table deletions",
            opt1 = "To record the original historical state of rows before modification, enabling rollback of aborted transactions and providing older row versions for concurrent snapshot isolation reads (MVCC)",
            opt2 = "To undo Git commits",
            opt3 = "To store deleted user passwords",
            correctIndex = 1,
            explanation = "The Undo Log stores old row data: it allows ROLLBACK to restore modified rows, and provides concurrent readers with consistent historical snapshots under MVCC without locking.",
            difficulty = "Beginner",
            tags = "SQL,StorageEngines,UndoLog,MVCC,Transactions"
        ),
        q(
            id = "sql_beg_099",
            categoryId = "sql",
            title = "B-Tree Index Depth and Lookup Efficiency",
            prompt = "In a standard B-Tree index with a fanout of 100 entries per page, how many disk/memory page reads are required to locate any single record among 1,000,000 rows?",
            opt0 = "1,000,000 page reads",
            opt1 = "Only 3 to 4 page reads (since $100^3 = 1,000,000$, tree height is ~3), achieving sub-millisecond retrieval even across massive datasets",
            opt2 = "500 page reads",
            opt3 = "100 page reads",
            correctIndex = 1,
            explanation = "B-Trees have massive fanout (often 100-500 entries per 8KB/16KB page). A 3-to-4 level deep B-Tree indexes tens of millions of rows. Point lookups inspect only 3-4 pages.",
            difficulty = "Beginner",
            tags = "SQL,Indexing,BTree,Math,Performance"
        ),
        q(
            id = "sql_beg_100",
            categoryId = "sql",
            title = "SQL Best Practice: Avoiding SELECT *",
            prompt = "Why is writing `SELECT *` considered a severe anti-pattern in production application code?",
            opt0 = "Because asterisk (*) is deprecated in SQL standard",
            opt1 = "It fetches unnecessary large columns (e.g. TEXT, BLOBs) wasting network bandwidth and RAM, breaks queries if column order changes, and prevents the database from utilizing efficient Index-Only Covering Scans",
            opt2 = "Because databases charge extra money for SELECT *",
            opt3 = "It automatically deletes table comments",
            correctIndex = 1,
            explanation = "Explicit column projection saves network I/O, prevents memory bloat from unneeded text/blobs, and enables Index-Only Scans (satisfying queries purely from the B-Tree index without touching table pages).",
            difficulty = "Beginner",
            tags = "SQL,BestPractices,SELECT,Optimization"
        )
    )
}
